{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Research CT","text":"<p>Unsupervised material segmentation for micro-CT and XRF of sealed historical books.</p>"},{"location":"#what-this-project-does","title":"What this project does","text":"<p>This repository implements a dual-modal pipeline for analyzing historical books without destruction:</p> <ul> <li>CT Pipeline (research_ct): Segments micro-CT volumes into material classes (air, paper, ink, cover, adhesive) using hierarchical GMM + HMRF spatial regularization. No labelled training data required.</li> <li>XRF Pipeline (xrf): Extracts elemental composition from X-Ray Fluorescence data, clusters pages by chemical makeup, and builds per-page leaf signatures for material comparison.</li> </ul>"},{"location":"#where-to-start","title":"Where to start","text":"<ul> <li>New users -&gt; Installation then Quick Start</li> <li>Running CT analysis -&gt; CT Pipeline guide</li> <li>Running XRF analysis -&gt; XRF Pipeline guide</li> <li>API details -&gt; research_ct API / xrf API</li> </ul>"},{"location":"#status-2026-08-09","title":"Status (2026-08-09)","text":"<p>Both pipelines are independently operational on real data (Brevar Capucin CT, Letter_1 XRF). The CT-XRF fusion module (ct_xrf_fusion.py) is planned but not yet implemented.</p>"},{"location":"changelog/","title":"Changelog","text":""},{"location":"changelog/#2026-08-09","title":"2026-08-09","text":"<ul> <li>XRF pipeline (<code>src/xrf/</code>) fully implemented and tested on <code>Letter_1</code> data.</li> <li>Dragonfly export utilities added (<code>dragonfly_exporter.py</code>, <code>dragonfly_utils.py</code>).</li> <li>CT pipeline executed end-to-end on <code>Brevar Capucin</code> real data.</li> <li>Memory bank updated to dual-pipeline milestone.</li> <li>MkDocs documentation site initialized.</li> </ul>"},{"location":"changelog/#2026-07-31","title":"2026-07-31","text":"<ul> <li>Sparse Bayesian GMM implemented (<code>sparse_bayesian_gmm.py</code>).</li> <li>Volume I/O expanded with lazy memmap + chunked streaming (<code>volume_saver.py</code>).</li> <li>Notebooks renamed and consolidated (01-05).</li> <li>All 11 resolved conflicts verified against actual code.</li> </ul>"},{"location":"license/","title":"License","text":"<p>This project is released under the MIT License. See the full license text in the repository root.</p>"},{"location":"api/research_ct/","title":"research_ct \u2014 CT Pipeline API","text":"<p>Auto-generated from source docstrings.</p>"},{"location":"api/research_ct/#io","title":"IO","text":""},{"location":"api/research_ct/#research_ct.io.volume_loader","title":"<code>research_ct.io.volume_loader</code>","text":"<p>Volume I/O: loading and saving 3D micro-CT data.</p> <p>Supports TIFF stacks, PNG sequences, and NIfTI. Handles color-to-grayscale conversion, dtype normalization, and format fallbacks.</p> <p>Functions:</p> Name Description <code>Load_Slice_Stack</code> <p>Load a directory of image slices into a 3D volume.</p> <code>Save_Volume_As_Stack</code> <p>Save a 3D volume as individual slice images.</p>"},{"location":"api/research_ct/#research_ct.io.volume_loader-functions","title":"Functions:","text":""},{"location":"api/research_ct/#research_ct.io.volume_loader.Load_Slice_Stack","title":"<code>Load_Slice_Stack(Folder, Pattern='*.tif*', Sort_Key=None, Start=0, Stop=None, Step=1)</code>","text":"<p>Load a directory of image slices into a 3D volume.</p> <p>Attempts TIFF first, then PNG. Converts color images to grayscale. All slices must have the same (H, W) shape.</p> <p>Parameters:</p> Name Type Description Default <code>Folder</code> <code>Path_Like</code> <p>Directory containing slice images.</p> required <code>Pattern</code> <code>str</code> <p>Glob pattern for selecting files.</p> <code>'*.tif*'</code> <code>Sort_Key</code> <code>Optional[Callable]</code> <p>Function for sorting file names. Default: alphabetical.</p> <code>None</code> <code>Start</code> <code>int</code> <p>Index of the first slice to load (0-based).</p> <code>0</code> <code>Stop</code> <code>Optional[int]</code> <p>Index after the last slice to load. None means load to the end.</p> <code>None</code> <code>Step</code> <code>int</code> <p>Stride between loaded slices.</p> <code>1</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>np.ndarray: Volume of shape (D_subset, H, W), dtype float64.</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>If no matching files found.</p> <code>ValueError</code> <p>If slices have inconsistent shapes.</p> Example Source code in <code>src\\research_ct\\io\\volume_loader.py</code> <pre><code>def Load_Slice_Stack(\n    Folder: Path_Like,\n    Pattern: str = \"*.tif*\",\n    Sort_Key: Optional[Callable] = None,\n    Start: int = 0,\n    Stop: Optional[int] = None,\n    Step: int = 1,\n) -&gt; np.ndarray:\n    \"\"\"\n    Load a directory of image slices into a 3D volume.\n\n    Attempts TIFF first, then PNG. Converts color images to grayscale.\n    All slices must have the same (H, W) shape.\n\n    Args:\n        Folder (Path_Like): Directory containing slice images.\n        Pattern (str): Glob pattern for selecting files.\n        Sort_Key (Optional[Callable]): Function for sorting file names.\n            Default: alphabetical.\n        Start (int): Index of the first slice to load (0-based).\n        Stop (Optional[int]): Index after the last slice to load.\n            None means load to the end.\n        Step (int): Stride between loaded slices.\n\n    Returns:\n        np.ndarray: Volume of shape (D_subset, H, W), dtype float64.\n\n    Raises:\n        FileNotFoundError: If no matching files found.\n        ValueError: If slices have inconsistent shapes.\n\n    Example:\n        &gt;&gt;&gt; # Load all slices\n        &gt;&gt;&gt; vol = Load_Slice_Stack(\"./data/raw\", Pattern=\"reco*.png\")\n        &gt;&gt;&gt; # Load slices 10 through 49 only\n        &gt;&gt;&gt; vol = Load_Slice_Stack(\"./data/raw\", Start=10, Stop=50)\n        &gt;&gt;&gt; # Load every 5th slice\n        &gt;&gt;&gt; vol = Load_Slice_Stack(\"./data/raw\", Step=5)\n    \"\"\"\n    Folder = Path(Folder)\n\n    Files = sorted(Folder.glob(Pattern), key=Sort_Key)\n\n    # Fallback patterns\n    if not Files:\n        for Fallback in [\"*.png\", \"*.jpg\", \"*.bmp\"]:\n            Files = sorted(Folder.glob(Fallback), key=Sort_Key)\n            if Files:\n                break\n\n    if not Files:\n        Available = list(Folder.iterdir())[:10]\n        raise FileNotFoundError(\n            f\"No images in {Folder} matching '{Pattern}'. \"\n            f\"Found: {[p.name for p in Available]}...\"\n        )\n\n    # Apply slice range to the file list\n    Total_Files = len(Files)\n    Effective_Stop = Stop if Stop is not None else Total_Files\n    Files = Files[Start:Effective_Stop:Step]\n\n    if not Files:\n        raise ValueError(\n            f\"Slice range Start={Start} Stop={Stop} Step={Step} \"\n            f\"selected 0 files from {Total_Files} total.\"\n        )\n\n    print(f\"[Load_Slice_Stack] {len(Files)} slices from {Folder} \"\n          f\"(range {Start}:{Effective_Stop}:{Step} of {Total_Files} total)\")\n\n    # Read first slice to determine shape\n    First_Slice = iio.imread(Files[0])\n\n    if First_Slice.ndim == 3:\n        First_Slice = np.mean(First_Slice[..., :3], axis=-1)\n\n    Height, Width = First_Slice.shape\n    Depth = len(Files)\n\n    Volume = np.zeros((Depth, Height, Width), dtype=np.float64)\n    Volume[0] = First_Slice.astype(np.float64)\n\n    # Read remaining slices\n    for Z, File_Path in enumerate(Files[1:], start=1):\n        Slice = iio.imread(File_Path)\n\n        if Slice.ndim == 3:\n            Slice = np.mean(Slice[..., :3], axis=-1)\n\n        if Slice.shape != (Height, Width):\n            raise ValueError(\n                f\"Shape mismatch at {File_Path.name}: \"\n                f\"expected {(Height, Width)}, got {Slice.shape}\"\n            )\n\n        Volume[Z] = Slice.astype(np.float64)\n\n    print(f\"[Load_Slice_Stack] Volume: {Volume.shape}, \"\n          f\"range [{Volume.min():.1f}, {Volume.max():.1f}]\")\n\n    return Volume\n</code></pre>"},{"location":"api/research_ct/#research_ct.io.volume_loader.Load_Slice_Stack--load-all-slices","title":"Load all slices","text":"<p>vol = Load_Slice_Stack(\"./data/raw\", Pattern=\"reco*.png\")</p>"},{"location":"api/research_ct/#research_ct.io.volume_loader.Load_Slice_Stack--load-slices-10-through-49-only","title":"Load slices 10 through 49 only","text":"<p>vol = Load_Slice_Stack(\"./data/raw\", Start=10, Stop=50)</p>"},{"location":"api/research_ct/#research_ct.io.volume_loader.Load_Slice_Stack--load-every-5th-slice","title":"Load every 5th slice","text":"<p>vol = Load_Slice_Stack(\"./data/raw\", Step=5)</p>"},{"location":"api/research_ct/#research_ct.io.volume_loader.Save_Volume_As_Stack","title":"<code>Save_Volume_As_Stack(Volume, Folder, Prefix='slice', Extension='.tiff')</code>","text":"<p>Save a 3D volume as individual slice images.</p> <p>Normalizes to uint8 before saving.</p> <p>Parameters:</p> Name Type Description Default <code>Volume</code> <code>ndarray</code> <p>3D array of shape (D, H, W).</p> required <code>Folder</code> <code>Path_Like</code> <p>Output directory (created if needed).</p> required <code>Prefix</code> <code>str</code> <p>File name prefix.</p> <code>'slice'</code> <code>Extension</code> <code>str</code> <p>File extension.</p> <code>'.tiff'</code> Source code in <code>src\\research_ct\\io\\volume_loader.py</code> <pre><code>def Save_Volume_As_Stack(\n    Volume: np.ndarray,\n    Folder: Path_Like,\n    Prefix: str = \"slice\",\n    Extension: str = \".tiff\"\n) -&gt; None:\n    \"\"\"\n    Save a 3D volume as individual slice images.\n\n    Normalizes to uint8 before saving.\n\n    Args:\n        Volume (np.ndarray): 3D array of shape (D, H, W).\n        Folder (Path_Like): Output directory (created if needed).\n        Prefix (str): File name prefix.\n        Extension (str): File extension.\n    \"\"\"\n    Folder = Path(Folder)\n    Folder.mkdir(parents=True, exist_ok=True)\n\n    for Z in range(Volume.shape[0]):\n        Slice = Volume[Z]\n\n        # Normalize to uint8\n        if Slice.max() &gt; 255 or Slice.min() &lt; 0 or Slice.dtype != np.uint8:\n            Min_Val, Max_Val = Slice.min(), Slice.max()\n            if Max_Val == Min_Val:\n                Slice_U8 = np.zeros_like(Slice, dtype=np.uint8)\n            else:\n                Slice_U8 = (\n                    (Slice - Min_Val) / (Max_Val - Min_Val) * 255.0\n                ).astype(np.uint8)\n        else:\n            Slice_U8 = Slice.astype(np.uint8)\n\n        File_Name = f\"{Prefix}_{Z:04d}{Extension}\"\n        iio.imwrite(Folder / File_Name, Slice_U8)\n\n    print(f\"[Save_Volume_As_Stack] {Volume.shape[0]} slices \u2192 {Folder}\")\n</code></pre>"},{"location":"api/research_ct/#research_ct.io.volume_saver","title":"<code>research_ct.io.volume_saver</code>","text":"<p>Memory-efficient volume I/O with lazy loading and chunked streaming.</p> <p>All load functions default to memmap (lazy) -- no data enters RAM until explicitly materialized via <code>np.array(...)</code>, slicing, or iteration. This prevents 24 GB probability arrays and 5 GB processed volumes from exhausting 32 GB RAM on Windows, where <code>del</code> alone is insufficient.</p> <p>Functions:</p> Name Description <code>Save_As_Numpy</code> <p>Save volume as compressed .npz file.</p> <code>Load_From_Numpy</code> <p>Load a volume from .npz or .npy with memory-mapped (lazy) access.</p> <code>Load_From_Numpy_Chunked</code> <p>Yield concrete, contiguous chunks along axis without loading the</p> <code>Load_From_Numpy_Slab</code> <p>Convenience wrapper: load a concrete Z-slab <code>[z_start, z_stop)</code>.</p> <code>Reduce_Streaming</code> <p>Apply a per-slab reduction/transform to a large on-disk array and</p> <code>Compute_Labels_From_Probabilities</code> <p>Derive integer labels (argmax over last axis) from a large</p> <code>Compute_Confidence_From_Probabilities</code> <p>Derive per-voxel confidence (max prob over last axis) from a large</p>"},{"location":"api/research_ct/#research_ct.io.volume_saver-functions","title":"Functions:","text":""},{"location":"api/research_ct/#research_ct.io.volume_saver.Save_As_Numpy","title":"<code>Save_As_Numpy(Volume, File_Path)</code>","text":"<p>Save volume as compressed .npz file.</p> <p>Parameters:</p> Name Type Description Default <code>Volume</code> <code>ndarray</code> <p>3D array.</p> required <code>File_Path</code> <code>Path_Like</code> <p>Output path with .npz extension.</p> required Source code in <code>src\\research_ct\\io\\volume_saver.py</code> <pre><code>def Save_As_Numpy(\n    Volume: np.ndarray,\n    File_Path: Path_Like,\n) -&gt; Path:\n    \"\"\"Save volume as compressed .npz file.\n\n    Args:\n        Volume: 3D array.\n        File_Path: Output path with .npz extension.\n    \"\"\"\n    File_Path = Path(File_Path)\n    File_Path.parent.mkdir(parents=True, exist_ok=True)\n    np.savez_compressed(File_Path, volume=Volume)\n    print(f\"[Save_As_Numpy] Saved -&gt; {File_Path}\")\n    return File_Path\n</code></pre>"},{"location":"api/research_ct/#research_ct.io.volume_saver.Load_From_Numpy","title":"<code>Load_From_Numpy(File_Path, *, lazy=True, key='volume', z_slice=None)</code>","text":"<p>Load a volume from .npz or .npy with memory-mapped (lazy) access.</p> <p>By default the returned array is a memmap -- no data is read from disk until you index into it.  Use <code>lazy=False</code> or <code>z_slice</code> to materialize a concrete array in RAM.</p> <p>Parameters:</p> Name Type Description Default <code>File_Path</code> <code>Path_Like</code> <p>Path to <code>.npz</code> or <code>.npy</code> file.</p> required <code>lazy</code> <code>bool</code> <p>If True (default), return a memmap / lazy view.  If False, materialize the full array into RAM immediately.</p> <code>True</code> <code>key</code> <code>str</code> <p>Array key inside the <code>.npz</code> archive (ignored for <code>.npy</code>). Defaults to <code>\"volume\"</code>.</p> <code>'volume'</code> <code>z_slice</code> <code>Optional[Union[slice, Tuple[int, Optional[int]]]]</code> <p>Optional Z-axis range to load as a concrete array. Accepts <code>slice(start, stop)</code> or <code>(start, stop)</code>. When provided, the result is always materialized regardless of <code>lazy</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>ndarray</code> <ul> <li>With <code>lazy=True</code> and no <code>z_slice</code>: a memmap-backed array (<code>.npy</code>) or memmap view into the <code>.npz</code> archive.  Shape is read from the header without touching pixel data.</li> </ul> <code>ndarray</code> <ul> <li>With <code>lazy=False</code>: full concrete <code>np.ndarray</code>.</li> </ul> <code>ndarray</code> <ul> <li>With <code>z_slice</code>: concrete slice <code>(Nz, H, W, ...)</code>.</li> </ul> Source code in <code>src\\research_ct\\io\\volume_saver.py</code> <pre><code>def Load_From_Numpy(\n    File_Path: Path_Like,\n    *,\n    lazy: bool = True,\n    key: str = \"volume\",\n    z_slice: Optional[Union[slice, Tuple[int, Optional[int]]]] = None,\n) -&gt; np.ndarray:\n    \"\"\"Load a volume from .npz or .npy with memory-mapped (lazy) access.\n\n    By default the returned array is a **memmap** -- no data is read from\n    disk until you index into it.  Use ``lazy=False`` or ``z_slice`` to\n    materialize a concrete array in RAM.\n\n    Args:\n        File_Path: Path to ``.npz`` or ``.npy`` file.\n        lazy: If True (default), return a memmap / lazy view.  If False,\n            materialize the full array into RAM immediately.\n        key: Array key inside the ``.npz`` archive (ignored for ``.npy``).\n            Defaults to ``\"volume\"``.\n        z_slice: Optional Z-axis range to load as a **concrete** array.\n            Accepts ``slice(start, stop)`` or ``(start, stop)``.\n            When provided, the result is always materialized regardless\n            of ``lazy``.\n\n    Returns:\n        - With ``lazy=True`` and no ``z_slice``: a memmap-backed array\n          (``.npy``) or memmap view into the ``.npz`` archive.  Shape\n          is read from the header without touching pixel data.\n        - With ``lazy=False``: full concrete ``np.ndarray``.\n        - With ``z_slice``: concrete slice ``(Nz, H, W, ...)``.\n    \"\"\"\n    File_Path = Path(File_Path)\n    ext = File_Path.suffix.lower()\n\n    if ext == \".npz\":\n        # np.load with mmap_mode='r' on .npz opens an NpzFile whose\n        # internal arrays are backed by the mmap -- no data read yet.\n        archive = np.load(File_Path, mmap_mode=\"r\")\n        try:\n            array = archive[key]\n        except KeyError:\n            available = list(archive.keys())\n            raise KeyError(\n                f\"Key {key!r} not found in {File_Path.name}. \"\n                f\"Available keys: {available}\"\n            )\n        # Register the archive so it stays alive as long as the array does.\n        _register_npz(array, archive)\n\n    elif ext == \".npy\":\n        # np.load on .npy with mmap_mode returns a memmap directly.\n        array = np.load(File_Path, mmap_mode=\"r\" if lazy else None)\n\n    else:\n        raise ValueError(\n            f\"Unsupported extension {ext!r}. Expected .npz or .npy.\"\n        )\n\n    # ---- Z-slice materialization ------------------------------------------\n    if z_slice is not None:\n        if isinstance(z_slice, tuple):\n            z_slice = slice(*z_slice)\n        # copy=True: array[z_slice] may be a memmap view into the .npz;\n        # we must detach before closing the archive below.\n        result = np.array(array[z_slice], dtype=array.dtype, copy=True)\n        _close_npz(array)\n        print(\n            f\"[Load_From_Numpy] Loaded z-slice {z_slice} -&gt; \"\n            f\"{result.shape}  ({result.nbytes / 1e9:.2f} GB)\"\n        )\n        return result\n\n    # ---- Full materialization ---------------------------------------------\n    if not lazy:\n        result = np.array(array, copy=True)\n        _close_npz(array)\n        print(\n            f\"[Load_From_Numpy] Loaded (materialized): {result.shape}  \"\n            f\"({result.nbytes / 1e9:.2f} GB)\"\n        )\n        return result\n\n    # ---- Lazy (memmap) ----------------------------------------------------\n    print(\n        f\"[Load_From_Numpy] Loaded (lazy): {array.shape}  \"\n        f\"({array.nbytes / 1e9:.2f} GB on disk)\"\n    )\n    return array\n</code></pre>"},{"location":"api/research_ct/#research_ct.io.volume_saver.Load_From_Numpy_Chunked","title":"<code>Load_From_Numpy_Chunked(File_Path, chunk_size, *, axis=0, key='volume', dtype=None, gc_every=10)</code>","text":"<p>Yield concrete, contiguous chunks along axis without loading the full volume into RAM.</p> <p>Each chunk is a writable <code>np.ndarray</code> in C order.  The underlying memmap is discarded after each yield so Windows releases the mapping.</p> <p>Parameters:</p> Name Type Description Default <code>File_Path</code> <code>Path_Like</code> <p><code>.npz</code> or <code>.npy</code> path.</p> required <code>chunk_size</code> <code>int</code> <p>Number of slices (or elements) per chunk along axis.</p> required <code>axis</code> <code>int</code> <p>Axis to chunk along (0 = Z for 3D volumes).</p> <code>0</code> <code>key</code> <code>str</code> <p>Array key inside <code>.npz</code> (ignored for <code>.npy</code>).</p> <code>'volume'</code> <code>dtype</code> <code>Optional[dtype]</code> <p>Optional cast dtype (e.g. <code>np.float32</code>).  If None, the on-disk dtype is preserved.</p> <code>None</code> <code>gc_every</code> <code>int</code> <p>Call <code>gc.collect()</code> every N chunks to keep Windows resident-set size under control.</p> <code>10</code> <p>Yields:</p> Type Description <code>ndarray</code> <p><code>np.ndarray</code> of shape <code>(chunk_size, ...)</code> (last chunk may be</p> <code>ndarray</code> <p>smaller).</p> Source code in <code>src\\research_ct\\io\\volume_saver.py</code> <pre><code>def Load_From_Numpy_Chunked(\n    File_Path: Path_Like,\n    chunk_size: int,\n    *,\n    axis: int = 0,\n    key: str = \"volume\",\n    dtype: Optional[np.dtype] = None,\n    gc_every: int = 10,\n) -&gt; Generator[np.ndarray, None, None]:\n    \"\"\"Yield concrete, contiguous chunks along *axis* without loading the\n    full volume into RAM.\n\n    Each chunk is a writable ``np.ndarray`` in C order.  The underlying\n    memmap is discarded after each yield so Windows releases the mapping.\n\n    Args:\n        File_Path: ``.npz`` or ``.npy`` path.\n        chunk_size: Number of slices (or elements) per chunk along *axis*.\n        axis: Axis to chunk along (0 = Z for 3D volumes).\n        key: Array key inside ``.npz`` (ignored for ``.npy``).\n        dtype: Optional cast dtype (e.g. ``np.float32``).  If None, the\n            on-disk dtype is preserved.\n        gc_every: Call ``gc.collect()`` every N chunks to keep Windows\n            resident-set size under control.\n\n    Yields:\n        ``np.ndarray`` of shape ``(chunk_size, ...)`` (last chunk may be\n        smaller).\n    \"\"\"\n    data = Load_From_Numpy(File_Path, lazy=True, key=key)\n    total = data.shape[axis]\n\n    for start in range(0, total, chunk_size):\n        end = min(start + chunk_size, total)\n        idx = tuple(\n            slice(start, end) if i == axis else slice(None)\n            for i in range(data.ndim)\n        )\n        chunk = np.array(data[idx], dtype=dtype or data.dtype, copy=True,\n                         order=\"C\")\n\n        yield chunk\n\n        del chunk\n        if ((start // chunk_size) + 1) % gc_every == 0:\n            gc.collect()\n\n    _close_npz(data)\n    del data\n    gc.collect()\n</code></pre>"},{"location":"api/research_ct/#research_ct.io.volume_saver.Load_From_Numpy_Slab","title":"<code>Load_From_Numpy_Slab(File_Path, z_start, z_stop, *, key='volume', dtype=None)</code>","text":"<p>Convenience wrapper: load a concrete Z-slab <code>[z_start, z_stop)</code>.</p> <p>Equivalent to <code>Load_From_Numpy(..., z_slice=(z_start, z_stop))</code> with an optional dtype override.</p> Source code in <code>src\\research_ct\\io\\volume_saver.py</code> <pre><code>def Load_From_Numpy_Slab(\n    File_Path: Path_Like,\n    z_start: int,\n    z_stop: int,\n    *,\n    key: str = \"volume\",\n    dtype: Optional[np.dtype] = None,\n) -&gt; np.ndarray:\n    \"\"\"Convenience wrapper: load a concrete Z-slab ``[z_start, z_stop)``.\n\n    Equivalent to ``Load_From_Numpy(..., z_slice=(z_start, z_stop))``\n    with an optional dtype override.\n    \"\"\"\n    slab = Load_From_Numpy(File_Path, z_slice=slice(z_start, z_stop), key=key)\n    if dtype is not None and slab.dtype != dtype:\n        slab = slab.astype(dtype, copy=False)\n    return slab\n</code></pre>"},{"location":"api/research_ct/#research_ct.io.volume_saver.Reduce_Streaming","title":"<code>Reduce_Streaming(File_Path, Output_Path, Reduce_Fn, *, chunk_size=10, key='volume', dtype=None, gc_every=10)</code>","text":"<p>Apply a per-slab reduction/transform to a large on-disk array and stream the result directly to a new .npy file -- the full source array and full result never coexist in RAM.</p> <p>Typical use: deriving labels (argmax) or confidence (max) from a (D, H, W, K) probability volume too large to materialize.</p> <p>Parameters:</p> Name Type Description Default <code>File_Path</code> <code>Path_Like</code> <p>Source .npz or .npy array.</p> required <code>Output_Path</code> <code>Path_Like</code> <p>Destination .npy path for the streamed result.</p> required <code>Reduce_Fn</code> <code>Callable[[ndarray], ndarray]</code> <p>Function applied to each concrete slab of shape (chunk_size, ...); must preserve axis 0 (the chunk/Z axis), e.g. <code>lambda Slab: Slab.argmax(axis=-1)</code>.</p> required <code>chunk_size</code> <code>int</code> <p>Number of slices along axis 0 per slab.</p> <code>10</code> <code>key</code> <code>str</code> <p>Array key inside .npz (ignored for .npy).</p> <code>'volume'</code> <code>dtype</code> <code>Optional[dtype]</code> <p>Output dtype. If None, inferred from Reduce_Fn's first output.</p> <code>None</code> <code>gc_every</code> <code>int</code> <p>Print progress / collect garbage every N slabs.</p> <code>10</code> <p>Returns:</p> Type Description <code>Path</code> <p>Output_Path, now containing a valid streamed .npy file.</p> Source code in <code>src\\research_ct\\io\\volume_saver.py</code> <pre><code>def Reduce_Streaming(\n    File_Path: Path_Like,\n    Output_Path: Path_Like,\n    Reduce_Fn: Callable[[np.ndarray], np.ndarray],\n    *,\n    chunk_size: int = 10,\n    key: str = \"volume\",\n    dtype: Optional[np.dtype] = None,\n    gc_every: int = 10,\n) -&gt; Path:\n    \"\"\"Apply a per-slab reduction/transform to a large on-disk array and\n    stream the result directly to a new .npy file -- the full source\n    array and full result never coexist in RAM.\n\n    Typical use: deriving labels (argmax) or confidence (max) from a\n    (D, H, W, K) probability volume too large to materialize.\n\n    Args:\n        File_Path: Source .npz or .npy array.\n        Output_Path: Destination .npy path for the streamed result.\n        Reduce_Fn: Function applied to each concrete slab of shape\n            (chunk_size, ...); must preserve axis 0 (the chunk/Z axis),\n            e.g. ``lambda Slab: Slab.argmax(axis=-1)``.\n        chunk_size: Number of slices along axis 0 per slab.\n        key: Array key inside .npz (ignored for .npy).\n        dtype: Output dtype. If None, inferred from Reduce_Fn's first\n            output.\n        gc_every: Print progress / collect garbage every N slabs.\n\n    Returns:\n        Output_Path, now containing a valid streamed .npy file.\n    \"\"\"\n    Output_Path = Path(Output_Path)\n    Output_Path.parent.mkdir(parents=True, exist_ok=True)\n\n    # Peek shape only -- header read, no pixel data touched.\n    Source = Load_From_Numpy(File_Path, lazy=True, key=key)\n    Total = Source.shape[0]\n    _close_npz(Source)\n    del Source\n\n    Header_Written = False\n    Out_Dtype: Optional[np.dtype] = dtype\n    Written = 0\n\n    with open(Output_Path, \"wb\") as f:\n        for Chunk_Index, Slab in enumerate(\n            Load_From_Numpy_Chunked(\n                File_Path, chunk_size, axis=0, key=key, gc_every=gc_every\n            )\n        ):\n            Result_Slab = Reduce_Fn(Slab)\n            del Slab\n\n            if not Header_Written:\n                Out_Dtype = Out_Dtype or Result_Slab.dtype\n                Out_Shape = (Total,) + Result_Slab.shape[1:]\n                _write_npy_header(f, shape=Out_Shape, dtype=Out_Dtype)\n                Header_Written = True\n\n            f.write(np.ascontiguousarray(Result_Slab, dtype=Out_Dtype).tobytes())\n            f.flush()\n            Written += Result_Slab.shape[0]\n            del Result_Slab\n\n            if (Chunk_Index + 1) % gc_every == 0:\n                print(f\"[Reduce_Streaming] {Written}/{Total}\")\n\n    gc.collect()\n    print(f\"[Reduce_Streaming] Saved -&gt; {Output_Path}\")\n    return Output_Path\n</code></pre>"},{"location":"api/research_ct/#research_ct.io.volume_saver.Compute_Labels_From_Probabilities","title":"<code>Compute_Labels_From_Probabilities(Probabilities_Path, Output_Path, *, chunk_size=10, key='volume')</code>","text":"<p>Derive integer labels (argmax over last axis) from a large (D, H, W, K) probability volume, streamed slab-by-slab.</p> <p>Equivalent to <code>Probabilities.argmax(axis=-1)</code> but memory-bounded -- avoids materializing the full probability array to compute it.</p> Source code in <code>src\\research_ct\\io\\volume_saver.py</code> <pre><code>def Compute_Labels_From_Probabilities(\n    Probabilities_Path: Path_Like,\n    Output_Path: Path_Like,\n    *,\n    chunk_size: int = 10,\n    key: str = \"volume\",\n) -&gt; Path:\n    \"\"\"Derive integer labels (argmax over last axis) from a large\n    (D, H, W, K) probability volume, streamed slab-by-slab.\n\n    Equivalent to ``Probabilities.argmax(axis=-1)`` but memory-bounded --\n    avoids materializing the full probability array to compute it.\n    \"\"\"\n    return Reduce_Streaming(\n        Probabilities_Path,\n        Output_Path,\n        Reduce_Fn=lambda Slab: Slab.argmax(axis=-1).astype(np.int32),\n        chunk_size=chunk_size,\n        key=key,\n        dtype=np.int32,\n    )\n</code></pre>"},{"location":"api/research_ct/#research_ct.io.volume_saver.Compute_Confidence_From_Probabilities","title":"<code>Compute_Confidence_From_Probabilities(Probabilities_Path, Output_Path, *, chunk_size=10, key='volume')</code>","text":"<p>Derive per-voxel confidence (max prob over last axis) from a large (D, H, W, K) probability volume, streamed slab-by-slab.</p> <p>Equivalent to <code>Probabilities.max(axis=-1)</code> but memory-bounded.</p> Source code in <code>src\\research_ct\\io\\volume_saver.py</code> <pre><code>def Compute_Confidence_From_Probabilities(\n    Probabilities_Path: Path_Like,\n    Output_Path: Path_Like,\n    *,\n    chunk_size: int = 10,\n    key: str = \"volume\",\n) -&gt; Path:\n    \"\"\"Derive per-voxel confidence (max prob over last axis) from a large\n    (D, H, W, K) probability volume, streamed slab-by-slab.\n\n    Equivalent to ``Probabilities.max(axis=-1)`` but memory-bounded.\n    \"\"\"\n    return Reduce_Streaming(\n        Probabilities_Path,\n        Output_Path,\n        Reduce_Fn=lambda Slab: Slab.max(axis=-1).astype(np.float32),\n        chunk_size=chunk_size,\n        key=key,\n        dtype=np.float32,\n    )\n</code></pre>"},{"location":"api/research_ct/#research_ct.io.metadata_parser","title":"<code>research_ct.io.metadata_parser</code>","text":"<p>Metadata extraction from TIFF stacks and manual inference.</p> <p>Classes:</p> Name Description <code>Scan_Metadata</code> <p>Inferred metadata from TIFF volume analysis.</p> <p>Functions:</p> Name Description <code>Load_Metadata</code> <p>Infer metadata from a loaded numpy volume.</p>"},{"location":"api/research_ct/#research_ct.io.metadata_parser-classes","title":"Classes","text":""},{"location":"api/research_ct/#research_ct.io.metadata_parser.Scan_Metadata","title":"<code>Scan_Metadata(Voxel_Size_Um=(1.0, 1.0, 1.0), Volume_Shape=(0, 0, 0), Bit_Depth=16, Intensity_Range=(0.0, 65535.0), Scanner_Model='Unknown', Num_Pages=None, Page_Thickness_Voxels=None, Raw_Metadata=None)</code>  <code>dataclass</code>","text":"<p>Inferred metadata from TIFF volume analysis.</p> <p>Since raw TIFFs have no equipment metadata, all values are inferred from pixel statistics or set to defaults.</p> <p>Attributes:</p> Name Type Description <code>Voxel_Size_Um</code> <code>Tuple[float, float, float]</code> <p>Default (1, 1, 1) unless calibrated.</p> <code>Volume_Shape</code> <code>Tuple[int, int, int]</code> <p>(D, H, W) from array.</p> <code>Bit_Depth</code> <code>int</code> <p>From dtype (8, 16, 32).</p> <code>Intensity_Range</code> <code>Tuple[float, float]</code> <p>(min, max) from actual data.</p> <code>Scanner_Model</code> <code>str</code> <p>\"Unknown\" (no equipment metadata).</p> <code>Num_Pages</code> <code>Optional[int]</code> <p>None (must be set manually if known).</p>"},{"location":"api/research_ct/#research_ct.io.metadata_parser-functions","title":"Functions:","text":""},{"location":"api/research_ct/#research_ct.io.metadata_parser.Load_Metadata","title":"<code>Load_Metadata(Volume, Voxel_Size_Um=None, Num_Pages=None)</code>","text":"<p>Infer metadata from a loaded numpy volume.</p> <p>This is the primary entry point when no external metadata exists. All values are derived from array properties.</p> <p>Parameters:</p> Name Type Description Default <code>Volume</code> <code>ndarray</code> <p>Loaded 3D volume.</p> required <code>Voxel_Size_Um</code> <code>Optional[float]</code> <p>Voxel size if known from calibration.</p> <code>None</code> <code>Num_Pages</code> <code>Optional[int]</code> <p>Expected page count if known.</p> <code>None</code> <p>Returns:</p> Type Description <code>Scan_Metadata</code> <p>Scan_Metadata with inferred values.</p> Source code in <code>src\\research_ct\\io\\metadata_parser.py</code> <pre><code>def Load_Metadata(\n    Volume: np.ndarray,\n    Voxel_Size_Um: Optional[float] = None,\n    Num_Pages: Optional[int] = None,\n) -&gt; Scan_Metadata:\n    \"\"\"Infer metadata from a loaded numpy volume.\n\n    This is the primary entry point when no external metadata exists.\n    All values are derived from array properties.\n\n    Args:\n        Volume: Loaded 3D volume.\n        Voxel_Size_Um: Voxel size if known from calibration.\n        Num_Pages: Expected page count if known.\n\n    Returns:\n        Scan_Metadata with inferred values.\n    \"\"\"\n    D, H, W = Volume.shape\n\n    # Bit depth from dtype\n    if Volume.dtype == np.uint8:\n        Bit_Depth = 8\n    elif Volume.dtype == np.uint16:\n        Bit_Depth = 16\n    elif Volume.dtype == np.uint32:\n        Bit_Depth = 32\n    else:\n        Bit_Depth = Volume.dtype.itemsize * 8\n\n    # Intensity range from actual data\n    Intensity_Min = float(Volume.min())\n    Intensity_Max = float(Volume.max())\n\n    # Theoretical max for integer types\n    if np.issubdtype(Volume.dtype, np.integer):\n        Theoretical_Max = float(2**Bit_Depth - 1)\n        Intensity_Max = max(Intensity_Max, Theoretical_Max)\n\n    Metadata = Scan_Metadata(\n        Voxel_Size_Um=(Voxel_Size_Um or 1.0,) * 3,\n        Volume_Shape=(D, H, W),\n        Bit_Depth=Bit_Depth,\n        Intensity_Range=(Intensity_Min, Intensity_Max),\n        Num_Pages=Num_Pages,\n        Raw_Metadata={\n            \"inferred\": True,\n            \"dtype\": str(Volume.dtype),\n            \"actual_data_range\": (float(Volume.min()), float(Volume.max())),\n        },\n    )\n\n    print(f\"[Load_Metadata] Inferred: shape={Metadata.Volume_Shape}, \"\n          f\"bit_depth={Metadata.Bit_Depth}, range={Metadata.Intensity_Range}\")\n\n    return Metadata\n</code></pre>"},{"location":"api/research_ct/#research_ct.io.dragonfly_exporter","title":"<code>research_ct.io.dragonfly_exporter</code>","text":"<p>Low-level multi-page TIFF export for Dragonfly.</p> <p>Saves 3D volumes as ImageJ-compatible multi-page TIFF stacks with voxel-spacing metadata injected into the TIFF tags so Dragonfly recognizes them as calibrated 3D images.</p> <p>Functions:</p> Name Description <code>Save_Volume_As_Tiff</code> <p>Save a 3D volume as an ImageJ-style multi-page TIFF stack.</p> <code>Verify_Dragonfly_Channels</code> <p>Quick integrity check on exported TIFF stacks.</p> <code>Save_Dragonfly_Metadata_Json</code> <p>Write a human-readable JSON sidecar with acquisition metadata.</p> <code>Save_Label_Colors_Csv</code> <p>Write a CSV mapping <code>label_id -&gt; (R, G, B, Name)</code>.</p>"},{"location":"api/research_ct/#research_ct.io.dragonfly_exporter-functions","title":"Functions:","text":""},{"location":"api/research_ct/#research_ct.io.dragonfly_exporter.Save_Volume_As_Tiff","title":"<code>Save_Volume_As_Tiff(Volume, Output_Path, *, Voxel_Spacing=(1.0, 1.0, 1.0), Spacing_Units='um', Compression=None, Big_Tiff=None, Big_Tiff_Threshold_Bytes=2 * 1024 ** 3)</code>","text":"<p>Save a 3D volume as an ImageJ-style multi-page TIFF stack.</p> <p>Each Z-slice is written as one page.  Voxel spacing is stored in the XResolution / YResolution tags (converted to pixels/cm) and in an ImageJ metadata string so that Dragonfly auto-calibrates the image on import.</p> <p>Parameters:</p> Name Type Description Default <code>Volume</code> <code>ndarray</code> <p>3-D array (Z, Y, X).  May be a memmap; slices are fetched one at a time to avoid full materialisation.</p> required <code>Output_Path</code> <code>Path_Like</code> <p>Destination <code>.tif</code> file path.</p> required <code>Voxel_Spacing</code> <code>Tuple[float, float, float]</code> <p>(Z_spacing, Y_spacing, X_spacing) in Spacing_Units.</p> <code>(1.0, 1.0, 1.0)</code> <code>Spacing_Units</code> <code>str</code> <p>Label for the spacing unit (<code>\"um\"</code>, <code>\"mm\"</code>).</p> <code>'um'</code> <code>Compression</code> <code>Optional[str]</code> <p>Pass-through to <code>tifffile.TiffWriter.write</code> (<code>None</code>, <code>\"zlib\"</code>, <code>\"lzma\"</code>, \u2026).  None = uncompressed.</p> <code>None</code> <code>Big_Tiff</code> <code>Optional[bool]</code> <p>Force BigTIFF mode.  If None, auto-detect from Big_Tiff_Threshold_Bytes.</p> <code>None</code> <code>Big_Tiff_Threshold_Bytes</code> <code>int</code> <p>When the projected file size exceeds this threshold (and Big_Tiff is None), BigTIFF is enabled automatically.</p> <code>2 * 1024 ** 3</code> <p>Returns:</p> Type Description <code>Path</code> <p>Output_Path as a <code>Path</code>.</p> Source code in <code>src\\research_ct\\io\\dragonfly_exporter.py</code> <pre><code>def Save_Volume_As_Tiff(\n    Volume: np.ndarray,\n    Output_Path: Path_Like,\n    *,\n    Voxel_Spacing: Tuple[float, float, float] = (1.0, 1.0, 1.0),\n    Spacing_Units: str = \"um\",\n    Compression: Optional[str] = None,\n    Big_Tiff: Optional[bool] = None,\n    Big_Tiff_Threshold_Bytes: int = 2 * 1024**3,\n) -&gt; Path:\n    \"\"\"Save a 3D volume as an ImageJ-style multi-page TIFF stack.\n\n    Each Z-slice is written as one page.  Voxel spacing is stored in\n    the XResolution / YResolution tags (converted to pixels/cm) and in\n    an ImageJ metadata string so that Dragonfly auto-calibrates the\n    image on import.\n\n    Args:\n        Volume: 3-D array (Z, Y, X).  May be a memmap; slices are fetched\n            one at a time to avoid full materialisation.\n        Output_Path: Destination ``.tif`` file path.\n        Voxel_Spacing: (Z_spacing, Y_spacing, X_spacing) in *Spacing_Units*.\n        Spacing_Units: Label for the spacing unit (``\"um\"``, ``\"mm\"``).\n        Compression: Pass-through to ``tifffile.TiffWriter.write``\n            (``None``, ``\"zlib\"``, ``\"lzma\"``, \u2026).  None = uncompressed.\n        Big_Tiff: Force BigTIFF mode.  If *None*, auto-detect from\n            *Big_Tiff_Threshold_Bytes*.\n        Big_Tiff_Threshold_Bytes: When the projected file size exceeds\n            this threshold (and *Big_Tiff* is None), BigTIFF is enabled\n            automatically.\n\n    Returns:\n        *Output_Path* as a ``Path``.\n    \"\"\"\n    Output_Path = Path(Output_Path)\n    Output_Path.parent.mkdir(parents=True, exist_ok=True)\n\n    D, H, W = Volume.shape[:3]\n    Z_Spacing, Y_Spacing, X_Spacing = Voxel_Spacing\n\n    # --- ImageJ resolution: pixels per cm ----------------------------------\n    # tifffile `imagej=True` expects (x_res, y_res) in px/cm.\n    # 1 um pixel -&gt; 10 000 px/cm; 1 mm pixel -&gt; 10 px/cm\n    Unit_To_Cm: Dict[str, float] = {\n        \"um\": 1e4,\n        \"mm\": 10.0,\n        \"cm\": 1.0,\n    }\n    Scale = Unit_To_Cm.get(Spacing_Units.lower(), 1e4)\n    Res_X = Scale / X_Spacing if X_Spacing &gt; 0 else 1.0\n    Res_Y = Scale / Y_Spacing if Y_Spacing &gt; 0 else 1.0\n\n    ImageJ_Meta: Dict[str, object] = {\n        \"spacing\": Z_Spacing,\n        \"unit\": Spacing_Units,\n        \"axes\": \"ZYX\",\n    }\n\n    # ---- BigTIFF auto-detection -------------------------------------------\n    if Big_Tiff is None:\n        Est_Bytes = int(D) * int(H) * int(W) * np.dtype(Volume.dtype).itemsize\n        Big_Tiff = Est_Bytes &gt; Big_Tiff_Threshold_Bytes\n\n    # ---- Write ------------------------------------------------------------\n    with tifffile.TiffWriter(Output_Path, bigtiff=Big_Tiff, imagej=True) as Writer:\n        Writer.write(\n            Volume,\n            resolution=(Res_X, Res_Y),\n            resolutionunit=tifffile.RESUNIT.CENTIMETER,\n            metadata=ImageJ_Meta,\n            compression=Compression,\n        )\n\n    print(\n        f\"[DragonflyExport] Saved -&gt; {Output_Path}  \"\n        f\"({D}x{H}x{W}, {Volume.dtype}, bigtiff={Big_Tiff})\"\n    )\n    return Output_Path\n</code></pre>"},{"location":"api/research_ct/#research_ct.io.dragonfly_exporter.Verify_Dragonfly_Channels","title":"<code>Verify_Dragonfly_Channels(Channel_Paths, *, Expected_Shape=None)</code>","text":"<p>Quick integrity check on exported TIFF stacks.</p> <p>Opens the first page of each file and reports dimensions vs expected shape.</p> <p>Parameters:</p> Name Type Description Default <code>Channel_Paths</code> <code>Dict[str, Path]</code> <p>Dict <code>{\"grayscale\": Path(...), \"labels\": ..., ...}</code>.</p> required <code>Expected_Shape</code> <code>Optional[Tuple[int, int, int]]</code> <p>Expected <code>(Z, Y, X)</code>.  If None, shape is inferred from the first channel and all others must match.</p> <code>None</code> <p>Returns:</p> Type Description <code>Dict[str, str]</code> <p>Dict mapping channel name to <code>\"OK\"</code> or an error message.</p> Source code in <code>src\\research_ct\\io\\dragonfly_exporter.py</code> <pre><code>def Verify_Dragonfly_Channels(\n    Channel_Paths: Dict[str, Path],\n    *,\n    Expected_Shape: Optional[Tuple[int, int, int]] = None,\n) -&gt; Dict[str, str]:\n    \"\"\"Quick integrity check on exported TIFF stacks.\n\n    Opens the first page of each file and reports dimensions vs\n    expected shape.\n\n    Args:\n        Channel_Paths: Dict ``{\"grayscale\": Path(...), \"labels\": ..., ...}``.\n        Expected_Shape: Expected ``(Z, Y, X)``.  If None, shape is\n            inferred from the first channel and all others must match.\n\n    Returns:\n        Dict mapping channel name to ``\"OK\"`` or an error message.\n    \"\"\"\n    Status: Dict[str, str] = {}\n    Ref_Shape: Optional[Tuple[int, int, int]] = Expected_Shape\n\n    for Name, Path in Channel_Paths.items():\n        if not Path.exists():\n            Status[Name] = f\"MISSING ({Path})\"\n            continue\n\n        try:\n            with tifffile.TiffFile(Path) as Tiff:\n                Pages = len(Tiff.pages)\n                if Pages == 0:\n                    Status[Name] = \"EMPTY\"\n                    continue\n                H, W = Tiff.pages[0].shape\n\n            Shape = (Pages, H, W)\n            if Ref_Shape is None:\n                Ref_Shape = Shape\n            if Shape != Ref_Shape:\n                Status[Name] = (\n                    f\"SHAPE MISMATCH: got {Shape}, expected {Ref_Shape}\"\n                )\n            else:\n                Status[Name] = \"OK\"\n        except Exception as Exc:\n            Status[Name] = f\"ERROR: {Exc}\"\n\n    return Status\n</code></pre>"},{"location":"api/research_ct/#research_ct.io.dragonfly_exporter.Save_Dragonfly_Metadata_Json","title":"<code>Save_Dragonfly_Metadata_Json(Output_Path, Shape, Voxel_Spacing, Spacing_Units='um', Channels=None, Label_Colors=None, Label_Names=None)</code>","text":"<p>Write a human-readable JSON sidecar with acquisition metadata.</p> <p>Parameters:</p> Name Type Description Default <code>Output_Path</code> <code>Path_Like</code> <p>Destination <code>.json</code> path.</p> required <code>Shape</code> <code>Tuple[int, int, int]</code> <p>Volume shape <code>(Z, Y, X)</code>.</p> required <code>Voxel_Spacing</code> <code>Tuple[float, float, float]</code> <p><code>(Z, Y, X)</code> spacing in Spacing_Units.</p> required <code>Spacing_Units</code> <code>str</code> <p>Unit label (<code>\"um\"</code>, <code>\"mm\"</code>).</p> <code>'um'</code> <code>Channels</code> <code>Optional[Dict[str, Dict[str, object]]]</code> <p>Optional dict of channel metadata keyed by channel name.</p> <code>None</code> <code>Label_Colors</code> <code>Optional[Dict[int, Tuple[int, int, int]]]</code> <p>Optional <code>{label_id: (R, G, B)}</code>.</p> <code>None</code> <code>Label_Names</code> <code>Optional[Dict[int, str]]</code> <p>Optional <code>{label_id: \"name\"}</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>Path</code> <p>Output_Path as <code>Path</code>.</p> Source code in <code>src\\research_ct\\io\\dragonfly_exporter.py</code> <pre><code>def Save_Dragonfly_Metadata_Json(\n    Output_Path: Path_Like,\n    Shape: Tuple[int, int, int],\n    Voxel_Spacing: Tuple[float, float, float],\n    Spacing_Units: str = \"um\",\n    Channels: Optional[Dict[str, Dict[str, object]]] = None,\n    Label_Colors: Optional[Dict[int, Tuple[int, int, int]]] = None,\n    Label_Names: Optional[Dict[int, str]] = None,\n) -&gt; Path:\n    \"\"\"Write a human-readable JSON sidecar with acquisition metadata.\n\n    Args:\n        Output_Path: Destination ``.json`` path.\n        Shape: Volume shape ``(Z, Y, X)``.\n        Voxel_Spacing: ``(Z, Y, X)`` spacing in *Spacing_Units*.\n        Spacing_Units: Unit label (``\"um\"``, ``\"mm\"``).\n        Channels: Optional dict of channel metadata keyed by channel name.\n        Label_Colors: Optional ``{label_id: (R, G, B)}``.\n        Label_Names: Optional ``{label_id: \"name\"}``.\n\n    Returns:\n        *Output_Path* as ``Path``.\n    \"\"\"\n    Output_Path = Path(Output_Path)\n    Output_Path.parent.mkdir(parents=True, exist_ok=True)\n\n    Doc: Dict[str, object] = {\n        \"shape\": list(Shape),\n        \"voxel_spacing\": {\n            \"z\": Voxel_Spacing[0],\n            \"y\": Voxel_Spacing[1],\n            \"x\": Voxel_Spacing[2],\n        },\n        \"spacing_units\": Spacing_Units,\n    }\n    if Channels is not None:\n        Doc[\"channels\"] = Channels\n    if Label_Colors is not None:\n        Doc[\"label_colors\"] = {\n            str(K): list(V) for K, V in Label_Colors.items()\n        }\n    if Label_Names is not None:\n        Doc[\"label_names\"] = {str(K): V for K, V in Label_Names.items()}\n\n    with open(Output_Path, \"w\", encoding=\"utf-8\") as Fh:\n        json.dump(Doc, Fh, indent=2, ensure_ascii=False)\n\n    print(f\"[DragonflyExport] Metadata -&gt; {Output_Path}\")\n    return Output_Path\n</code></pre>"},{"location":"api/research_ct/#research_ct.io.dragonfly_exporter.Save_Label_Colors_Csv","title":"<code>Save_Label_Colors_Csv(Output_Path, Label_Colors, Label_Names=None)</code>","text":"<p>Write a CSV mapping <code>label_id -&gt; (R, G, B, Name)</code>.</p> <p>Dragonfly can import this CSV to colour-code segmentations.</p> <p>Parameters:</p> Name Type Description Default <code>Output_Path</code> <code>Path_Like</code> <p>Destination <code>.csv</code> path.</p> required <code>Label_Colors</code> <code>Dict[int, Tuple[int, int, int]]</code> <p><code>{label_id: (R, G, B)}</code> with values 0-255.</p> required <code>Label_Names</code> <code>Optional[Dict[int, str]]</code> <p>Optional <code>{label_id: \"name\"}</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>Path</code> <p>Output_Path as <code>Path</code>.</p> Source code in <code>src\\research_ct\\io\\dragonfly_exporter.py</code> <pre><code>def Save_Label_Colors_Csv(\n    Output_Path: Path_Like,\n    Label_Colors: Dict[int, Tuple[int, int, int]],\n    Label_Names: Optional[Dict[int, str]] = None,\n) -&gt; Path:\n    \"\"\"Write a CSV mapping ``label_id -&gt; (R, G, B, Name)``.\n\n    Dragonfly can import this CSV to colour-code segmentations.\n\n    Args:\n        Output_Path: Destination ``.csv`` path.\n        Label_Colors: ``{label_id: (R, G, B)}`` with values 0-255.\n        Label_Names: Optional ``{label_id: \"name\"}``.\n\n    Returns:\n        *Output_Path* as ``Path``.\n    \"\"\"\n    import csv\n\n    Output_Path = Path(Output_Path)\n    Output_Path.parent.mkdir(parents=True, exist_ok=True)\n\n    with open(Output_Path, \"w\", newline=\"\", encoding=\"utf-8\") as Fh:\n        Writer = csv.writer(Fh)\n        Writer.writerow([\"Label_ID\", \"R\", \"G\", \"B\", \"Name\"])\n\n        for Lab_Id in sorted(Label_Colors.keys()):\n            R, G, B = Label_Colors[Lab_Id]\n            Name = (\n                Label_Names.get(Lab_Id, f\"Class_{Lab_Id}\")\n                if Label_Names\n                else f\"Class_{Lab_Id}\"\n            )\n            Writer.writerow([Lab_Id, R, G, B, Name])\n\n    print(f\"[DragonflyExport] Label colours -&gt; {Output_Path}\")\n    return Output_Path\n</code></pre>"},{"location":"api/research_ct/#preprocessing","title":"Preprocessing","text":""},{"location":"api/research_ct/#research_ct.preprocessing.pipeline_revised","title":"<code>research_ct.preprocessing.pipeline_revised</code>","text":"<p>Revised preprocessing pipeline optimized for GMM/HMRF segmentation.</p> <p>Replaces the visual-enhancement pipeline with a statistics-first approach that preserves global intensity relationships and produces Gaussian-mixture-friendly histograms.</p> <p>Functions:</p> Name Description <code>Preprocess_For_Gmm_Revised</code> <p>Complete preprocessing pipeline for GMM segmentation.</p>"},{"location":"api/research_ct/#research_ct.preprocessing.pipeline_revised-functions","title":"Functions:","text":""},{"location":"api/research_ct/#research_ct.preprocessing.pipeline_revised.Preprocess_For_Gmm_Revised","title":"<code>Preprocess_For_Gmm_Revised(Volume, Background_Sigma=None, Noise_Sigma=0.8, Clip_Low_Percentile=0.1, Clip_High_Percentile=99.9, Check_Stationarity=True, Apply_Slice_Standardization=False, Verbose=True)</code>","text":"<p>Complete preprocessing pipeline for GMM segmentation.</p> Pipeline <ol> <li>Global background correction (beam hardening removal)</li> <li>Mild Gaussian noise reduction</li> <li>Optional per-slice standardization (if non-stationary)</li> <li>Global percentile normalization</li> </ol> <p>Parameters:</p> Name Type Description Default <code>Volume</code> <code>ndarray</code> <p>Raw 3D volume (D, H, W), any numeric dtype.</p> required <code>Background_Sigma</code> <code>Optional[float]</code> <p>Gaussian sigma for background estimation. If None, auto-estimated from volume dimensions.</p> <code>None</code> <code>Noise_Sigma</code> <code>float</code> <p>Gaussian sigma for noise reduction (default 0.8).</p> <code>0.8</code> <code>Clip_Low_Percentile</code> <code>float</code> <p>Lower percentile for global clipping.</p> <code>0.1</code> <code>Clip_High_Percentile</code> <code>float</code> <p>Upper percentile for global clipping.</p> <code>99.9</code> <code>Check_Stationarity</code> <code>bool</code> <p>Whether to test for slice-to-slice drift.</p> <code>True</code> <code>Apply_Slice_Standardization</code> <code>bool</code> <p>Force per-slice z-score even if stationary. Use with caution.</p> <code>False</code> <code>Verbose</code> <code>bool</code> <p>Print progress messages.</p> <code>True</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>Tuple of (preprocessed_volume, diagnostics_dict).</p> <code>Dict</code> <p>diagnostics_dict contains histogram stats and GMM readiness.</p> Source code in <code>src\\research_ct\\preprocessing\\pipeline_revised.py</code> <pre><code>def Preprocess_For_Gmm_Revised(\n    Volume: np.ndarray,\n    Background_Sigma: Optional[float] = None,\n    Noise_Sigma: float = 0.8,\n    Clip_Low_Percentile: float = 0.1,\n    Clip_High_Percentile: float = 99.9,\n    Check_Stationarity: bool = True,\n    Apply_Slice_Standardization: bool = False,\n    Verbose: bool = True,\n) -&gt; Tuple[np.ndarray, Dict]:\n    \"\"\"Complete preprocessing pipeline for GMM segmentation.\n\n    Pipeline:\n        1. Global background correction (beam hardening removal)\n        2. Mild Gaussian noise reduction\n        3. Optional per-slice standardization (if non-stationary)\n        4. Global percentile normalization\n\n    Args:\n        Volume: Raw 3D volume (D, H, W), any numeric dtype.\n        Background_Sigma: Gaussian sigma for background estimation.\n            If None, auto-estimated from volume dimensions.\n        Noise_Sigma: Gaussian sigma for noise reduction (default 0.8).\n        Clip_Low_Percentile: Lower percentile for global clipping.\n        Clip_High_Percentile: Upper percentile for global clipping.\n        Check_Stationarity: Whether to test for slice-to-slice drift.\n        Apply_Slice_Standardization: Force per-slice z-score even if\n            stationary. Use with caution.\n        Verbose: Print progress messages.\n\n    Returns:\n        Tuple of (preprocessed_volume, diagnostics_dict).\n        diagnostics_dict contains histogram stats and GMM readiness.\n    \"\"\"\n    # \u2500\u2500 Input validation \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    if Volume.ndim != 3:\n        raise ValueError(f\"Expected 3D volume, got shape {Volume.shape} ({Volume.ndim}D)\")\n    if Volume.size == 0:\n        raise ValueError(\"Volume is empty\")\n    if not np.isfinite(Volume).all():\n        raise ValueError(\"Volume contains NaN or Inf values\")\n\n    D, H, W = Volume.shape\n\n    def _vprint(*args, **kwargs):\n        \"\"\"Conditional verbose print \u2014 single guard for all logging.\"\"\"\n        if Verbose:\n            print(*args, **kwargs)\n\n    _vprint(\n        f\"[Preprocess_GMM] Input: {Volume.shape}, \"\n        f\"dtype={Volume.dtype}, range [{Volume.min():.1f}, {Volume.max():.1f}]\"\n    )\n\n    # \u2500\u2500 Step 0: Convert to float64 once \u2014 this is the ONLY allocation \u2500\u2500\n    Working = Volume.astype(np.float64, copy=True)\n\n    if Background_Sigma is None:\n        Background_Sigma = Auto_Estimate_Background_Sigma(Working)\n\n    # \u2500\u2500 Step 1: Global background correction (in-place via out=) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    _vprint(f\"\\n[Step 1/4] Background correction (sigma={Background_Sigma:.1f})...\")\n    t0 = time.time()\n\n    Working = Correct_Background_Volume(Working, Background_Sigma, out=Working)\n\n    _vprint(\n        f\"[Step 1/4] Done in {time.time() - t0:.1f}s. \"\n        f\"Range: [{Working.min():.1f}, {Working.max():.1f}]\"\n    )\n\n    # \u2500\u2500 Step 2: Noise reduction (in-place via scipy output=) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    _vprint(f\"\\n[Step 2/4] Noise reduction (sigma={Noise_Sigma})...\")\n    t0 = time.time()\n\n    Working = Reduce_Noise_Volume(Working, Noise_Sigma, out=Working)\n\n    _vprint(\n        f\"[Step 2/4] Done in {time.time() - t0:.1f}s. \"\n        f\"Range: [{Working.min():.1f}, {Working.max():.1f}]\"\n    )\n\n    # \u2500\u2500 Step 3: Stationarity check and optional standardization \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    Need_Standardization = Apply_Slice_Standardization\n\n    if Check_Stationarity and not Apply_Slice_Standardization:\n        _vprint(\"\\n[Step 3/4] Checking slice stationarity...\")\n        Is_Stationary, Similarity = Check_Slice_Stationarity(Working)\n        _vprint(\n            f\"[Step 3/4] Stationarity: {Is_Stationary} \"\n            f\"(similarity={Similarity:.3f})\"\n        )\n        Need_Standardization = not Is_Stationary\n\n    if Need_Standardization:\n        _vprint(\"[Step 3/4] Applying per-slice standardization...\")\n        Working = Z_Score_Per_Slice(Working, out=Working)\n\n    # \u2500\u2500 Step 4: Global normalization (in-place via out=) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    _vprint(\n        f\"\\n[Step 4/4] Global normalization \"\n        f\"({Clip_Low_Percentile}-{Clip_High_Percentile} percentiles)...\"\n    )\n    t0 = time.time()\n\n    Working = Global_Percentile_Normalize(\n        Working,\n        Low_Percentile=Clip_Low_Percentile,\n        High_Percentile=Clip_High_Percentile,\n        Target_Min=0.0,\n        Target_Max=255.0,\n        out=Working,\n    )\n\n    _vprint(\n        f\"[Step 4/4] Done in {time.time() - t0:.1f}s. \"\n        f\"Range: [{Working.min():.1f}, {Working.max():.1f}]\"\n    )\n\n    # \u2500\u2500 Diagnostics \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    _vprint(\"\\n[Diagnostics] Assessing GMM readiness...\")\n\n    Readiness = Assess_Gmm_Readiness(Working, Exclude_Zero=True)\n\n    if Verbose:\n        print(\n            f\"[Diagnostics] Modes: {Readiness['n_modes']}, \"\n            f\"Skew: {Readiness['skewness']:.2f}, \"\n            f\"Kurt: {Readiness['kurtosis']:.2f}\"\n        )\n        print(f\"[Diagnostics] Readiness score: {Readiness['readiness_score']:.2f}\")\n        print(f\"[Diagnostics] {Readiness['recommendation']}\")\n\n    Diagnostics = {\n        \"input_shape\": Volume.shape,\n        \"input_dtype\": str(Volume.dtype),\n        \"background_sigma\": Background_Sigma,\n        \"noise_sigma\": Noise_Sigma,\n        \"stationarity_applied\": Need_Standardization,\n        \"clip_percentiles\": (Clip_Low_Percentile, Clip_High_Percentile),\n        \"readiness\": Readiness,\n        \"output_range\": (float(Working.min()), float(Working.max())),\n    }\n\n    return Working, Diagnostics\n</code></pre>"},{"location":"api/research_ct/#research_ct.preprocessing.global_normalization","title":"<code>research_ct.preprocessing.global_normalization</code>","text":"<p>Global intensity normalization using volume-wide percentiles.</p> <p>Linear rescaling that preserves the global intensity-to-material relationship. Avoids local transforms like CLAHE that destroy mixture model assumptions.</p> <p>Functions:</p> Name Description <code>Global_Percentile_Normalize</code> <p>Linear scale entire volume using global percentiles.</p> <code>Z_Score_Per_Slice</code> <p>Standardize each slice independently to correct inter-slice drift.</p> <code>Check_Slice_Stationarity</code> <p>Check if volume intensity distribution is stationary across slices.</p>"},{"location":"api/research_ct/#research_ct.preprocessing.global_normalization-functions","title":"Functions:","text":""},{"location":"api/research_ct/#research_ct.preprocessing.global_normalization.Global_Percentile_Normalize","title":"<code>Global_Percentile_Normalize(Volume, Low_Percentile=0.1, High_Percentile=99.9, Target_Min=0.0, Target_Max=255.0, out=None)</code>","text":"<p>Linear scale entire volume using global percentiles.</p> Every voxel is transformed by the same affine function <p>I' = (I - P_low) / (P_high - P_low) * (Target_Max - Target_Min) + Target_Min</p> <p>This preserves proportional separation between materials.</p> <p>Parameters:</p> Name Type Description Default <code>Volume</code> <code>ndarray</code> <p>Input 3D array.</p> required <code>Low_Percentile</code> <code>float</code> <p>Lower clipping percentile (default 0.1).</p> <code>0.1</code> <code>High_Percentile</code> <code>float</code> <p>Upper clipping percentile (default 99.9).</p> <code>99.9</code> <code>Target_Min</code> <code>float</code> <p>Desired output minimum.</p> <code>0.0</code> <code>Target_Max</code> <code>float</code> <p>Desired output maximum.</p> <code>255.0</code> <code>out</code> <code>Optional[ndarray]</code> <p>Optional output array (in-place when out is Volume).</p> <code>None</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>Normalized volume, float64, range [Target_Min, Target_Max].</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If Low_Percentile &gt;= High_Percentile.</p> Source code in <code>src\\research_ct\\preprocessing\\global_normalization.py</code> <pre><code>def Global_Percentile_Normalize(\n    Volume: np.ndarray,\n    Low_Percentile: float = 0.1,\n    High_Percentile: float = 99.9,\n    Target_Min: float = 0.0,\n    Target_Max: float = 255.0,\n    out: Optional[np.ndarray] = None,\n) -&gt; np.ndarray:\n    \"\"\"Linear scale entire volume using global percentiles.\n\n    Every voxel is transformed by the same affine function:\n        I' = (I - P_low) / (P_high - P_low) * (Target_Max - Target_Min) + Target_Min\n\n    This preserves proportional separation between materials.\n\n    Args:\n        Volume: Input 3D array.\n        Low_Percentile: Lower clipping percentile (default 0.1).\n        High_Percentile: Upper clipping percentile (default 99.9).\n        Target_Min: Desired output minimum.\n        Target_Max: Desired output maximum.\n        out: Optional output array (in-place when out is Volume).\n\n    Returns:\n        Normalized volume, float64, range [Target_Min, Target_Max].\n\n    Raises:\n        ValueError: If Low_Percentile &gt;= High_Percentile.\n    \"\"\"\n    if Low_Percentile &gt;= High_Percentile:\n        raise ValueError(\n            f\"Low_Percentile ({Low_Percentile}) must be &lt; \"\n            f\"High_Percentile ({High_Percentile})\"\n        )\n\n    Volume_Float = Volume.astype(np.float64, copy=False)\n\n    # Compute global percentiles\n    Low_Val, High_Val = np.percentile(\n        Volume_Float,\n        [Low_Percentile, High_Percentile],\n    )\n\n    if out is None:\n        out = np.empty_like(Volume_Float, dtype=np.float64)\n\n    if High_Val &lt;= Low_Val:\n        # Constant image\n        out.fill(Target_Min)\n        return out\n\n    # In-place clip into output, then rescale\n    np.clip(Volume_Float, Low_Val, High_Val, out=out)\n    out -= Low_Val\n    out /= (High_Val - Low_Val)\n    out *= (Target_Max - Target_Min)\n    out += Target_Min\n\n    return out\n</code></pre>"},{"location":"api/research_ct/#research_ct.preprocessing.global_normalization.Z_Score_Per_Slice","title":"<code>Z_Score_Per_Slice(Volume, out=None)</code>","text":"<p>Standardize each slice independently to correct inter-slice drift.</p> <p>Computes per-slice mean and std, then applies z-score:     I'_z = (I_z - mu_z) / sigma_z</p> <p>Use only if histograms vary significantly between slices. This removes global intensity differences but preserves within-slice contrast.</p> <p>Parameters:</p> Name Type Description Default <code>Volume</code> <code>ndarray</code> <p>3D array, shape (D, H, W).</p> required <code>out</code> <code>Optional[ndarray]</code> <p>Optional output array (in-place when out is Volume).</p> <code>None</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>Standardized volume, float64, approximately N(0,1) per slice.</p> Source code in <code>src\\research_ct\\preprocessing\\global_normalization.py</code> <pre><code>def Z_Score_Per_Slice(\n    Volume: np.ndarray,\n    out: Optional[np.ndarray] = None,\n) -&gt; np.ndarray:\n    \"\"\"Standardize each slice independently to correct inter-slice drift.\n\n    Computes per-slice mean and std, then applies z-score:\n        I'_z = (I_z - mu_z) / sigma_z\n\n    Use only if histograms vary significantly between slices.\n    This removes global intensity differences but preserves\n    within-slice contrast.\n\n    Args:\n        Volume: 3D array, shape (D, H, W).\n        out: Optional output array (in-place when out is Volume).\n\n    Returns:\n        Standardized volume, float64, approximately N(0,1) per slice.\n    \"\"\"\n    D = Volume.shape[0]\n\n    if out is None:\n        out = np.empty_like(Volume, dtype=np.float64)\n\n    for Z in range(D):\n        Slice = Volume[Z].astype(np.float64, copy=False)\n        Mean = Slice.mean()\n        Std = Slice.std()\n\n        if Std &gt; 0:\n            out[Z] = (Slice - Mean) / Std\n        else:\n            out[Z] = Slice - Mean\n\n    return out\n</code></pre>"},{"location":"api/research_ct/#research_ct.preprocessing.global_normalization.Check_Slice_Stationarity","title":"<code>Check_Slice_Stationarity(Volume, N_Bins=256)</code>","text":"<p>Check if volume intensity distribution is stationary across slices.</p> <p>Compares histogram intersection between first, middle, and last slices.</p> <p>Parameters:</p> Name Type Description Default <code>Volume</code> <code>ndarray</code> <p>3D array.</p> required <code>N_Bins</code> <code>int</code> <p>Number of histogram bins.</p> <code>256</code> <p>Returns:</p> Name Type Description <code>bool</code> <p>Tuple of (is_stationary, similarity_score).</p> <code>is_stationary</code> <code>float</code> <p>True if similarity &gt; 0.8.</p> <code>similarity_score</code> <code>Tuple[bool, float]</code> <p>Histogram intersection score [0, 1].</p> Source code in <code>src\\research_ct\\preprocessing\\global_normalization.py</code> <pre><code>def Check_Slice_Stationarity(\n    Volume: np.ndarray,\n    N_Bins: int = 256,\n) -&gt; Tuple[bool, float]:\n    \"\"\"Check if volume intensity distribution is stationary across slices.\n\n    Compares histogram intersection between first, middle, and last slices.\n\n    Args:\n        Volume: 3D array.\n        N_Bins: Number of histogram bins.\n\n    Returns:\n        Tuple of (is_stationary, similarity_score).\n        is_stationary: True if similarity &gt; 0.8.\n        similarity_score: Histogram intersection score [0, 1].\n    \"\"\"\n    D = Volume.shape[0]\n\n    # Compute histograms\n    Hist_0, Bins = np.histogram(Volume[0], bins=N_Bins, density=True)\n    Hist_mid, _ = np.histogram(Volume[D // 2], bins=Bins, density=True)\n    Hist_end, _ = np.histogram(Volume[-1], bins=Bins, density=True)\n\n    # Histogram intersection\n    Sim_0_mid = np.minimum(Hist_0, Hist_mid).sum()\n    Sim_0_end = np.minimum(Hist_0, Hist_end).sum()\n\n    Similarity = min(Sim_0_mid, Sim_0_end)\n\n    Is_Stationary = Similarity &gt; 0.8\n\n    return Is_Stationary, Similarity\n</code></pre>"},{"location":"api/research_ct/#research_ct.preprocessing.background_correction","title":"<code>research_ct.preprocessing.background_correction</code>","text":"<p>Global background correction for beam hardening removal.</p> <p>Uses large-scale Gaussian blur subtraction per slice to flatten the slowly varying intensity baseline while preserving material contrast.</p> <p>Functions:</p> Name Description <code>Correct_Background_Global</code> <p>Subtract large-scale background to correct beam hardening.</p> <code>Correct_Background_Volume</code> <p>Apply global background correction to entire 3D volume.</p> <code>Auto_Estimate_Background_Sigma</code> <p>Estimate appropriate background sigma from volume dimensions.</p>"},{"location":"api/research_ct/#research_ct.preprocessing.background_correction-functions","title":"Functions:","text":""},{"location":"api/research_ct/#research_ct.preprocessing.background_correction.Correct_Background_Global","title":"<code>Correct_Background_Global(Slice, Sigma=30.0)</code>","text":"<p>Subtract large-scale background to correct beam hardening.</p> <p>A Gaussian filter with large sigma estimates the low-frequency background gradient (beam hardening artifact). Subtracting this flattens the baseline without affecting high-frequency material boundaries.</p> <p>Parameters:</p> Name Type Description Default <code>Slice</code> <code>ndarray</code> <p>2D grayscale image, shape (H, W).</p> required <code>Sigma</code> <code>float</code> <p>Standard deviation of Gaussian kernel in voxels. Larger sigma = more background removed. Default 30.0 works for typical book micro-CT.</p> <code>30.0</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>Background-corrected slice, float64, shifted to positive range.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If Sigma &lt;= 0.</p> Source code in <code>src\\research_ct\\preprocessing\\background_correction.py</code> <pre><code>def Correct_Background_Global(\n    Slice: np.ndarray,\n    Sigma: float = 30.0,\n) -&gt; np.ndarray:\n    \"\"\"Subtract large-scale background to correct beam hardening.\n\n    A Gaussian filter with large sigma estimates the low-frequency\n    background gradient (beam hardening artifact). Subtracting this\n    flattens the baseline without affecting high-frequency material\n    boundaries.\n\n    Args:\n        Slice: 2D grayscale image, shape (H, W).\n        Sigma: Standard deviation of Gaussian kernel in voxels.\n            Larger sigma = more background removed.\n            Default 30.0 works for typical book micro-CT.\n\n    Returns:\n        Background-corrected slice, float64, shifted to positive range.\n\n    Raises:\n        ValueError: If Sigma &lt;= 0.\n    \"\"\"\n    if Sigma &lt;= 0:\n        raise ValueError(f\"Sigma must be positive, got {Sigma}\")\n\n    Slice_Float = Slice.astype(np.float64, copy=False)\n\n    # Estimate background with large Gaussian\n    Background = gaussian_filter(Slice_Float, sigma=Sigma)\n\n    # Subtract and shift to positive\n    Corrected = Slice_Float - Background\n    return Corrected - Corrected.min()\n</code></pre>"},{"location":"api/research_ct/#research_ct.preprocessing.background_correction.Correct_Background_Volume","title":"<code>Correct_Background_Volume(Volume, Sigma=30.0, out=None)</code>","text":"<p>Apply global background correction to entire 3D volume.</p> <p>Processes each slice independently. The background model is 2D per-slice; no inter-slice smoothing is applied.</p> <p>Parameters:</p> Name Type Description Default <code>Volume</code> <code>ndarray</code> <p>3D array, shape (D, H, W).</p> required <code>Sigma</code> <code>float</code> <p>Gaussian kernel sigma in voxels.</p> <code>30.0</code> <code>out</code> <code>Optional[ndarray]</code> <p>Optional output array to write into (in-place when out is Volume). Must have same shape as Volume.</p> <code>None</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>Corrected volume, float64.</p> Source code in <code>src\\research_ct\\preprocessing\\background_correction.py</code> <pre><code>def Correct_Background_Volume(\n    Volume: np.ndarray,\n    Sigma: float = 30.0,\n    out: Optional[np.ndarray] = None,\n) -&gt; np.ndarray:\n    \"\"\"Apply global background correction to entire 3D volume.\n\n    Processes each slice independently. The background model is\n    2D per-slice; no inter-slice smoothing is applied.\n\n    Args:\n        Volume: 3D array, shape (D, H, W).\n        Sigma: Gaussian kernel sigma in voxels.\n        out: Optional output array to write into (in-place when\n            out is Volume). Must have same shape as Volume.\n\n    Returns:\n        Corrected volume, float64.\n    \"\"\"\n    D = Volume.shape[0]\n\n    if out is None:\n        out = np.zeros_like(Volume, dtype=np.float64)\n    elif out.shape != Volume.shape:\n        raise ValueError(\n            f\"out shape {out.shape} does not match Volume shape {Volume.shape}\"\n        )\n\n    for Z in range(D):\n        out[Z] = Correct_Background_Global(Volume[Z], Sigma)\n\n    return out\n</code></pre>"},{"location":"api/research_ct/#research_ct.preprocessing.background_correction.Auto_Estimate_Background_Sigma","title":"<code>Auto_Estimate_Background_Sigma(Volume, Fraction=0.05)</code>","text":"<p>Estimate appropriate background sigma from volume dimensions.</p> <p>Uses a fraction of the smaller lateral dimension as a heuristic. For a 2000x2000 slice, Fraction=0.05 gives sigma=100.</p> <p>Parameters:</p> Name Type Description Default <code>Volume</code> <code>ndarray</code> <p>3D array.</p> required <code>Fraction</code> <code>float</code> <p>Fraction of min(H,W) to use as sigma.</p> <code>0.05</code> <p>Returns:</p> Type Description <code>float</code> <p>Estimated sigma value.</p> Source code in <code>src\\research_ct\\preprocessing\\background_correction.py</code> <pre><code>def Auto_Estimate_Background_Sigma(\n    Volume: np.ndarray,\n    Fraction: float = 0.05,\n) -&gt; float:\n    \"\"\"Estimate appropriate background sigma from volume dimensions.\n\n    Uses a fraction of the smaller lateral dimension as a heuristic.\n    For a 2000x2000 slice, Fraction=0.05 gives sigma=100.\n\n    Args:\n        Volume: 3D array.\n        Fraction: Fraction of min(H,W) to use as sigma.\n\n    Returns:\n        Estimated sigma value.\n    \"\"\"\n    _, H, W = Volume.shape\n    Sigma = max(10.0, Fraction * min(H, W))\n\n    return Sigma\n</code></pre>"},{"location":"api/research_ct/#research_ct.preprocessing.noise_reduction","title":"<code>research_ct.preprocessing.noise_reduction</code>","text":"<p>Mild isotropic Gaussian smoothing for noise reduction.</p> <p>Designed specifically to tighten material intensity distributions into approximately Gaussian shapes for GMM fitting. Uses linear, shift-invariant filtering that preserves global mixture structure.</p> <p>Functions:</p> Name Description <code>Reduce_Noise_Gaussian</code> <p>Apply mild Gaussian smoothing to reduce Poisson noise.</p> <code>Reduce_Noise_Volume</code> <p>Apply Gaussian smoothing to entire 3D volume.</p>"},{"location":"api/research_ct/#research_ct.preprocessing.noise_reduction-functions","title":"Functions:","text":""},{"location":"api/research_ct/#research_ct.preprocessing.noise_reduction.Reduce_Noise_Gaussian","title":"<code>Reduce_Noise_Gaussian(Image, Sigma=0.8)</code>","text":"<p>Apply mild Gaussian smoothing to reduce Poisson noise.</p> <p>The sigma is intentionally small (sub-voxel) to avoid blurring material boundaries while sufficient to apply the Central Limit Theorem effect that makes noise more Gaussian.</p> <p>Parameters:</p> Name Type Description Default <code>Image</code> <code>ndarray</code> <p>Input array, any shape.</p> required <code>Sigma</code> <code>float</code> <p>Standard deviation of Gaussian kernel in voxels. Default 0.8 is optimal for most micro-CT data. Range: 0.5\u20131.5 recommended.</p> <code>0.8</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>Smoothed array, float64.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If Sigma &lt;= 0.</p> Source code in <code>src\\research_ct\\preprocessing\\noise_reduction.py</code> <pre><code>def Reduce_Noise_Gaussian(\n    Image: np.ndarray,\n    Sigma: float = 0.8,\n) -&gt; np.ndarray:\n    \"\"\"Apply mild Gaussian smoothing to reduce Poisson noise.\n\n    The sigma is intentionally small (sub-voxel) to avoid blurring\n    material boundaries while sufficient to apply the Central Limit\n    Theorem effect that makes noise more Gaussian.\n\n    Args:\n        Image: Input array, any shape.\n        Sigma: Standard deviation of Gaussian kernel in voxels.\n            Default 0.8 is optimal for most micro-CT data.\n            Range: 0.5\u20131.5 recommended.\n\n    Returns:\n        Smoothed array, float64.\n\n    Raises:\n        ValueError: If Sigma &lt;= 0.\n    \"\"\"\n    if Sigma &lt;= 0:\n        raise ValueError(f\"Sigma must be positive, got {Sigma}\")\n\n    Image_Float = Image.astype(np.float64, copy=False)\n    return gaussian_filter(Image_Float, sigma=Sigma)\n</code></pre>"},{"location":"api/research_ct/#research_ct.preprocessing.noise_reduction.Reduce_Noise_Volume","title":"<code>Reduce_Noise_Volume(Volume, Sigma=0.8, out=None)</code>","text":"<p>Apply Gaussian smoothing to entire 3D volume.</p> <p>Uses 3D Gaussian kernel for isotropic smoothing across all dimensions. For anisotropic voxels, consider per-slice 2D smoothing with different sigmas.</p> <p>Parameters:</p> Name Type Description Default <code>Volume</code> <code>ndarray</code> <p>3D array, shape (D, H, W).</p> required <code>Sigma</code> <code>float</code> <p>Gaussian kernel sigma in voxels.</p> <code>0.8</code> <code>out</code> <code>Optional[ndarray]</code> <p>Optional output array (in-place when out is Volume).</p> <code>None</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>Smoothed volume, float64.</p> Source code in <code>src\\research_ct\\preprocessing\\noise_reduction.py</code> <pre><code>def Reduce_Noise_Volume(\n    Volume: np.ndarray,\n    Sigma: float = 0.8,\n    out: Optional[np.ndarray] = None,\n) -&gt; np.ndarray:\n    \"\"\"Apply Gaussian smoothing to entire 3D volume.\n\n    Uses 3D Gaussian kernel for isotropic smoothing across all\n    dimensions. For anisotropic voxels, consider per-slice 2D\n    smoothing with different sigmas.\n\n    Args:\n        Volume: 3D array, shape (D, H, W).\n        Sigma: Gaussian kernel sigma in voxels.\n        out: Optional output array (in-place when out is Volume).\n\n    Returns:\n        Smoothed volume, float64.\n    \"\"\"\n    Volume_Float = Volume.astype(np.float64, copy=False)\n    return gaussian_filter(Volume_Float, sigma=Sigma, output=out)\n</code></pre>"},{"location":"api/research_ct/#segmentation","title":"Segmentation","text":""},{"location":"api/research_ct/#research_ct.segmentation.gmm_fitter","title":"<code>research_ct.segmentation.gmm_fitter</code>","text":"<p>Gaussian Mixture Model fitting with automatic K selection.</p> <p>Classes:</p> Name Description <code>Gmm_Fitter</code> <p>Fit GMM to intensity data with BIC-based model selection.</p>"},{"location":"api/research_ct/#research_ct.segmentation.gmm_fitter-classes","title":"Classes","text":""},{"location":"api/research_ct/#research_ct.segmentation.gmm_fitter.Gmm_Fitter","title":"<code>Gmm_Fitter(Min_Components=1, Max_Components=10, Covariance_Type='full')</code>","text":"<p>Fit GMM to intensity data with BIC-based model selection.</p> <p>Attributes:</p> Name Type Description <code>Num_Components</code> <code>Optional[int]</code> <p>Selected K.</p> <code>Model</code> <code>Optional[GaussianMixture]</code> <p>Fitted sklearn GaussianMixture.</p> <code>Bic_Scores</code> <code>List[float]</code> <p>BIC for each K tested.</p> <p>Methods:</p> Name Description <code>Fit</code> <p>Fit GMM with automatic K selection via BIC.</p> <code>Predict_Labels</code> <p>Predict hard labels (argmax posterior).</p> <code>Predict_Probabilities</code> <p>Predict posterior probabilities.</p> <code>Get_Material_Statistics</code> <p>Extract fitted parameters per component.</p> Source code in <code>src\\research_ct\\segmentation\\gmm_fitter.py</code> <pre><code>def __init__(\n    self,\n    Min_Components: int = 1,\n    Max_Components: int = 10,\n    Covariance_Type: str = \"full\",\n):\n    self.Min_Components = Min_Components\n    self.Max_Components = Max_Components\n    self.Covariance_Type = Covariance_Type\n\n    self.Num_Components: Optional[int] = None\n    self.Model: Optional[GaussianMixture] = None\n    self.Bic_Scores: List[float] = []\n</code></pre>"},{"location":"api/research_ct/#research_ct.segmentation.gmm_fitter.Gmm_Fitter-functions","title":"Methods:","text":""},{"location":"api/research_ct/#research_ct.segmentation.gmm_fitter.Gmm_Fitter.Fit","title":"<code>Fit(Data, Verbose=True)</code>","text":"<p>Fit GMM with automatic K selection via BIC.</p> <p>Parameters:</p> Name Type Description Default <code>Data</code> <code>ndarray</code> <p>1D array of intensities, shape (N,) or (N, 1).</p> required <code>Verbose</code> <code>bool</code> <p>Print progress.</p> <code>True</code> <p>Returns:</p> Type Description <code>Gmm_Fitter</code> <p>Self for method chaining.</p> Source code in <code>src\\research_ct\\segmentation\\gmm_fitter.py</code> <pre><code>def Fit(self, Data: np.ndarray, Verbose: bool = True) -&gt; \"Gmm_Fitter\":\n    \"\"\"Fit GMM with automatic K selection via BIC.\n\n    Args:\n        Data: 1D array of intensities, shape (N,) or (N, 1).\n        Verbose: Print progress.\n\n    Returns:\n        Self for method chaining.\n    \"\"\"\n    if Data.ndim == 1:\n        Data = Data.reshape(-1, 1)\n\n    self.Bic_Scores = []\n    Best_Bic = np.inf\n    Best_Model = None\n    Best_K = self.Min_Components\n\n    for K in range(self.Min_Components, self.Max_Components + 1):\n        Model = GaussianMixture(\n            n_components=K,\n            covariance_type=self.Covariance_Type,\n            random_state=42,\n            max_iter=1000,\n        )\n        Model.fit(Data)\n\n        Bic = Model.bic(Data)\n        self.Bic_Scores.append(Bic)\n\n        if Verbose:\n            print(f\"  K={K}: BIC={Bic:.2e}, converged={Model.converged_}\")\n\n        if Bic &lt; Best_Bic:\n            Best_Bic = Bic\n            Best_Model = Model\n            Best_K = K\n\n    self.Num_Components = Best_K\n    self.Model = Best_Model\n\n    if Verbose:\n        print(f\"[Gmm_Fitter] Selected K={Best_K}\")\n\n    return self\n</code></pre>"},{"location":"api/research_ct/#research_ct.segmentation.gmm_fitter.Gmm_Fitter.Predict_Labels","title":"<code>Predict_Labels(Data)</code>","text":"<p>Predict hard labels (argmax posterior).</p> <p>Parameters:</p> Name Type Description Default <code>Data</code> <code>ndarray</code> <p>Intensities, shape (N,) or (N, 1).</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>Integer labels, shape (N,).</p> Source code in <code>src\\research_ct\\segmentation\\gmm_fitter.py</code> <pre><code>def Predict_Labels(self, Data: np.ndarray) -&gt; np.ndarray:\n    \"\"\"Predict hard labels (argmax posterior).\n\n    Args:\n        Data: Intensities, shape (N,) or (N, 1).\n\n    Returns:\n        Integer labels, shape (N,).\n    \"\"\"\n    if self.Model is None:\n        raise RuntimeError(\"Model not fitted. Call Fit() first.\")\n\n    if Data.ndim == 1:\n        Data = Data.reshape(-1, 1)\n\n    return self.Model.predict(Data)\n</code></pre>"},{"location":"api/research_ct/#research_ct.segmentation.gmm_fitter.Gmm_Fitter.Predict_Probabilities","title":"<code>Predict_Probabilities(Data)</code>","text":"<p>Predict posterior probabilities.</p> <p>Parameters:</p> Name Type Description Default <code>Data</code> <code>ndarray</code> <p>Intensities, shape (N,) or (N, 1).</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>Probabilities, shape (N, K).</p> Source code in <code>src\\research_ct\\segmentation\\gmm_fitter.py</code> <pre><code>def Predict_Probabilities(self, Data: np.ndarray) -&gt; np.ndarray:\n    \"\"\"Predict posterior probabilities.\n\n    Args:\n        Data: Intensities, shape (N,) or (N, 1).\n\n    Returns:\n        Probabilities, shape (N, K).\n    \"\"\"\n    if self.Model is None:\n        raise RuntimeError(\"Model not fitted. Call Fit() first.\")\n\n    if Data.ndim == 1:\n        Data = Data.reshape(-1, 1)\n\n    return self.Model.predict_proba(Data)\n</code></pre>"},{"location":"api/research_ct/#research_ct.segmentation.gmm_fitter.Gmm_Fitter.Get_Material_Statistics","title":"<code>Get_Material_Statistics()</code>","text":"<p>Extract fitted parameters per component.</p> <p>Returns:</p> Type Description <code>Dict[str, ndarray]</code> <p>Dict with Means, Variances, Weights.</p> Source code in <code>src\\research_ct\\segmentation\\gmm_fitter.py</code> <pre><code>def Get_Material_Statistics(self) -&gt; Dict[str, np.ndarray]:\n    \"\"\"Extract fitted parameters per component.\n\n    Returns:\n        Dict with Means, Variances, Weights.\n    \"\"\"\n    if self.Model is None:\n        raise RuntimeError(\"Model not fitted. Call Fit() first.\")\n\n    Variances = np.array([\n        np.diag(Cov) if Cov.ndim == 2 else Cov\n        for Cov in self.Model.covariances_\n    ])\n\n    return {\n        \"Means\": self.Model.means_.flatten(),\n        \"Variances\": Variances,\n        \"Weights\": self.Model.weights_,\n    }\n</code></pre>"},{"location":"api/research_ct/#research_ct.segmentation.sparse_bayesian_gmm","title":"<code>research_ct.segmentation.sparse_bayesian_gmm</code>","text":"<p>Sparse Finite Bayesian Gaussian Mixture Model with Variational Inference.</p> <p>Classes:</p> Name Description <code>Sparse_Bayesian_Gmm</code> <p>Overcomplete Bayesian GMM with sparse Dirichlet weight prior.</p>"},{"location":"api/research_ct/#research_ct.segmentation.sparse_bayesian_gmm-classes","title":"Classes","text":""},{"location":"api/research_ct/#research_ct.segmentation.sparse_bayesian_gmm.Sparse_Bayesian_Gmm","title":"<code>Sparse_Bayesian_Gmm(Max_Components=10, Weight_Concentration_Prior=None, Weight_Threshold=0.001, Min_Samples=1000, Covariance_Type='full', Random_State=42)</code>","text":"<p>Overcomplete Bayesian GMM with sparse Dirichlet weight prior.</p> <p>Automatically collapses unneeded components to zero weight during inference. Active components are filtered based on weight thresholds and effective sample sizes.</p> <p>Methods:</p> Name Description <code>Fit</code> <p>Fit overcomplete model and prune inactive components.</p> <code>Predict_Probabilities</code> <p>Predict soft responsibilities normalized over active components only.</p> <code>Predict_Labels</code> <p>Predict hard integer labels based on active components.</p> <code>Get_Material_Statistics</code> <p>Return parameters for active components.</p> Source code in <code>src\\research_ct\\segmentation\\sparse_bayesian_gmm.py</code> <pre><code>def __init__(\n    self,\n    Max_Components: int = 10,\n    Weight_Concentration_Prior: Optional[float] = None,\n    Weight_Threshold: float = 1e-3,\n    Min_Samples: int = 1000,\n    Covariance_Type: str = \"full\",\n    Random_State: int = 42,\n):\n    self.Max_Components = Max_Components\n    self.Weight_Concentration_Prior = Weight_Concentration_Prior\n    self.Weight_Threshold = Weight_Threshold\n    self.Min_Samples = Min_Samples\n    self.Covariance_Type = Covariance_Type\n    self.Random_State = Random_State\n\n    self.Model: Optional[BayesianGaussianMixture] = None\n    self.Active_Indices: np.ndarray = np.array([])\n    self.Num_Active_Components: int = 0\n    self.Means: np.ndarray = np.array([])\n    self.Variances: np.ndarray = np.array([])\n    self.Weights: np.ndarray = np.array([])\n</code></pre>"},{"location":"api/research_ct/#research_ct.segmentation.sparse_bayesian_gmm.Sparse_Bayesian_Gmm-functions","title":"Methods:","text":""},{"location":"api/research_ct/#research_ct.segmentation.sparse_bayesian_gmm.Sparse_Bayesian_Gmm.Fit","title":"<code>Fit(Data, Verbose=True)</code>","text":"<p>Fit overcomplete model and prune inactive components.</p> <p>Parameters:</p> Name Type Description Default <code>Data</code> <code>ndarray</code> <p>1D intensity dataset view, shape (N, 1) or (N,).</p> required <code>Verbose</code> <code>bool</code> <p>Print active component counts.</p> <code>True</code> <p>Returns:</p> Type Description <code>Sparse_Bayesian_Gmm</code> <p>Self for method chaining.</p> Source code in <code>src\\research_ct\\segmentation\\sparse_bayesian_gmm.py</code> <pre><code>def Fit(self, Data: np.ndarray, Verbose: bool = True) -&gt; \"Sparse_Bayesian_Gmm\":\n    \"\"\"Fit overcomplete model and prune inactive components.\n\n    Args:\n        Data: 1D intensity dataset view, shape (N, 1) or (N,).\n        Verbose: Print active component counts.\n\n    Returns:\n        Self for method chaining.\n    \"\"\"\n    if Data.ndim == 1:\n        Data = Data.reshape(-1, 1)\n\n    self.Model = BayesianGaussianMixture(\n        n_components=self.Max_Components,\n        covariance_type=self.Covariance_Type,\n        weight_concentration_prior_type=\"dirichlet_distribution\",\n        weight_concentration_prior=self.Weight_Concentration_Prior,\n        random_state=self.Random_State,\n        max_iter=1000,\n    )\n    self.Model.fit(Data)\n\n    # Calculate full component responsibilities\n    Raw_Weights = self.Model.weights_\n    Responsibilities = self.Model.predict_proba(Data)\n    Effective_Samples = Responsibilities.sum(axis=0)\n\n    # Thresholding for active status\n    Active_Mask = (Raw_Weights &gt;= self.Weight_Threshold) &amp; (\n        Effective_Samples &gt;= self.Min_Samples\n    )\n    self.Active_Indices = np.where(Active_Mask)[0]\n    self.Num_Active_Components = len(self.Active_Indices)\n\n    # Fallback mechanism if thresholding is too aggressive\n    if self.Num_Active_Components == 0:\n        Fallback_Index = np.argmax(Raw_Weights)\n        self.Active_Indices = np.array([Fallback_Index])\n        self.Num_Active_Components = 1\n\n    self.Means = self.Model.means_[self.Active_Indices].flatten()\n\n    Raw_Covariances = self.Model.covariances_[self.Active_Indices]\n    if self.Covariance_Type == \"full\":\n        self.Variances = np.array([np.diag(Cov) for Cov in Raw_Covariances])\n    else:\n        self.Variances = Raw_Covariances.flatten()\n\n    self.Weights = Raw_Weights[self.Active_Indices]\n\n    # Renormalize extracted weights\n    self.Weights /= self.Weights.sum()\n\n    if Verbose:\n        print(\n            f\"[Sparse_Bayesian_Gmm] Retained {self.Num_Active_Components}/{self.Max_Components} active components.\"\n        )\n\n    return self\n</code></pre>"},{"location":"api/research_ct/#research_ct.segmentation.sparse_bayesian_gmm.Sparse_Bayesian_Gmm.Predict_Probabilities","title":"<code>Predict_Probabilities(Data)</code>","text":"<p>Predict soft responsibilities normalized over active components only.</p> <p>Parameters:</p> Name Type Description Default <code>Data</code> <code>ndarray</code> <p>1D intensity dataset view.</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>Normalized active probabilities, shape (N, K_active).</p> Source code in <code>src\\research_ct\\segmentation\\sparse_bayesian_gmm.py</code> <pre><code>def Predict_Probabilities(self, Data: np.ndarray) -&gt; np.ndarray:\n    \"\"\"Predict soft responsibilities normalized over active components only.\n\n    Args:\n        Data: 1D intensity dataset view.\n\n    Returns:\n        Normalized active probabilities, shape (N, K_active).\n    \"\"\"\n    if self.Model is None:\n        raise RuntimeError(\"Model is not fitted. Call Fit() first.\")\n\n    if Data.ndim == 1:\n        Data = Data.reshape(-1, 1)\n\n    # Obtain full responsibilities and slice dynamically\n    Raw_Responsibilities = self.Model.predict_proba(Data)\n    Active_Responsibilities = Raw_Responsibilities[:, self.Active_Indices]\n\n    # In-place row normalization\n    Row_Sums = Active_Responsibilities.sum(axis=1, keepdims=True)\n    Row_Sums[Row_Sums == 0.0] = 1.0\n    Active_Responsibilities /= Row_Sums\n\n    return Active_Responsibilities\n</code></pre>"},{"location":"api/research_ct/#research_ct.segmentation.sparse_bayesian_gmm.Sparse_Bayesian_Gmm.Predict_Labels","title":"<code>Predict_Labels(Data)</code>","text":"<p>Predict hard integer labels based on active components.</p> <p>Returns:</p> Type Description <code>ndarray</code> <p>Array of integers, shape (N,).</p> Source code in <code>src\\research_ct\\segmentation\\sparse_bayesian_gmm.py</code> <pre><code>def Predict_Labels(self, Data: np.ndarray) -&gt; np.ndarray:\n    \"\"\"Predict hard integer labels based on active components.\n\n    Returns:\n        Array of integers, shape (N,).\n    \"\"\"\n    Active_Probabilities = self.Predict_Probabilities(Data)\n    return np.argmax(Active_Probabilities, axis=1)\n</code></pre>"},{"location":"api/research_ct/#research_ct.segmentation.sparse_bayesian_gmm.Sparse_Bayesian_Gmm.Get_Material_Statistics","title":"<code>Get_Material_Statistics()</code>","text":"<p>Return parameters for active components.</p> Source code in <code>src\\research_ct\\segmentation\\sparse_bayesian_gmm.py</code> <pre><code>def Get_Material_Statistics(self) -&gt; Dict[str, np.ndarray]:\n    \"\"\"Return parameters for active components.\"\"\"\n    return {\n        \"Means\": self.Means,\n        \"Variances\": self.Variances,\n        \"Weights\": self.Weights,\n        \"Active_Indices\": self.Active_Indices,\n    }\n</code></pre>"},{"location":"api/research_ct/#research_ct.segmentation.hierarchy","title":"<code>research_ct.segmentation.hierarchy</code>","text":"<p>Hierarchical component splitting for GMM refinement with soft path propagation.</p> <p>Classes:</p> Name Description <code>Hierarchical_Gmm</code> <p>Recursively split GMM components using soft path probability propagation.</p>"},{"location":"api/research_ct/#research_ct.segmentation.hierarchy-classes","title":"Classes","text":""},{"location":"api/research_ct/#research_ct.segmentation.hierarchy.Hierarchical_Gmm","title":"<code>Hierarchical_Gmm(Min_Samples=1000, Max_Depth=5, Significance_Alpha=0.05)</code>","text":"<p>Recursively split GMM components using soft path probability propagation.</p> <p>Evaluates recursive 2-component splits across all initial root components subject to 3 statistical criteria: effective sample size, BIC parsimony, and Likelihood Ratio Test (LRT) significance.</p> <p>Methods:</p> Name Description <code>Fit</code> <p>Build hierarchical GMM trees by recursive splitting across all root components.</p> <code>Predict_Leaf_Probabilities</code> <p>Compute accumulated soft path probabilities for all leaf nodes across data.</p> <code>Get_Leaf_Components</code> <p>Extract metadata for all active leaf components.</p> Source code in <code>src\\research_ct\\segmentation\\hierarchy.py</code> <pre><code>def __init__(\n    self,\n    Min_Samples: int = 1000,\n    Max_Depth: int = 5,\n    Significance_Alpha: float = 0.05,\n):\n    self.Min_Samples = Min_Samples\n    self.Max_Depth = Max_Depth\n    self.Significance_Alpha = Significance_Alpha\n\n    self.Root_Gmm: Optional[GaussianMixture] = None\n    self.Trees: List[Dict] = []\n    self.Leaf_Nodes: List[Dict] = []\n</code></pre>"},{"location":"api/research_ct/#research_ct.segmentation.hierarchy.Hierarchical_Gmm-functions","title":"Methods:","text":""},{"location":"api/research_ct/#research_ct.segmentation.hierarchy.Hierarchical_Gmm.Fit","title":"<code>Fit(Data, Initial_K=2)</code>","text":"<p>Build hierarchical GMM trees by recursive splitting across all root components.</p> <p>Parameters:</p> Name Type Description Default <code>Data</code> <code>ndarray</code> <p>1D intensities view, shape (N, 1) or (N,).</p> required <code>Initial_K</code> <code>int</code> <p>Number of root components from the initial flat GMM.</p> <code>2</code> <p>Returns:</p> Type Description <code>Hierarchical_Gmm</code> <p>Self for method chaining.</p> Source code in <code>src\\research_ct\\segmentation\\hierarchy.py</code> <pre><code>def Fit(self, Data: np.ndarray, Initial_K: int = 2) -&gt; \"Hierarchical_Gmm\":\n    \"\"\"Build hierarchical GMM trees by recursive splitting across all root components.\n\n    Args:\n        Data: 1D intensities view, shape (N, 1) or (N,).\n        Initial_K: Number of root components from the initial flat GMM.\n\n    Returns:\n        Self for method chaining.\n    \"\"\"\n    if Data.ndim == 1:\n        Data = Data.reshape(-1, 1)\n\n    # Fit root model across Initial_K components\n    self.Root_Gmm = GaussianMixture(\n        n_components=Initial_K,\n        covariance_type=\"full\",\n        random_state=42,\n    )\n    self.Root_Gmm.fit(Data)\n\n    # Initial root responsibilities: shape (N, Initial_K)\n    Root_Responsibilities = self.Root_Gmm.predict_proba(Data)\n\n    self.Trees = []\n    # Recursively split EVERY root component k in {0, ..., Initial_K - 1}\n    for Component_Index in range(Initial_K):\n        Root_Path = Root_Responsibilities[:, Component_Index]\n        Tree_Root = self._Split_Component(\n            Data=Data,\n            Path_Probabilities=Root_Path,\n            Parent_Mean=self.Root_Gmm.means_[Component_Index].item(),\n            Parent_Variance=np.diag(self.Root_Gmm.covariances_[Component_Index]).item(),\n            Depth=0,\n        )\n        self.Trees.append(Tree_Root)\n\n    # Collect flat list of all leaves across all root subtrees\n    self.Leaf_Nodes = []\n    for Tree in self.Trees:\n        self._Collect_Leaves(Tree)\n\n    return self\n</code></pre>"},{"location":"api/research_ct/#research_ct.segmentation.hierarchy.Hierarchical_Gmm.Predict_Leaf_Probabilities","title":"<code>Predict_Leaf_Probabilities(Data)</code>","text":"<p>Compute accumulated soft path probabilities for all leaf nodes across data.</p> <p>Parameters:</p> Name Type Description Default <code>Data</code> <code>ndarray</code> <p>1D intensities view, shape (N, 1) or (N,).</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>Leaf responsibilities tensor of shape (N, L), normalized so sum_l P(leaf_l | x_i) = 1.0.</p> Source code in <code>src\\research_ct\\segmentation\\hierarchy.py</code> <pre><code>def Predict_Leaf_Probabilities(self, Data: np.ndarray) -&gt; np.ndarray:\n    \"\"\"Compute accumulated soft path probabilities for all leaf nodes across data.\n\n    Args:\n        Data: 1D intensities view, shape (N, 1) or (N,).\n\n    Returns:\n        Leaf responsibilities tensor of shape (N, L), normalized so sum_l P(leaf_l | x_i) = 1.0.\n    \"\"\"\n    if Data.ndim == 1:\n        Data = Data.reshape(-1, 1)\n\n    N_Samples = Data.shape[0]\n    N_Leaves = len(self.Leaf_Nodes)\n\n    if self.Root_Gmm is None or N_Leaves == 0:\n        raise RuntimeError(\"Model not fitted. Call Fit() first.\")\n\n    Root_Responsibilities = self.Root_Gmm.predict_proba(Data)\n    Leaf_Probabilities = np.empty((N_Samples, N_Leaves), dtype=np.float64)\n\n    Leaf_Counter = 0\n    for Component_Index, Tree_Root in enumerate(self.Trees):\n        Root_Path = Root_Responsibilities[:, Component_Index]\n        Leaf_Counter = self._Evaluate_Node_Probabilities(\n            Data=Data,\n            Node=Tree_Root,\n            Current_Path=Root_Path,\n            Output_Array=Leaf_Probabilities,\n            Leaf_Offset=Leaf_Counter,\n        )\n\n    # In-place normalization across leaves: sum_l P(leaf_l | x_i) = 1.0\n    Row_Sums = Leaf_Probabilities.sum(axis=1, keepdims=True)\n    Row_Sums[Row_Sums == 0.0] = 1.0\n    Leaf_Probabilities /= Row_Sums\n\n    return Leaf_Probabilities\n</code></pre>"},{"location":"api/research_ct/#research_ct.segmentation.hierarchy.Hierarchical_Gmm.Get_Leaf_Components","title":"<code>Get_Leaf_Components()</code>","text":"<p>Extract metadata for all active leaf components.</p> Source code in <code>src\\research_ct\\segmentation\\hierarchy.py</code> <pre><code>def Get_Leaf_Components(self) -&gt; List[Dict]:\n    \"\"\"Extract metadata for all active leaf components.\"\"\"\n    return self.Leaf_Nodes\n</code></pre>"},{"location":"api/research_ct/#research_ct.segmentation.hmrf","title":"<code>research_ct.segmentation.hmrf</code>","text":"<p>Hidden Markov Random Field (HMRF) with Potts prior for 3D spatial regularization.</p> <p>Classes:</p> Name Description <code>Hmrf_Segmenter</code> <p>HMRF spatial regularization using Iterated Conditional Modes (ICM).</p>"},{"location":"api/research_ct/#research_ct.segmentation.hmrf-classes","title":"Classes","text":""},{"location":"api/research_ct/#research_ct.segmentation.hmrf.Hmrf_Segmenter","title":"<code>Hmrf_Segmenter(Beta=0.5, Max_Iterations=50, Connectivity=6)</code>","text":"<p>HMRF spatial regularization using Iterated Conditional Modes (ICM).</p> Energy Formulation <p>E(z) = sum_i -log P(x_i | z_i) + Beta * sum_{(i,j) in E} delta(z_i != z_j)</p> <p>Methods:</p> Name Description <code>Fit</code> <p>Run ICM optimization for spatial label field regularization.</p> Source code in <code>src\\research_ct\\segmentation\\hmrf.py</code> <pre><code>def __init__(\n    self,\n    Beta: float = 0.5,\n    Max_Iterations: int = 50,\n    Connectivity: int = 6,\n):\n    self.Beta = Beta\n    self.Max_Iterations = Max_Iterations\n    self.Connectivity = Connectivity\n\n    self.Labels: Optional[np.ndarray] = None\n</code></pre>"},{"location":"api/research_ct/#research_ct.segmentation.hmrf.Hmrf_Segmenter-functions","title":"Methods:","text":""},{"location":"api/research_ct/#research_ct.segmentation.hmrf.Hmrf_Segmenter.Fit","title":"<code>Fit(Log_Probabilities)</code>","text":"<p>Run ICM optimization for spatial label field regularization.</p> <p>Parameters:</p> Name Type Description Default <code>Log_Probabilities</code> <code>ndarray</code> <p>Pre-computed log unary scores, shape (D, H, W, K).</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>Regularized integer labels, shape (D, H, W).</p> Source code in <code>src\\research_ct\\segmentation\\hmrf.py</code> <pre><code>def Fit(\n    self,\n    Log_Probabilities: np.ndarray,\n) -&gt; np.ndarray:\n    \"\"\"Run ICM optimization for spatial label field regularization.\n\n    Args:\n        Log_Probabilities: Pre-computed log unary scores, shape (D, H, W, K).\n\n    Returns:\n        Regularized integer labels, shape (D, H, W).\n    \"\"\"\n    D, H, W, K = Log_Probabilities.shape\n    Neighbors = self._Get_Neighbors()\n\n    # Initialize labels using Maximum A Posteriori (MAP)\n    self.Labels = np.argmax(Log_Probabilities, axis=3).astype(np.int32)\n\n    # Allocate memory buffers once to avoid memory churn across iterations\n    Energy = np.empty((D, H, W), dtype=np.float64)\n    Best_Energy = np.empty((D, H, W), dtype=np.float64)\n    Best_Label = np.empty((D, H, W), dtype=np.int32)\n\n    for Iteration in range(self.Max_Iterations):\n        Best_Energy.fill(np.inf)\n        np.copyto(Best_Label, self.Labels)\n\n        for K_Class in range(K):\n            # Unary potential: -log P(x_i | K_Class) calculated in-place\n            np.negative(Log_Probabilities[..., K_Class], out=Energy)\n\n            # Pairwise Potts spatial prior: accumulate neighbor mismatches in-place\n            for Dz, Dy, Dx in Neighbors:\n                Neighbor_Labels = self._Shifted_View(self.Labels, Dz, Dy, Dx)\n                Energy += np.where(Neighbor_Labels != K_Class, self.Beta, 0.0)\n\n            # Update best label wherever candidate class achieves strictly lower energy\n            Improved = Energy &lt; Best_Energy\n            Best_Energy[Improved] = Energy[Improved]\n            Best_Label[Improved] = K_Class\n\n        Changes = int(np.sum(Best_Label != self.Labels))\n        np.copyto(self.Labels, Best_Label)\n\n        print(\n            f\"[HMRF] Iteration {Iteration + 1}/{self.Max_Iterations}: {Changes} label updates\"\n        )\n\n        if Changes == 0:\n            print(\"[HMRF] Spatial optimization converged.\")\n            break\n\n    return self.Labels\n</code></pre>"},{"location":"api/research_ct/#research_ct.segmentation.decision_engine","title":"<code>research_ct.segmentation.decision_engine</code>","text":"<p>Orchestration engine: GMM -&gt; Hierarchical Refinement -&gt; Spatial HMRF.</p> <p>Classes:</p> Name Description <code>Segmentation_Engine</code> <p>End-to-end segmentation engine operating on 3D micro-CT volumes.</p>"},{"location":"api/research_ct/#research_ct.segmentation.decision_engine-classes","title":"Classes","text":""},{"location":"api/research_ct/#research_ct.segmentation.decision_engine.Segmentation_Engine","title":"<code>Segmentation_Engine(Gmm_Min_K=2, Gmm_Max_K=8, Hierarchy_Max_Depth=5, Hierarchy_Min_Samples=1000, Hierarchy_Alpha=0.05, Hmrf_Beta=0.5, Hmrf_Iterations=50)</code>","text":"<p>End-to-end segmentation engine operating on 3D micro-CT volumes.</p> Usage <p>Engine = Segmentation_Engine() Labels, Probabilities = Engine.Run(Volume)</p> <p>Methods:</p> Name Description <code>Run</code> <p>Run complete segmentation pipeline without array copies.</p> Source code in <code>src\\research_ct\\segmentation\\decision_engine.py</code> <pre><code>def __init__(\n    self,\n    Gmm_Min_K: int = 2,\n    Gmm_Max_K: int = 8,\n    Hierarchy_Max_Depth: int = 5,\n    Hierarchy_Min_Samples: int = 1000,\n    Hierarchy_Alpha: float = 0.05,\n    Hmrf_Beta: float = 0.5,\n    Hmrf_Iterations: int = 50,\n):\n    self.Gmm_Min_K = Gmm_Min_K\n    self.Gmm_Max_K = Gmm_Max_K\n    self.Hierarchy_Max_Depth = Hierarchy_Max_Depth\n    self.Hierarchy_Min_Samples = Hierarchy_Min_Samples\n    self.Hierarchy_Alpha = Hierarchy_Alpha\n    self.Hmrf_Beta = Hmrf_Beta\n    self.Hmrf_Iterations = Hmrf_Iterations\n\n    self.Gmm: Optional[Gmm_Fitter] = None\n    self.Hierarchy: Optional[Hierarchical_Gmm] = None\n    self.Hmrf: Optional[Hmrf_Segmenter] = None\n</code></pre>"},{"location":"api/research_ct/#research_ct.segmentation.decision_engine.Segmentation_Engine-functions","title":"Methods:","text":""},{"location":"api/research_ct/#research_ct.segmentation.decision_engine.Segmentation_Engine.Run","title":"<code>Run(Volume, Use_Hierarchy=True, Use_Hmrf=True)</code>","text":"<p>Run complete segmentation pipeline without array copies.</p> <p>Parameters:</p> Name Type Description Default <code>Volume</code> <code>ndarray</code> <p>Preprocessed 3D volume, shape (D, H, W).</p> required <code>Use_Hierarchy</code> <code>bool</code> <p>Enable soft hierarchical splitting.</p> <code>True</code> <code>Use_Hmrf</code> <code>bool</code> <p>Enable spatial regularization.</p> <code>True</code> <p>Returns:</p> Type Description <code>Tuple[ndarray, ndarray]</code> <p>Tuple of (Labels, Probabilities): Labels: Integer label volume, shape (D, H, W). Probabilities: Component posterior volume, shape (D, H, W, K_Final).</p> Source code in <code>src\\research_ct\\segmentation\\decision_engine.py</code> <pre><code>def Run(\n    self,\n    Volume: np.ndarray,\n    Use_Hierarchy: bool = True,\n    Use_Hmrf: bool = True,\n) -&gt; Tuple[np.ndarray, np.ndarray]:\n    \"\"\"Run complete segmentation pipeline without array copies.\n\n    Args:\n        Volume: Preprocessed 3D volume, shape (D, H, W).\n        Use_Hierarchy: Enable soft hierarchical splitting.\n        Use_Hmrf: Enable spatial regularization.\n\n    Returns:\n        Tuple of (Labels, Probabilities):\n            Labels: Integer label volume, shape (D, H, W).\n            Probabilities: Component posterior volume, shape (D, H, W, K_Final).\n    \"\"\"\n    D, H, W = Volume.shape\n    Flat_Data = Volume.reshape(-1, 1)\n\n    print(\"[Engine] Step 1/3: Fitting flat baseline GMM...\")\n    self.Gmm = Gmm_Fitter(\n        Min_Components=self.Gmm_Min_K,\n        Max_Components=self.Gmm_Max_K,\n    )\n    self.Gmm.Fit(Flat_Data)\n\n    Initial_K = self.Gmm.Num_Components\n    print(f\"[Engine] Flat GMM selected initial K={Initial_K}\")\n\n    Probabilities = self.Gmm.Predict_Probabilities(Flat_Data)\n\n    # Optional Step 2: Soft Hierarchical Refinement\n    if Use_Hierarchy:\n        print(\"[Engine] Step 2/3: Building soft hierarchical refinement trees...\")\n        self.Hierarchy = Hierarchical_Gmm(\n            Min_Samples=self.Hierarchy_Min_Samples,\n            Max_Depth=self.Hierarchy_Max_Depth,\n            Significance_Alpha=self.Hierarchy_Alpha,\n        )\n        self.Hierarchy.Fit(Flat_Data, Initial_K=Initial_K)\n\n        # Extract soft leaf path probabilities directly (no flat GMM re-fitting)\n        Probabilities = self.Hierarchy.Predict_Leaf_Probabilities(Flat_Data)\n        Final_K = Probabilities.shape[1]\n        print(f\"[Engine] Hierarchical refinement produced Final K={Final_K} leaf components\")\n    else:\n        Final_K = Initial_K\n\n    # Compute log unary scores for spatial regularization\n    Log_Probs = np.log(Probabilities + 1e-10)\n    Log_Probs_Volume = Log_Probs.reshape(D, H, W, Final_K)\n    Prob_Volume = Probabilities.reshape(D, H, W, Final_K)\n\n    # Optional Step 3: Spatial HMRF Optimization\n    if Use_Hmrf:\n        print(\"[Engine] Step 3/3: Running HMRF spatial regularization...\")\n        self.Hmrf = Hmrf_Segmenter(\n            Beta=self.Hmrf_Beta,\n            Max_Iterations=self.Hmrf_Iterations,\n        )\n        # Pass log unary probabilities volume directly as the primary parameter\n        Labels = self.Hmrf.Fit(Log_Probs_Volume)\n    else:\n        Labels = np.argmax(Probabilities, axis=1).reshape(D, H, W)\n\n    print(\"[Engine] Segmentation complete successfully.\")\n    return Labels, Prob_Volume\n</code></pre>"},{"location":"api/research_ct/#analysis","title":"Analysis","text":""},{"location":"api/research_ct/#research_ct.analysis.material_stats","title":"<code>research_ct.analysis.material_stats</code>","text":"<p>Material statistics computation.</p> <p>Functions:</p> Name Description <code>Compute_Material_Statistics</code> <p>Compute statistics per material class.</p> <code>Print_Material_Report</code> <p>Print formatted material statistics to console.</p>"},{"location":"api/research_ct/#research_ct.analysis.material_stats-functions","title":"Functions:","text":""},{"location":"api/research_ct/#research_ct.analysis.material_stats.Compute_Material_Statistics","title":"<code>Compute_Material_Statistics(Volume, Labels, Num_Classes)</code>","text":"<p>Compute statistics per material class.</p> <p>Parameters:</p> Name Type Description Default <code>Volume</code> <code>ndarray</code> <p>Preprocessed volume (D, H, W).</p> required <code>Labels</code> <code>ndarray</code> <p>Integer label array (D, H, W).</p> required <code>Num_Classes</code> <code>int</code> <p>Total number of classes.</p> required <p>Returns:</p> Type Description <code>Dict[str, any]</code> <p>Dictionary with per-class and global statistics.</p> Source code in <code>src\\research_ct\\analysis\\material_stats.py</code> <pre><code>def Compute_Material_Statistics(\n    Volume: np.ndarray,\n    Labels: np.ndarray,\n    Num_Classes: int,\n) -&gt; Dict[str, any]:\n    \"\"\"Compute statistics per material class.\n\n    Args:\n        Volume: Preprocessed volume (D, H, W).\n        Labels: Integer label array (D, H, W).\n        Num_Classes: Total number of classes.\n\n    Returns:\n        Dictionary with per-class and global statistics.\n    \"\"\"\n    Total_Voxels = Volume.size\n\n    Stats = {\n        \"num_classes\": Num_Classes,\n        \"total_voxels\": Total_Voxels,\n        \"classes\": [],\n    }\n\n    for K in range(Num_Classes):\n        Mask = Labels == K\n        Count = Mask.sum()\n\n        if Count == 0:\n            continue\n\n        Class_Voxels = Volume[Mask]\n\n        Class_Stats = {\n            \"class_id\": K,\n            \"voxel_count\": int(Count),\n            \"volume_fraction\": float(Count / Total_Voxels),\n            \"mean_intensity\": float(Class_Voxels.mean()),\n            \"std_intensity\": float(Class_Voxels.std()),\n            \"min_intensity\": float(Class_Voxels.min()),\n            \"max_intensity\": float(Class_Voxels.max()),\n        }\n\n        Stats[\"classes\"].append(Class_Stats)\n\n    # Global\n    Stats[\"global_mean\"] = float(Volume.mean())\n    Stats[\"global_std\"] = float(Volume.std())\n\n    return Stats\n</code></pre>"},{"location":"api/research_ct/#research_ct.analysis.material_stats.Print_Material_Report","title":"<code>Print_Material_Report(Stats)</code>","text":"<p>Print formatted material statistics to console.</p> <p>Parameters:</p> Name Type Description Default <code>Stats</code> <code>Dict</code> <p>Output from Compute_Material_Statistics.</p> required Source code in <code>src\\research_ct\\analysis\\material_stats.py</code> <pre><code>def Print_Material_Report(Stats: Dict) -&gt; None:\n    \"\"\"Print formatted material statistics to console.\n\n    Args:\n        Stats: Output from Compute_Material_Statistics.\n    \"\"\"\n    print(\"\\n\" + \"=\" * 60)\n    print(\"MATERIAL STATISTICS\")\n    print(\"=\" * 60)\n    print(f\"Total voxels: {Stats['total_voxels']:,}\")\n    print(f\"Global mean: {Stats['global_mean']:.2f}\")\n    print(f\"Global std: {Stats['global_std']:.2f}\")\n    print(\"-\" * 60)\n    print(f\"{'Class':&lt;8} {'Count':&lt;12} {'Fraction':&lt;12} {'Mean':&lt;10} {'Std':&lt;10}\")\n    print(\"-\" * 60)\n\n    for C in Stats[\"classes\"]:\n        print(\n            f\"{C['class_id']:&lt;8} \"\n            f\"{C['voxel_count']:&lt;12,} \"\n            f\"{C['volume_fraction']:&lt;12.4f} \"\n            f\"{C['mean_intensity']:&lt;10.2f} \"\n            f\"{C['std_intensity']:&lt;10.2f}\"\n        )\n\n    print(\"=\" * 60)\n</code></pre>"},{"location":"api/research_ct/#research_ct.analysis.uncertainty_maps","title":"<code>research_ct.analysis.uncertainty_maps</code>","text":"<p>Uncertainty quantification from probabilistic segmentation.</p> <p>Functions:</p> Name Description <code>Compute_Uncertainty</code> <p>Compute uncertainty maps from class probabilities.</p> <code>Compute_Margin</code> <p>Compute margin between top two class probabilities.</p>"},{"location":"api/research_ct/#research_ct.analysis.uncertainty_maps-functions","title":"Functions:","text":""},{"location":"api/research_ct/#research_ct.analysis.uncertainty_maps.Compute_Uncertainty","title":"<code>Compute_Uncertainty(Probabilities)</code>","text":"<p>Compute uncertainty maps from class probabilities.</p> <p>Parameters:</p> Name Type Description Default <code>Probabilities</code> <code>ndarray</code> <p>Class probabilities, shape (D, H, W, K).</p> required <p>Returns:</p> Type Description <code>Tuple[ndarray, ndarray]</code> <p>Tuple of (entropy, max_probability). Entropy: shape (D, H, W), high = uncertain. Max_Probability: shape (D, H, W), high = confident.</p> Source code in <code>src\\research_ct\\analysis\\uncertainty_maps.py</code> <pre><code>def Compute_Uncertainty(Probabilities: np.ndarray) -&gt; Tuple[np.ndarray, np.ndarray]:\n    \"\"\"Compute uncertainty maps from class probabilities.\n\n    Args:\n        Probabilities: Class probabilities, shape (D, H, W, K).\n\n    Returns:\n        Tuple of (entropy, max_probability).\n            Entropy: shape (D, H, W), high = uncertain.\n            Max_Probability: shape (D, H, W), high = confident.\n    \"\"\"\n    # Shannon entropy: H = -sum(p * log(p))\n    # Clip to avoid log(0)\n    Clipped = np.clip(Probabilities, 1e-10, 1.0)\n    Entropy = -np.sum(Clipped * np.log(Clipped), axis=3)\n\n    # Maximum probability (confidence)\n    Max_Prob = np.max(Probabilities, axis=3)\n\n    return Entropy, Max_Prob\n</code></pre>"},{"location":"api/research_ct/#research_ct.analysis.uncertainty_maps.Compute_Margin","title":"<code>Compute_Margin(Probabilities, Top_K=2)</code>","text":"<p>Compute margin between top two class probabilities.</p> <p>Small margin = high uncertainty (ambiguous between two classes).</p> <p>Parameters:</p> Name Type Description Default <code>Probabilities</code> <code>ndarray</code> <p>Class probabilities, shape (D, H, W, K).</p> required <code>Top_K</code> <code>int</code> <p>Number of top classes to compare.</p> <code>2</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>Margin array, shape (D, H, W).</p> Source code in <code>src\\research_ct\\analysis\\uncertainty_maps.py</code> <pre><code>def Compute_Margin(\n    Probabilities: np.ndarray,\n    Top_K: int = 2,\n) -&gt; np.ndarray:\n    \"\"\"Compute margin between top two class probabilities.\n\n    Small margin = high uncertainty (ambiguous between two classes).\n\n    Args:\n        Probabilities: Class probabilities, shape (D, H, W, K).\n        Top_K: Number of top classes to compare.\n\n    Returns:\n        Margin array, shape (D, H, W).\n    \"\"\"\n    Sorted = np.sort(Probabilities, axis=3)\n\n    if Top_K == 2:\n        return Sorted[..., -1] - Sorted[..., -2]\n\n    return Sorted[..., -1] - Sorted[..., -Top_K].mean(axis=3)\n</code></pre>"},{"location":"api/research_ct/#processing","title":"Processing","text":""},{"location":"api/research_ct/#research_ct.processing.dragonfly_utils","title":"<code>research_ct.processing.dragonfly_utils</code>","text":"<p>High-level Dragonfly export orchestration.</p> <p>Thin wrapper around <code>research_ct.io.dragonfly_exporter</code> that produces a self-describing directory with three TIFF stacks, a metadata JSON, and a label-colour CSV \u2014 everything needed to drop into Dragonfly for 3D inspection.</p> <p>Classes:</p> Name Description <code>Dragonfly_Export_Config</code> <p>Parameters for a Dragonfly export batch.</p> <p>Functions:</p> Name Description <code>Export_Dragonfly_Channels</code> <p>Export the three analysis channels as Dragonfly-compatible TIFF stacks.</p>"},{"location":"api/research_ct/#research_ct.processing.dragonfly_utils-classes","title":"Classes","text":""},{"location":"api/research_ct/#research_ct.processing.dragonfly_utils.Dragonfly_Export_Config","title":"<code>Dragonfly_Export_Config(Output_Dir, Voxel_Spacing=(1.0, 1.0, 1.0), Spacing_Units='um', Label_Colors=(lambda: dict(_DEFAULT_COLORS))(), Label_Names=(lambda: dict(_DEFAULT_NAMES))(), Compression=None)</code>  <code>dataclass</code>","text":"<p>Parameters for a Dragonfly export batch.</p> <p>Attributes:</p> Name Type Description <code>Output_Dir</code> <code>Path</code> <p>Directory that will contain all exported files.</p> <code>Voxel_Spacing</code> <code>Tuple[float, float, float]</code> <p>(Z, Y, X) spacing in Spacing_Units.</p> <code>Spacing_Units</code> <code>str</code> <p>Unit label (<code>\"um\"</code>, <code>\"mm\"</code>, <code>\"cm\"</code>).</p> <code>Label_Colors</code> <code>Dict[int, Tuple[int, int, int]]</code> <p><code>{class_id: (R, G, B)}</code> mapping, 0-255 per channel.</p> <code>Label_Names</code> <code>Dict[int, str]</code> <p><code>{class_id: \"name\"}</code> mapping.</p> <code>Compression</code> <code>Optional[str]</code> <p>TIFF compression (None = uncompressed).</p>"},{"location":"api/research_ct/#research_ct.processing.dragonfly_utils.Dragonfly_Export_Config-attributes","title":"Attributes","text":""},{"location":"api/research_ct/#research_ct.processing.dragonfly_utils.Dragonfly_Export_Config.Output_Dir","title":"<code>Output_Dir</code>  <code>instance-attribute</code>","text":"<p>Directory that will contain all exported files.</p>"},{"location":"api/research_ct/#research_ct.processing.dragonfly_utils.Dragonfly_Export_Config.Voxel_Spacing","title":"<code>Voxel_Spacing = (1.0, 1.0, 1.0)</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>(Z, Y, X) spacing in Spacing_Units.</p>"},{"location":"api/research_ct/#research_ct.processing.dragonfly_utils.Dragonfly_Export_Config.Spacing_Units","title":"<code>Spacing_Units = 'um'</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>Unit label (<code>\"um\"</code>, <code>\"mm\"</code>, <code>\"cm\"</code>).</p>"},{"location":"api/research_ct/#research_ct.processing.dragonfly_utils.Dragonfly_Export_Config.Label_Colors","title":"<code>Label_Colors = field(default_factory=(lambda: dict(_DEFAULT_COLORS)))</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p><code>{class_id: (R, G, B)}</code> mapping, 0-255 per channel.</p>"},{"location":"api/research_ct/#research_ct.processing.dragonfly_utils.Dragonfly_Export_Config.Label_Names","title":"<code>Label_Names = field(default_factory=(lambda: dict(_DEFAULT_NAMES)))</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p><code>{class_id: \"name\"}</code> mapping.</p>"},{"location":"api/research_ct/#research_ct.processing.dragonfly_utils.Dragonfly_Export_Config.Compression","title":"<code>Compression = None</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>TIFF compression (None = uncompressed).</p>"},{"location":"api/research_ct/#research_ct.processing.dragonfly_utils-functions","title":"Functions:","text":""},{"location":"api/research_ct/#research_ct.processing.dragonfly_utils.Export_Dragonfly_Channels","title":"<code>Export_Dragonfly_Channels(Processed, Labels, Entropy, Config, *, Prefix='dragonfly', Big_Tiff=None)</code>","text":"<p>Export the three analysis channels as Dragonfly-compatible TIFF stacks.</p> <p>Creates the following files inside <code>Config.Output_Dir</code>:</p> <ul> <li><code>{Prefix}_grayscale.tif</code> \u2014 preprocessed intensity volume</li> <li><code>{Prefix}_labels.tif</code> \u2014 RGB label volume with user-defined colours</li> <li><code>{Prefix}_entropy.tif</code> \u2014 entropy (uncertainty) heatmap</li> <li><code>{Prefix}_metadata.json</code> \u2014 shape, spacing, and class metadata</li> <li><code>{Prefix}_label_colors.csv</code> \u2014 Dragonfly material colour table</li> </ul> <p>All writes stream slice-by-slice to keep memory bounded for large memmap-backed arrays.</p> <p>Parameters:</p> Name Type Description Default <code>Processed</code> <code>ndarray</code> <p><code>(D, H, W)</code> grayscale volume (float or int).</p> required <code>Labels</code> <code>ndarray</code> <p><code>(D, H, W)</code> integer labels.</p> required <code>Entropy</code> <code>ndarray</code> <p><code>(D, H, W)</code> entropy map.</p> required <code>Config</code> <code>Dragonfly_Export_Config</code> <p>Export parameters.</p> required <code>Prefix</code> <code>str</code> <p>Basename prefix for written files.</p> <code>'dragonfly'</code> <code>Big_Tiff</code> <code>Optional[bool]</code> <p>If True, force BigTIFF.  If None, auto-detect.</p> <code>None</code> <p>Returns:</p> Type Description <code>Dict[str, Path]</code> <p>Dict mapping channel name (<code>\"grayscale\"</code>, <code>\"labels\"</code>, <code>\"entropy\"</code>)</p> <code>Dict[str, Path]</code> <p>to the written <code>Path</code>.</p> Source code in <code>src\\research_ct\\processing\\dragonfly_utils.py</code> <pre><code>def Export_Dragonfly_Channels(\n    Processed: np.ndarray,\n    Labels: np.ndarray,\n    Entropy: np.ndarray,\n    Config: Dragonfly_Export_Config,\n    *,\n    Prefix: str = \"dragonfly\",\n    Big_Tiff: Optional[bool] = None,\n) -&gt; Dict[str, Path]:\n    \"\"\"Export the three analysis channels as Dragonfly-compatible TIFF stacks.\n\n    Creates the following files inside ``Config.Output_Dir``:\n\n    * ``{Prefix}_grayscale.tif`` \u2014 preprocessed intensity volume\n    * ``{Prefix}_labels.tif`` \u2014 RGB label volume with user-defined colours\n    * ``{Prefix}_entropy.tif`` \u2014 entropy (uncertainty) heatmap\n    * ``{Prefix}_metadata.json`` \u2014 shape, spacing, and class metadata\n    * ``{Prefix}_label_colors.csv`` \u2014 Dragonfly material colour table\n\n    All writes stream slice-by-slice to keep memory bounded for large\n    memmap-backed arrays.\n\n    Args:\n        Processed: ``(D, H, W)`` grayscale volume (float or int).\n        Labels: ``(D, H, W)`` integer labels.\n        Entropy: ``(D, H, W)`` entropy map.\n        Config: Export parameters.\n        Prefix: Basename prefix for written files.\n        Big_Tiff: If True, force BigTIFF.  If None, auto-detect.\n\n    Returns:\n        Dict mapping channel name (``\"grayscale\"``, ``\"labels\"``, ``\"entropy\"``)\n        to the written ``Path``.\n    \"\"\"\n    Output_Dir = Path(Config.Output_Dir)\n    Output_Dir.mkdir(parents=True, exist_ok=True)\n\n    # ---- Sidecar files -------------------------------------------------\n    Save_Dragonfly_Metadata_Json(\n        Output_Dir / f\"{Prefix}_metadata.json\",\n        Shape=Processed.shape[:3],\n        Voxel_Spacing=Config.Voxel_Spacing,\n        Spacing_Units=Config.Spacing_Units,\n        Channels={\n            \"grayscale\": {\"dtype\": str(Processed.dtype)},\n            \"labels\": {\"dtype\": \"uint16\", \"num_classes\": len(Config.Label_Colors)},\n            \"entropy\": {\"dtype\": \"float32\"},\n        },\n        Label_Colors=Config.Label_Colors,\n        Label_Names=Config.Label_Names,\n    )\n    Save_Label_Colors_Csv(\n        Output_Dir / f\"{Prefix}_label_colors.csv\",\n        Label_Colors=Config.Label_Colors,\n        Label_Names=Config.Label_Names,\n    )\n\n    # ---- Channel TIFFs -------------------------------------------------\n    Grayscale_Path = Save_Volume_As_Tiff(\n        Processed,\n        Output_Dir / f\"{Prefix}_grayscale.tif\",\n        Voxel_Spacing=Config.Voxel_Spacing,\n        Spacing_Units=Config.Spacing_Units,\n        Compression=Config.Compression,\n        Big_Tiff=Big_Tiff,\n    )\n\n    # \u2500\u2500 Label TIFF (single-channel uint16 \u2014 Dragonfly reads the CSV for colours)\n    Labels_Path = Save_Volume_As_Tiff(\n        Labels.astype(np.uint16, copy=False),\n        Output_Dir / f\"{Prefix}_labels.tif\",\n        Voxel_Spacing=Config.Voxel_Spacing,\n        Spacing_Units=Config.Spacing_Units,\n        Compression=Config.Compression,\n        Big_Tiff=Big_Tiff,\n    )\n\n    Entropy_Path = Save_Volume_As_Tiff(\n        Entropy.astype(np.float32, copy=False),\n        Output_Dir / f\"{Prefix}_entropy.tif\",\n        Voxel_Spacing=Config.Voxel_Spacing,\n        Spacing_Units=Config.Spacing_Units,\n        Compression=Config.Compression,\n        Big_Tiff=Big_Tiff,\n    )\n\n    Paths: Dict[str, Path] = {\n        \"grayscale\": Grayscale_Path,\n        \"labels\": Labels_Path,\n        \"entropy\": Entropy_Path,\n    }\n\n    print(f\"[DragonflyExport] {len(Paths)} channels written -&gt; {Output_Dir}\")\n    return Paths\n</code></pre>"},{"location":"api/research_ct/#visualization","title":"Visualization","text":""},{"location":"api/research_ct/#research_ct.visualization.napari_viewer","title":"<code>research_ct.visualization.napari_viewer</code>","text":"<p>Interactive 3D visualization with napari.</p> <p>Functions:</p> Name Description <code>launch_napari_viewer</code> <p>Launch napari with volume and optional segmentation overlays.</p>"},{"location":"api/research_ct/#research_ct.visualization.napari_viewer-functions","title":"Functions:","text":""},{"location":"api/research_ct/#research_ct.visualization.napari_viewer.launch_napari_viewer","title":"<code>launch_napari_viewer(Volume, Labels=None, Probabilities=None, Entropy=None, Probability_Threshold=0.3, Render_3d=False, Mesh_Surfaces=None, Surface_Opacity=0.6, Title='micro-CT Segmentation Results')</code>","text":"<p>Launch napari with volume and optional segmentation overlays.</p> <p>Parameters:</p> Name Type Description Default <code>Volume</code> <code>ndarray</code> <p>Raw or preprocessed volume (D, H, W).</p> required <code>Labels</code> <code>Optional[ndarray]</code> <p>Integer labels (D, H, W).</p> <code>None</code> <code>Probabilities</code> <code>Optional[ndarray]</code> <p>Class probabilities (D, H, W, K).</p> <code>None</code> <code>Entropy</code> <code>Optional[ndarray]</code> <p>Shannon entropy map (D, H, W) \u2014 added as semi-transparent heatmap.</p> <code>None</code> <code>Probability_Threshold</code> <code>float</code> <p>Contrast lower limit for probability layers.</p> <code>0.3</code> <code>Render_3d</code> <code>bool</code> <p>If True, enable 3D rendering on launch.</p> <code>False</code> <code>Mesh_Surfaces</code> <code>Optional[Dict[int, Dict[str, Any]]]</code> <p>Dict mapping class index to mesh dict with <code>vertices</code> and <code>faces</code> keys.  Each mesh is added as a surface layer.</p> <code>None</code> <code>Surface_Opacity</code> <code>float</code> <p>Opacity for surface layers (0.0\u20131.0).</p> <code>0.6</code> <code>Title</code> <code>str</code> <p>Viewer window title.</p> <code>'micro-CT Segmentation Results'</code> <p>Returns:</p> Type Description <code>Any</code> <p>napari.Viewer instance (caller can add further layers after return).</p> Source code in <code>src\\research_ct\\visualization\\napari_viewer.py</code> <pre><code>def launch_napari_viewer(\n    Volume: np.ndarray,\n    Labels: Optional[np.ndarray] = None,\n    Probabilities: Optional[np.ndarray] = None,\n    Entropy: Optional[np.ndarray] = None,\n    Probability_Threshold: float = 0.3,\n    Render_3d: bool = False,\n    Mesh_Surfaces: Optional[Dict[int, Dict[str, Any]]] = None,\n    Surface_Opacity: float = 0.6,\n    Title: str = \"micro-CT Segmentation Results\",\n) -&gt; Any:\n    \"\"\"Launch napari with volume and optional segmentation overlays.\n\n    Args:\n        Volume: Raw or preprocessed volume (D, H, W).\n        Labels: Integer labels (D, H, W).\n        Probabilities: Class probabilities (D, H, W, K).\n        Entropy: Shannon entropy map (D, H, W) \u2014 added as semi-transparent heatmap.\n        Probability_Threshold: Contrast lower limit for probability layers.\n        Render_3d: If True, enable 3D rendering on launch.\n        Mesh_Surfaces: Dict mapping class index to mesh dict with ``vertices``\n            and ``faces`` keys.  Each mesh is added as a surface layer.\n        Surface_Opacity: Opacity for surface layers (0.0\u20131.0).\n        Title: Viewer window title.\n\n    Returns:\n        napari.Viewer instance (caller can add further layers after return).\n    \"\"\"\n    import napari\n\n    Viewer = napari.Viewer(title=Title)\n\n    # --- Core volume -----------------------------------------------------------\n    Viewer.add_image(Volume, name=\"Volume\", colormap=\"gray\")\n\n    # --- Hard labels -----------------------------------------------------------\n    if Labels is not None:\n        Viewer.add_labels(Labels, name=\"Segmentation\", opacity=0.6)\n\n    # --- Probability layers (all classes) --------------------------------------\n    if Probabilities is not None:\n        K_Classes = Probabilities.shape[-1]\n        for K in range(K_Classes):\n            Viewer.add_image(\n                Probabilities[..., K],\n                name=f\"P(class={K})\",\n                visible=False,\n                colormap=\"magma\",\n                contrast_limits=(Probability_Threshold, 1.0),\n                blending=\"additive\",\n            )\n\n    # --- Entropy overlay (semi-transparent heatmap) ----------------------------\n    if Entropy is not None:\n        Entropy_Max = np.log(K_Classes) if Probabilities is not None else Entropy.max()\n        Viewer.add_image(\n            Entropy,\n            name=\"Entropy (uncertainty)\",\n            colormap=\"hot\",\n            opacity=0.35,\n            blending=\"additive\",\n            contrast_limits=(0.0, Entropy_Max),\n        )\n\n    # --- Surface meshes (3D reconstruction) ------------------------------------\n    if Mesh_Surfaces is not None:\n        # Pre-define class colors via matplotlib tab10\n        try:\n            import matplotlib.pyplot as plt\n\n            Tab10 = plt.cm.tab10(np.linspace(0, 1, max(K_Classes, 1)))\n        except Exception:  # noqa\n            Tab10 = np.array(\n                [\n                    [0.12, 0.47, 0.71],\n                    [1.00, 0.50, 0.05],\n                    [0.17, 0.63, 0.17],\n                    [0.84, 0.15, 0.16],\n                    [0.58, 0.40, 0.74],\n                ]\n            )\n\n        for Class_Idx, Mesh in Mesh_Surfaces.items():\n            Vertices = Mesh.get(\"vertices\")\n            Faces = Mesh.get(\"faces\")\n            if Vertices is None or Faces is None or len(Vertices) == 0:\n                continue\n\n            Color = Tab10[Class_Idx % len(Tab10)][:3]\n            Viewer.add_surface(\n                (Vertices, Faces),\n                name=f\"Surface class {Class_Idx}\",\n                colormap=[tuple(Color.tolist())],\n                opacity=Surface_Opacity,\n                blending=\"translucent\",\n            )\n\n    # --- 3D rendering mode -----------------------------------------------------\n    if Render_3d:\n        Viewer.dims.ndisplay = 3\n\n    napari.run()\n    return Viewer\n</code></pre>"},{"location":"api/research_ct/#research_ct.visualization.export","title":"<code>research_ct.visualization.export</code>","text":"<p>Export rendered outputs.</p> <p>Functions:</p> Name Description <code>export_probability_video</code> <p>Export probability map as video.</p> <code>export_label_colors</code> <p>Export colored label volume.</p>"},{"location":"api/research_ct/#research_ct.visualization.export-functions","title":"Functions:","text":""},{"location":"api/research_ct/#research_ct.visualization.export.export_probability_video","title":"<code>export_probability_video(Probabilities, Output_Path, Class_Index=0, Fps=10)</code>","text":"<p>Export probability map as video.</p> <p>Parameters:</p> Name Type Description Default <code>Probabilities</code> <code>ndarray</code> <p>Shape (D, H, W, K).</p> required <code>Output_Path</code> <code>str</code> <p>Output file path (.mp4 or .gif).</p> required <code>Class_Index</code> <code>int</code> <p>Which class probability to visualize.</p> <code>0</code> <code>Fps</code> <code>int</code> <p>Frames per second.</p> <code>10</code> Source code in <code>src\\research_ct\\visualization\\export.py</code> <pre><code>def export_probability_video(\n    Probabilities: np.ndarray,\n    Output_Path: str,\n    Class_Index: int = 0,\n    Fps: int = 10,\n) -&gt; None:\n    \"\"\"Export probability map as video.\n\n    Args:\n        Probabilities: Shape (D, H, W, K).\n        Output_Path: Output file path (.mp4 or .gif).\n        Class_Index: Which class probability to visualize.\n        Fps: Frames per second.\n    \"\"\"\n    try:\n        import imageio\n    except ImportError:\n        print(\"[export] imageio not available for video export\")\n        return\n\n    Frames = (Probabilities[..., Class_Index] * 255).astype(np.uint8)\n\n    Writer = imageio.get_writer(Output_Path, fps=Fps)\n\n    for Z in range(Frames.shape[0]):\n        Writer.append_data(Frames[Z])\n\n    Writer.close()\n    print(f\"[export] Saved video \u2192 {Output_Path}\")\n</code></pre>"},{"location":"api/research_ct/#research_ct.visualization.export.export_label_colors","title":"<code>export_label_colors(Labels, Output_Path, Color_Map=None)</code>","text":"<p>Export colored label volume.</p> <p>Parameters:</p> Name Type Description Default <code>Labels</code> <code>ndarray</code> <p>Integer labels (D, H, W).</p> required <code>Output_Path</code> <code>str</code> <p>Output file path.</p> required <code>Color_Map</code> <code>Optional[Dict[int, tuple]]</code> <p>Dict mapping label to (R, G, B).</p> <code>None</code> Source code in <code>src\\research_ct\\visualization\\export.py</code> <pre><code>def export_label_colors(\n    Labels: np.ndarray,\n    Output_Path: str,\n    Color_Map: Optional[Dict[int, tuple]] = None,\n) -&gt; None:\n    \"\"\"Export colored label volume.\n\n    Args:\n        Labels: Integer labels (D, H, W).\n        Output_Path: Output file path.\n        Color_Map: Dict mapping label to (R, G, B).\n    \"\"\"\n    # Save as stack\n    from research_ct.io.volume_loader import Save_Volume_As_Stack\n\n    if Color_Map is None:\n        # Default colors\n        Color_Map = {\n            0: (0, 0, 0),       # Background/air\n            1: (255, 228, 181), # Paper\n            2: (47, 79, 79),    # Ink\n            3: (139, 69, 19),   # Cover\n        }\n\n    D, H, W = Labels.shape\n    Colored = np.zeros((D, H, W, 3), dtype=np.uint8)\n\n    for Label, Color in Color_Map.items():\n        Mask = Labels == Label\n        Colored[Mask] = Color\n\n    # Or use imageio for multi-page TIFF\n    import imageio\n    imageio.mimwrite(Output_Path, [Colored[i] for i in range(D)])\n\n    Save_Volume_As_Stack(Colored, Output_Path)\n</code></pre>"},{"location":"api/xrf/","title":"xrf \u2014 XRF Pipeline API","text":"<p>Auto-generated from source docstrings.</p>"},{"location":"api/xrf/#io","title":"IO","text":""},{"location":"api/xrf/#xrf.io.xrf_loader","title":"<code>xrf.io.xrf_loader</code>","text":"<p>Module for loading multiespectral X-Ray Fluorescence (XRF) data. Manages TIFF file reading and total intensity mask T(p) computation.</p> <p>Classes:</p> Name Description <code>Xrf_Loader</code> <p>Utility class to load and preprocess the elemental XRF data cube.</p> <p>Functions:</p> Name Description <code>Update_Page_Metadata</code> <p>Merge new fields into an existing page_NNN_meta.json file.</p>"},{"location":"api/xrf/#xrf.io.xrf_loader-classes","title":"Classes","text":""},{"location":"api/xrf/#xrf.io.xrf_loader.Xrf_Loader","title":"<code>Xrf_Loader</code>","text":"<p>Utility class to load and preprocess the elemental XRF data cube.</p> <p>Methods:</p> Name Description <code>Load_Element_Stack</code> <p>Loads a list of TIFF files corresponding to each chemical channel and stacks them.</p> <code>Compute_Intensity_Mask</code> <p>Computes the accumulated total intensity T(p) and extracts valid pixels.</p>"},{"location":"api/xrf/#xrf.io.xrf_loader.Xrf_Loader-functions","title":"Methods:","text":""},{"location":"api/xrf/#xrf.io.xrf_loader.Xrf_Loader.Load_Element_Stack","title":"<code>Load_Element_Stack(File_Paths, Dtype='float64')</code>  <code>staticmethod</code>","text":"<p>Loads a list of TIFF files corresponding to each chemical channel and stacks them.</p> <p>Parameters:</p> Name Type Description Default <code>File_Paths</code> <code>List[Path_Like]</code> <p>List of paths to elemental TIFF files.</p> required <code>Dtype</code> <code>str</code> <p>Precision of the resulting array. Defaults to \"float64\".</p> <code>'float64'</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>np.ndarray: 3D data cube of shape (M, N, n) where n is the number of chemical elements.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the path list is empty or the images have different dimensions.</p> Source code in <code>src\\xrf\\io\\xrf_loader.py</code> <pre><code>@staticmethod\ndef Load_Element_Stack(File_Paths: List[Path_Like], Dtype: str = \"float64\") -&gt; np.ndarray:\n    \"\"\"\n    Loads a list of TIFF files corresponding to each chemical channel and stacks them.\n\n    Args:\n        File_Paths (List[Path_Like]): List of paths to elemental TIFF files.\n        Dtype (str, optional): Precision of the resulting array. Defaults to \"float64\".\n\n    Returns:\n        np.ndarray: 3D data cube of shape (M, N, n) where n is the\n            number of chemical elements.\n\n    Raises:\n        ValueError: If the path list is empty or the images have different dimensions.\n    \"\"\"\n    if not File_Paths:\n        raise ValueError(\"The File_Paths list cannot be empty.\")\n\n    Layers = []\n    for Path_Str in File_Paths:\n        Image = iio.imread(Path_Str)\n        Layers.append(Image.astype(Dtype, copy=False))\n\n    # Stack along the last axis (M, N, n)\n    Stack = np.stack(Layers, axis=-1)\n    return Stack\n</code></pre>"},{"location":"api/xrf/#xrf.io.xrf_loader.Xrf_Loader.Compute_Intensity_Mask","title":"<code>Compute_Intensity_Mask(Stack, Tau_Noise)</code>  <code>staticmethod</code>","text":"<p>Computes the accumulated total intensity T(p) and extracts valid pixels.</p> <p>Parameters:</p> Name Type Description Default <code>Stack</code> <code>ndarray</code> <p>Multichannel cube (M, N, n).</p> required <code>Tau_Noise</code> <code>float</code> <p>Minimum intensity threshold tau_noise.</p> required <p>Returns:</p> Type Description <code>Tuple[ndarray, ndarray]</code> <p>Tuple[np.ndarray, np.ndarray]: - Mask (np.ndarray): 2D boolean array (M, N) with valid pixels. - Valid_Pixels (np.ndarray): Flattened 2D array of shape (N_valid, n).</p> Source code in <code>src\\xrf\\io\\xrf_loader.py</code> <pre><code>@staticmethod\ndef Compute_Intensity_Mask(\n    Stack: np.ndarray, Tau_Noise: float\n) -&gt; Tuple[np.ndarray, np.ndarray]:\n    \"\"\"\n    Computes the accumulated total intensity T(p) and extracts valid pixels.\n\n    Args:\n        Stack (np.ndarray): Multichannel cube (M, N, n).\n        Tau_Noise (float): Minimum intensity threshold tau_noise.\n\n    Returns:\n        Tuple[np.ndarray, np.ndarray]:\n            - Mask (np.ndarray): 2D boolean array (M, N) with valid pixels.\n            - Valid_Pixels (np.ndarray): Flattened 2D array of shape (N_valid, n).\n    \"\"\"\n    # Total intensity T(p) by summing all channels\n    Total_Intensity = np.sum(Stack, axis=-1)\n\n    # Binary mask\n    Mask = Total_Intensity &gt;= Tau_Noise\n\n    # Index to extract only valid vectors (flattening)\n    Valid_Pixels = Stack[Mask]\n\n    return Mask, Valid_Pixels\n</code></pre>"},{"location":"api/xrf/#xrf.io.xrf_loader-functions","title":"Functions:","text":""},{"location":"api/xrf/#xrf.io.xrf_loader.Update_Page_Metadata","title":"<code>Update_Page_Metadata(Meta_Path, **Fields)</code>","text":"<p>Merge new fields into an existing page_NNN_meta.json file.</p> <p>Parameters:</p> Name Type Description Default <code>Meta_Path</code> <code>Path_Like</code> <p>Path to the page's meta.json file.</p> required <code>**Fields</code> <p>Key/value pairs to merge into the existing JSON object. Existing keys are overwritten; all other keys are preserved.</p> <code>{}</code> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>If Meta_Path does not exist \u2014 this function updates existing metadata, it does not create a page record from scratch.</p> Source code in <code>src\\xrf\\io\\xrf_loader.py</code> <pre><code>def Update_Page_Metadata(Meta_Path: Path_Like, **Fields) -&gt; None:\n    \"\"\"Merge new fields into an existing page_NNN_meta.json file.\n\n    Args:\n        Meta_Path: Path to the page's meta.json file.\n        **Fields: Key/value pairs to merge into the existing JSON object.\n            Existing keys are overwritten; all other keys are preserved.\n\n    Raises:\n        FileNotFoundError: If Meta_Path does not exist \u2014 this function\n            updates existing metadata, it does not create a page record\n            from scratch.\n    \"\"\"\n    Meta_Path = Path(Meta_Path)\n    if not Meta_Path.exists():\n        raise FileNotFoundError(\n            f\"Meta_Path {Meta_Path} does not exist. Update_Page_Metadata only \"\n            \"updates existing page metadata records.\"\n        )\n\n    with open(Meta_Path, \"r\", encoding=\"utf-8\") as Meta_File:\n        Metadata = json.load(Meta_File)\n\n    Metadata.update(Fields)\n\n    with open(Meta_Path, \"w\", encoding=\"utf-8\") as Meta_File:\n        json.dump(Metadata, Meta_File, indent=2)\n\n    print(f\"[Xrf_Loader] updated metadata fields {list(Fields.keys())} in {Meta_Path}\")\n</code></pre>"},{"location":"api/xrf/#preprocessing","title":"Preprocessing","text":""},{"location":"api/xrf/#xrf.preprocessing.bcf_extractor","title":"<code>xrf.preprocessing.bcf_extractor</code>","text":"<p>Bruker BCF hypercube \u2192 elemental TIFF extraction.</p> <p>Loads a .bcf spectrum image, dynamically resolves the correct emission line for each requested element, applies dual-window Bremsstrahlung subtraction, and writes one 32-bit float <code>_raw.tiff</code> per element.</p> <p>Classes:</p> Name Description <code>Bcf_Element_Extractor</code> <p>Ingests a .bcf hypercube and exports background-subtracted elemental maps.</p>"},{"location":"api/xrf/#xrf.preprocessing.bcf_extractor-classes","title":"Classes","text":""},{"location":"api/xrf/#xrf.preprocessing.bcf_extractor.Bcf_Element_Extractor","title":"<code>Bcf_Element_Extractor(Cutoff_At_Kv=40.0, Peak_Width_Kev=0.2, Bg_Width_Kev=0.1, Bg_Offset_Kev=0.25)</code>","text":"<p>Ingests a .bcf hypercube and exports background-subtracted elemental maps.</p> For each requested element the extractor <ol> <li>Resolves the dominant emission line (K\u03b1 for Z \u2264 40, L\u03b1 otherwise).</li> <li>Integrates a peak window \u00b1 Peak_Width_Kev/2 around the line.</li> <li>Estimates the continuum from two sideband windows offset by    \u00b1 Bg_Offset_Kev from the line, each of width Bg_Width_Kev.</li> <li>Subtracts the scaled average sideband from the peak integral    and clips negative residuals to zero.</li> <li>Writes the result as a 32-bit TIFF tagged with element    symbol and line energy.</li> </ol> <p>Parameters:</p> Name Type Description Default <code>Cutoff_At_Kv</code> <code>float</code> <p>Detector energy ceiling (keV).</p> <code>40.0</code> <code>Peak_Width_Kev</code> <code>float</code> <p>Full width of the integration window (keV).</p> <code>0.2</code> <code>Bg_Width_Kev</code> <code>float</code> <p>Full width of each background sideband (keV).</p> <code>0.1</code> <code>Bg_Offset_Kev</code> <code>float</code> <p>Distance from line center to sideband center (keV).</p> <code>0.25</code> <p>Methods:</p> Name Description <code>Resolve_Emission_Line</code> <p>Look up the dominant emission line and its energy for an element.</p> <code>Extract_And_Save</code> <p>Run the full BCF \u2192 element TIFF extraction pipeline.</p> Source code in <code>src\\xrf\\preprocessing\\bcf_extractor.py</code> <pre><code>def __init__(\n    self,\n    Cutoff_At_Kv: float = 40.0,\n    Peak_Width_Kev: float = 0.20,\n    Bg_Width_Kev: float = 0.10,\n    Bg_Offset_Kev: float = 0.25,\n) -&gt; None:\n    self.cutoff_at_kv = Cutoff_At_Kv\n    self.peak_width_kev = Peak_Width_Kev\n    self.bg_width_kev = Bg_Width_Kev\n    self.bg_offset_kev = Bg_Offset_Kev\n</code></pre>"},{"location":"api/xrf/#xrf.preprocessing.bcf_extractor.Bcf_Element_Extractor-functions","title":"Methods:","text":""},{"location":"api/xrf/#xrf.preprocessing.bcf_extractor.Bcf_Element_Extractor.Resolve_Emission_Line","title":"<code>Resolve_Emission_Line(Element_Input)</code>","text":"<p>Look up the dominant emission line and its energy for an element.</p> <p>Accepts either <code>\"Fe\"</code> (auto-selects the principal line) or <code>\"Fe_Ka\"</code> (forces a specific line).  Elements with Z \u2264 40 default to K\u03b1; heavier elements default to L\u03b1.</p> <p>Parameters:</p> Name Type Description Default <code>Element_Input</code> <code>str</code> <p>Element symbol optionally extended with an underscore-separated line label (e.g. <code>\"Au_La\"</code>).</p> required <p>Returns:</p> Type Description <code>float</code> <p>A tuple <code>(Energy_Kev, Resolved_Tag)</code> where Resolved_Tag</p> <code>str</code> <p>has the form <code>\"Fe_Ka\"</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the element symbol is not recognised by the <code>exspy</code> materials database or the requested emission line is missing.</p> Source code in <code>src\\xrf\\preprocessing\\bcf_extractor.py</code> <pre><code>def Resolve_Emission_Line(self, Element_Input: str) -&gt; Tuple[float, str]:\n    \"\"\"Look up the dominant emission line and its energy for an element.\n\n    Accepts either ``\"Fe\"`` (auto-selects the principal line) or\n    ``\"Fe_Ka\"`` (forces a specific line).  Elements with Z \u2264 40\n    default to K\u03b1; heavier elements default to L\u03b1.\n\n    Args:\n        Element_Input: Element symbol optionally extended with an\n            underscore-separated line label (e.g. ``\"Au_La\"``).\n\n    Returns:\n        A tuple ``(Energy_Kev, Resolved_Tag)`` where *Resolved_Tag*\n        has the form ``\"Fe_Ka\"``.\n\n    Raises:\n        ValueError: If the element symbol is not recognised by the\n            ``exspy`` materials database or the requested emission\n            line is missing.\n    \"\"\"\n    # lazy import \u2014 hyperspy/exspy are heavy and slow to load\n    import exspy\n\n    if \"_\" in Element_Input:\n        elem_symbol, line_symbol = Element_Input.split(\"_\", 1)\n    else:\n        elem_symbol = Element_Input\n        line_symbol = None\n\n    try:\n        elem_obj = getattr(exspy.material.elements, elem_symbol)\n    except AttributeError:\n        raise ValueError(\n            f\"Element '{elem_symbol}' not found in the periodic table database.\"\n        )\n\n    if not line_symbol:\n        z = _ATOMIC_NUMBERS.get(elem_symbol, 20)\n        line_symbol = \"Ka\" if z &lt;= 40 else \"La\"\n\n    resolved_tag = f\"{elem_symbol}_{line_symbol}\"\n\n    xray_lines = None\n    if hasattr(elem_obj, \"Xray_lines\"):\n        xray_lines = elem_obj.Xray_lines\n    elif hasattr(elem_obj, \"Atomic_properties\") and hasattr(\n        elem_obj.Atomic_properties, \"Xray_lines\"\n    ):\n        xray_lines = elem_obj.Atomic_properties.Xray_lines\n\n    if xray_lines is None or line_symbol not in xray_lines:\n        raise ValueError(\n            f\"Emission line '{line_symbol}' not found for element {elem_symbol}.\"\n        )\n\n    line_data = xray_lines[line_symbol]\n    try:\n        energy_kev = float(line_data[\"energy (keV)\"])\n    except Exception:\n        try:\n            energy_kev = float(line_data.energy)\n        except Exception:\n            energy_kev = float(line_data)\n\n    return energy_kev, resolved_tag\n</code></pre>"},{"location":"api/xrf/#xrf.preprocessing.bcf_extractor.Bcf_Element_Extractor.Extract_And_Save","title":"<code>Extract_And_Save(Bcf_File_Path, Target_Elements, Output_Dir=Path('data', 'xrf', 'raw'))</code>","text":"<p>Run the full BCF \u2192 element TIFF extraction pipeline.</p> <p>Parameters:</p> Name Type Description Default <code>Bcf_File_Path</code> <code>Path_Like</code> <p>Path to a <code>.bcf</code> Bruker hypercube file.</p> required <code>Target_Elements</code> <code>List[str]</code> <p>List of element symbols (e.g. <code>[\"Fe\", \"Cu\", \"Pb\"]</code>) or symbol-plus-line strings (e.g. <code>[\"Au_La\"]</code>).</p> required <code>Output_Dir</code> <code>Path_Like</code> <p>Directory where element TIFFs will be written. Defaults to <code>data/xrf/raw/</code> relative to the current working directory.</p> <code>Path('data', 'xrf', 'raw')</code> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>If Bcf_File_Path does not exist.</p> Source code in <code>src\\xrf\\preprocessing\\bcf_extractor.py</code> <pre><code>def Extract_And_Save(\n    self,\n    Bcf_File_Path: Path_Like,\n    Target_Elements: List[str],\n    Output_Dir: Path_Like = Path(\"data\", \"xrf\", \"raw\"),\n) -&gt; None:\n    \"\"\"Run the full BCF \u2192 element TIFF extraction pipeline.\n\n    Args:\n        Bcf_File_Path: Path to a ``.bcf`` Bruker hypercube file.\n        Target_Elements: List of element symbols (e.g. ``[\"Fe\", \"Cu\", \"Pb\"]``)\n            or symbol-plus-line strings (e.g. ``[\"Au_La\"]``).\n        Output_Dir: Directory where element TIFFs will be written.\n            Defaults to ``data/xrf/raw/`` relative to the current\n            working directory.\n\n    Raises:\n        FileNotFoundError: If *Bcf_File_Path* does not exist.\n    \"\"\"\n    # lazy import \u2014 hyperspy is heavy\n    import hyperspy.api as hs\n\n    bcf_path = Path(Bcf_File_Path)\n    output_dir = Path(Output_Dir)\n    output_dir.mkdir(parents=True, exist_ok=True)\n\n    sample_name = bcf_path.stem\n\n    print(f\"\\n[Bcf_Extractor] Loading hypercube: {bcf_path.name}\")\n    signal = hs.load(\n        str(bcf_path),\n        cutoff_at_kV=self.cutoff_at_kv,\n        select_type=\"spectrum_image\",\n    )\n\n    num_elements = len(Target_Elements)\n    print(f\"[Bcf_Extractor] Output directory: {output_dir.resolve()}\")\n\n    for idx, element_input in enumerate(Target_Elements):\n        try:\n            center_energy, resolved_tag = self.Resolve_Emission_Line(element_input)\n        except Exception as exc:\n            print(f\"[Bcf_Extractor] [{idx+1}/{num_elements}] SKIPPING '{element_input}': {exc}\")\n            continue\n\n        print(\n            f\"[Bcf_Extractor] [{idx+1}/{num_elements}] Extracting {resolved_tag} \"\n            f\"(line at {center_energy:.3f} keV) \u2026\"\n        )\n\n        p_min = center_energy - (self.peak_width_kev / 2.0)\n        p_max = center_energy + (self.peak_width_kev / 2.0)\n        bg1_min = (center_energy - self.bg_offset_kev) - (self.bg_width_kev / 2.0)\n        bg1_max = (center_energy - self.bg_offset_kev) + (self.bg_width_kev / 2.0)\n        bg2_min = (center_energy + self.bg_offset_kev) - (self.bg_width_kev / 2.0)\n        bg2_max = (center_energy + self.bg_offset_kev) + (self.bg_width_kev / 2.0)\n\n        try:\n            gross_map = signal.isig[p_min:p_max].sum(axis=-1).data.astype(np.float64)\n            bg1_map = signal.isig[bg1_min:bg1_max].sum(axis=-1).data.astype(np.float64)\n            bg2_map = signal.isig[bg2_min:bg2_max].sum(axis=-1).data.astype(np.float64)\n        except (IndexError, ValueError):\n            print(\n                f\"[Bcf_Extractor]   \u2514\u2500 SKIPPING: line at {center_energy:.2f} keV \"\n                f\"is outside detector range (max {self.cutoff_at_kv} kV).\"\n            )\n            continue\n\n        width_ratio = self.peak_width_kev / self.bg_width_kev\n        estimated_bg = ((bg1_map + bg2_map) / 2.0) * width_ratio\n\n        net_map = gross_map - estimated_bg\n        np.clip(net_map, 0.0, None, out=net_map)\n\n        final_map = net_map.astype(np.float32)\n\n        output_file = output_dir / f\"{sample_name}_{resolved_tag}_raw.tiff\"\n        tifffile.imwrite(\n            str(output_file),\n            final_map,\n            metadata={\"element\": resolved_tag, \"energy_kev\": center_energy},\n        )\n        print(f\"[Bcf_Extractor]   \u2514\u2500 Saved: {output_file.name}\")\n\n    print(\"[Bcf_Extractor] Pipeline complete.\\n\")\n</code></pre>"},{"location":"api/xrf/#transforms","title":"Transforms","text":""},{"location":"api/xrf/#xrf.transforms.coda","title":"<code>xrf.transforms.coda</code>","text":"<p>Module of transformations for Compositional Data Analysis (CoDa). Implements the Centered Log-Ratio (CLR) transformation to overcome the closure effect.</p> <p>Classes:</p> Name Description <code>Clr_Transformer</code> <p>Class to apply proportional and CLR transformations to XRF data.</p>"},{"location":"api/xrf/#xrf.transforms.coda-classes","title":"Classes","text":""},{"location":"api/xrf/#xrf.transforms.coda.Clr_Transformer","title":"<code>Clr_Transformer</code>","text":"<p>Class to apply proportional and CLR transformations to XRF data.</p> <p>Methods:</p> Name Description <code>Apply_Clr_Transform</code> <p>Converts raw intensities to proportions, applies zero replacement, and</p>"},{"location":"api/xrf/#xrf.transforms.coda.Clr_Transformer-functions","title":"Methods:","text":""},{"location":"api/xrf/#xrf.transforms.coda.Clr_Transformer.Apply_Clr_Transform","title":"<code>Apply_Clr_Transform(Valid_Pixels, Delta=0.0001)</code>  <code>staticmethod</code>","text":"<p>Converts raw intensities to proportions, applies zero replacement, and projects to Euclidean space via the CLR transformation.</p> <p>Parameters:</p> Name Type Description Default <code>Valid_Pixels</code> <code>ndarray</code> <p>Array (N_valid, n) with raw intensities &gt;= 0.</p> required <code>Delta</code> <code>float</code> <p>Small constant for zero replacement. Defaults to 1e-4.</p> <code>0.0001</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>np.ndarray: Array (N_valid, n) transformed to real space (R^n).</p> Source code in <code>src\\xrf\\transforms\\coda.py</code> <pre><code>@staticmethod\ndef Apply_Clr_Transform(Valid_Pixels: np.ndarray, Delta: float = 1e-4) -&gt; np.ndarray:\n    \"\"\"\n    Converts raw intensities to proportions, applies zero replacement, and\n    projects to Euclidean space via the CLR transformation.\n\n    Args:\n        Valid_Pixels (np.ndarray): Array (N_valid, n) with raw intensities &gt;= 0.\n        Delta (float, optional): Small constant for zero replacement.\n            Defaults to 1e-4.\n\n    Returns:\n        np.ndarray: Array (N_valid, n) transformed to real space (R^n).\n    \"\"\"\n    # 1. Normalization (closure) to proportions (sum = 1)\n    Row_Sums = np.sum(Valid_Pixels, axis=1, keepdims=True)\n    Proportions = Valid_Pixels / Row_Sums\n\n    # 2. Simple multiplicative zero replacement\n    Proportions[Proportions == 0.0] = Delta\n\n    # Renormalization after imputing zeros\n    Proportions = Proportions / np.sum(Proportions, axis=1, keepdims=True)\n\n    # 3. Centered Log-Ratio (CLR) transformation\n    # log( x_i / geometric_mean(x) )\n    Log_Proportions = np.log(Proportions)\n    Geometric_Mean = np.mean(Log_Proportions, axis=1, keepdims=True)\n    Clr_Data = Log_Proportions - Geometric_Mean\n\n    return Clr_Data\n</code></pre>"},{"location":"api/xrf/#segmentation","title":"Segmentation","text":""},{"location":"api/xrf/#xrf.segmentation.xrf_gmm","title":"<code>xrf.segmentation.xrf_gmm</code>","text":"<p>Module for dimensionality reduction and probabilistic clustering of XRF data.</p> <p>Classes:</p> Name Description <code>Xrf_Gmm_Segmenter</code> <p>Class responsible for reducing dimensionality (PCA) and grouping behaviors (GMM).</p>"},{"location":"api/xrf/#xrf.segmentation.xrf_gmm-classes","title":"Classes","text":""},{"location":"api/xrf/#xrf.segmentation.xrf_gmm.Xrf_Gmm_Segmenter","title":"<code>Xrf_Gmm_Segmenter</code>","text":"<p>Class responsible for reducing dimensionality (PCA) and grouping behaviors (GMM).</p> <p>Methods:</p> Name Description <code>Fit_Predict</code> <p>Applies PCA to retain variance and then fits a GMM.</p> <code>Compute_Bic_Curve</code> <p>Computes the Bayesian Information Criterion (BIC) for a range of K.</p>"},{"location":"api/xrf/#xrf.segmentation.xrf_gmm.Xrf_Gmm_Segmenter-functions","title":"Methods:","text":""},{"location":"api/xrf/#xrf.segmentation.xrf_gmm.Xrf_Gmm_Segmenter.Fit_Predict","title":"<code>Fit_Predict(Clr_Data, Num_Components, Variance_Ratio=0.95, Covariance_Type='full')</code>  <code>staticmethod</code>","text":"<p>Applies PCA to retain variance and then fits a GMM.</p> <p>Parameters:</p> Name Type Description Default <code>Clr_Data</code> <code>ndarray</code> <p>Data in log-ratio space (N_valid, n).</p> required <code>Num_Components</code> <code>int</code> <p>Number of classes K to search for.</p> required <code>Variance_Ratio</code> <code>float</code> <p>Variance to retain. Defaults to 0.95.</p> <code>0.95</code> <code>Covariance_Type</code> <code>str</code> <p>Covariance type. Defaults to \"full\".</p> <code>'full'</code> <p>Returns:</p> Name Type Description <code>Tuple</code> <code>Tuple[ndarray, ndarray, PCA, GaussianMixture]</code> <ul> <li>Discrete labels (N_valid,)</li> <li>Posterior probabilities (N_valid, K)</li> <li>Trained PCA object</li> <li>Trained GMM object</li> </ul> Source code in <code>src\\xrf\\segmentation\\xrf_gmm.py</code> <pre><code>@staticmethod\ndef Fit_Predict(\n    Clr_Data: np.ndarray,\n    Num_Components: int,\n    Variance_Ratio: float = 0.95,\n    Covariance_Type: str = \"full\",\n) -&gt; Tuple[np.ndarray, np.ndarray, PCA, GaussianMixture]:\n    \"\"\"\n    Applies PCA to retain variance and then fits a GMM.\n\n    Args:\n        Clr_Data (np.ndarray): Data in log-ratio space (N_valid, n).\n        Num_Components (int): Number of classes K to search for.\n        Variance_Ratio (float, optional): Variance to retain. Defaults to 0.95.\n        Covariance_Type (str, optional): Covariance type. Defaults to \"full\".\n\n    Returns:\n        Tuple:\n            - Discrete labels (N_valid,)\n            - Posterior probabilities (N_valid, K)\n            - Trained PCA object\n            - Trained GMM object\n    \"\"\"\n    # PCA reduction\n    Pca_Model = PCA(n_components=Variance_Ratio, svd_solver=\"full\")\n    Z_Data = Pca_Model.fit_transform(Clr_Data)\n\n    # GMM clustering\n    Gmm_Model = GaussianMixture(\n        n_components=Num_Components, covariance_type=Covariance_Type, random_state=42\n    )\n    Gmm_Model.fit(Z_Data)\n\n    Labels = Gmm_Model.predict(Z_Data)\n    Probabilities = Gmm_Model.predict_proba(Z_Data)\n\n    return Labels, Probabilities, Pca_Model, Gmm_Model\n</code></pre>"},{"location":"api/xrf/#xrf.segmentation.xrf_gmm.Xrf_Gmm_Segmenter.Compute_Bic_Curve","title":"<code>Compute_Bic_Curve(Clr_Data, K_Range, Variance_Ratio=0.95)</code>  <code>staticmethod</code>","text":"<p>Computes the Bayesian Information Criterion (BIC) for a range of K.</p> <p>Parameters:</p> Name Type Description Default <code>Clr_Data</code> <code>ndarray</code> <p>Data in log-ratio space.</p> required <code>K_Range</code> <code>List[int]</code> <p>List of K values to evaluate.</p> required <code>Variance_Ratio</code> <code>float</code> <p>Variance to retain in prior PCA.</p> <code>0.95</code> <p>Returns:</p> Type Description <code>Dict[int, float]</code> <p>Dict[int, float]: Dictionary mapping K to its BIC score.</p> Source code in <code>src\\xrf\\segmentation\\xrf_gmm.py</code> <pre><code>@staticmethod\ndef Compute_Bic_Curve(\n    Clr_Data: np.ndarray, K_Range: List[int], Variance_Ratio: float = 0.95\n) -&gt; Dict[int, float]:\n    \"\"\"\n    Computes the Bayesian Information Criterion (BIC) for a range of K.\n\n    Args:\n        Clr_Data (np.ndarray): Data in log-ratio space.\n        K_Range (List[int]): List of K values to evaluate.\n        Variance_Ratio (float): Variance to retain in prior PCA.\n\n    Returns:\n        Dict[int, float]: Dictionary mapping K to its BIC score.\n    \"\"\"\n    Pca_Model = PCA(n_components=Variance_Ratio, svd_solver=\"full\")\n    Z_Data = Pca_Model.fit_transform(Clr_Data)\n\n    Bic_Scores = {}\n    for K in K_Range:\n        Model = GaussianMixture(n_components=K, random_state=42).fit(Z_Data)\n        Bic_Scores[K] = Model.bic(Z_Data)\n\n    return Bic_Scores\n</code></pre>"},{"location":"api/xrf/#spatial","title":"Spatial","text":""},{"location":"api/xrf/#xrf.spatial.spatial_analyzer","title":"<code>xrf.spatial.spatial_analyzer</code>","text":"<p>Module for topological reconstruction and extraction of spatial metrics (CCA).</p> <p>Classes:</p> Name Description <code>Spatial_Analyzer</code> <p>Class to map labels to 2D and analyze their morphology.</p>"},{"location":"api/xrf/#xrf.spatial.spatial_analyzer-classes","title":"Classes","text":""},{"location":"api/xrf/#xrf.spatial.spatial_analyzer.Spatial_Analyzer","title":"<code>Spatial_Analyzer</code>","text":"<p>Class to map labels to 2D and analyze their morphology.</p> <p>Methods:</p> Name Description <code>Reconstruct_Class_Map</code> <p>Maps the flattened label vector back to the 2D grid.</p> <code>Extract_Spatial_Descriptors</code> <p>Computes connected-component descriptors for a class.</p>"},{"location":"api/xrf/#xrf.spatial.spatial_analyzer.Spatial_Analyzer-functions","title":"Methods:","text":""},{"location":"api/xrf/#xrf.spatial.spatial_analyzer.Spatial_Analyzer.Reconstruct_Class_Map","title":"<code>Reconstruct_Class_Map(Labels, Mask, Fill_Value=-1)</code>  <code>staticmethod</code>","text":"<p>Maps the flattened label vector back to the 2D grid.</p> <p>Parameters:</p> Name Type Description Default <code>Labels</code> <code>ndarray</code> <p>1D labels (N_valid,).</p> required <code>Mask</code> <code>ndarray</code> <p>2D binary mask (M, N).</p> required <code>Fill_Value</code> <code>int</code> <p>Background (noise) value. Defaults to -1.</p> <code>-1</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>np.ndarray: 2D class map of shape (M, N).</p> Source code in <code>src\\xrf\\spatial\\spatial_analyzer.py</code> <pre><code>@staticmethod\ndef Reconstruct_Class_Map(\n    Labels: np.ndarray, Mask: np.ndarray, Fill_Value: int = -1\n) -&gt; np.ndarray:\n    \"\"\"\n    Maps the flattened label vector back to the 2D grid.\n\n    Args:\n        Labels (np.ndarray): 1D labels (N_valid,).\n        Mask (np.ndarray): 2D binary mask (M, N).\n        Fill_Value (int, optional): Background (noise) value. Defaults to -1.\n\n    Returns:\n        np.ndarray: 2D class map of shape (M, N).\n    \"\"\"\n    Class_Map = np.full(Mask.shape, Fill_Value, dtype=np.int32)\n    Class_Map[Mask] = Labels\n    return Class_Map\n</code></pre>"},{"location":"api/xrf/#xrf.spatial.spatial_analyzer.Spatial_Analyzer.Extract_Spatial_Descriptors","title":"<code>Extract_Spatial_Descriptors(Class_Map, Target_Class, Min_Size=10)</code>  <code>staticmethod</code>","text":"<p>Computes connected-component descriptors for a class.</p> <p>Parameters:</p> Name Type Description Default <code>Class_Map</code> <code>ndarray</code> <p>2D class map.</p> required <code>Target_Class</code> <code>int</code> <p>Identifier of the class to analyze.</p> required <code>Min_Size</code> <code>int</code> <p>Minimum size to consider a region.</p> <code>10</code> <p>Returns:</p> Type Description <code>Dict[str, float]</code> <p>Dict[str, float]: Dictionary with Num_Regions and Average_Size.</p> Source code in <code>src\\xrf\\spatial\\spatial_analyzer.py</code> <pre><code>@staticmethod\ndef Extract_Spatial_Descriptors(\n    Class_Map: np.ndarray, Target_Class: int, Min_Size: int = 10\n) -&gt; Dict[str, float]:\n    \"\"\"\n    Computes connected-component descriptors for a class.\n\n    Args:\n        Class_Map (np.ndarray): 2D class map.\n        Target_Class (int): Identifier of the class to analyze.\n        Min_Size (int): Minimum size to consider a region.\n\n    Returns:\n        Dict[str, float]: Dictionary with Num_Regions and Average_Size.\n    \"\"\"\n    Binary_Mask = (Class_Map == Target_Class).astype(np.uint8)\n\n    # 8-connectivity (3x3 matrix of ones)\n    Structure = np.ones((3, 3), dtype=np.uint8)\n    Labeled_Array, Num_Features = ndimage.label(Binary_Mask, structure=Structure)\n\n    Valid_Regions = 0\n    Total_Area = 0\n\n    # Filter small regions\n    for Region_Id in range(1, Num_Features + 1):\n        Area = np.sum(Labeled_Array == Region_Id)\n        if Area &gt;= Min_Size:\n            Valid_Regions += 1\n            Total_Area += Area\n\n    Avg_Size = Total_Area / Valid_Regions if Valid_Regions &gt; 0 else 0.0\n\n    return {\"Num_Regiones\": float(Valid_Regions), \"Tamano_Promedio\": float(Avg_Size)}\n</code></pre>"},{"location":"api/xrf/#signatures","title":"Signatures","text":""},{"location":"api/xrf/#xrf.signatures.leaf_signature","title":"<code>xrf.signatures.leaf_signature</code>","text":"<p>Module to synthesize leaf composition and structure into the compact signature F_h.</p> <p>Classes:</p> Name Description <code>Leaf_Signature_Extractor</code> <p>Class to compile the descriptive signature of a book page.</p>"},{"location":"api/xrf/#xrf.signatures.leaf_signature-classes","title":"Classes","text":""},{"location":"api/xrf/#xrf.signatures.leaf_signature.Leaf_Signature_Extractor","title":"<code>Leaf_Signature_Extractor</code>","text":"<p>Class to compile the descriptive signature of a book page.</p> <p>Methods:</p> Name Description <code>Compute_Abundances</code> <p>Computes the area fraction A_k occupied by each class.</p> <code>Compute_Weighted_Book_Signature</code> <p>Computes the global average model of the document by applying weights.</p>"},{"location":"api/xrf/#xrf.signatures.leaf_signature.Leaf_Signature_Extractor-functions","title":"Methods:","text":""},{"location":"api/xrf/#xrf.signatures.leaf_signature.Leaf_Signature_Extractor.Compute_Abundances","title":"<code>Compute_Abundances(Labels, Num_Classes)</code>  <code>staticmethod</code>","text":"<p>Computes the area fraction A_k occupied by each class.</p> <p>Parameters:</p> Name Type Description Default <code>Labels</code> <code>ndarray</code> <p>1D assignment vector.</p> required <code>Num_Classes</code> <code>int</code> <p>Total number K of GMM classes.</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>np.ndarray: Abundance vector (K,) that sums to 1.0.</p> Source code in <code>src\\xrf\\signatures\\leaf_signature.py</code> <pre><code>@staticmethod\ndef Compute_Abundances(Labels: np.ndarray, Num_Classes: int) -&gt; np.ndarray:\n    \"\"\"\n    Computes the area fraction A_k occupied by each class.\n\n    Args:\n        Labels (np.ndarray): 1D assignment vector.\n        Num_Classes (int): Total number K of GMM classes.\n\n    Returns:\n        np.ndarray: Abundance vector (K,) that sums to 1.0.\n    \"\"\"\n    Total_Valid = len(Labels)\n    if Total_Valid == 0:\n        return np.zeros(Num_Classes)\n\n    Abundances = np.zeros(Num_Classes)\n    for K in range(Num_Classes):\n        Abundances[K] = np.sum(Labels == K) / Total_Valid\n\n    return Abundances\n</code></pre>"},{"location":"api/xrf/#xrf.signatures.leaf_signature.Leaf_Signature_Extractor.Compute_Weighted_Book_Signature","title":"<code>Compute_Weighted_Book_Signature(Signatures, Weights)</code>  <code>staticmethod</code>","text":"<p>Computes the global average model of the document by applying weights.</p> <p>Parameters:</p> Name Type Description Default <code>Signatures</code> <code>ndarray</code> <p>Signature matrix (H_pages, D_features).</p> required <code>Weights</code> <code>ndarray</code> <p>Weight vector w_h (H_pages,).</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>np.ndarray: Averaged global signature F_bar (D_features,).</p> Source code in <code>src\\xrf\\signatures\\leaf_signature.py</code> <pre><code>@staticmethod\ndef Compute_Weighted_Book_Signature(Signatures: np.ndarray, Weights: np.ndarray) -&gt; np.ndarray:\n    \"\"\"\n    Computes the global average model of the document by applying weights.\n\n    Args:\n        Signatures (np.ndarray): Signature matrix (H_pages, D_features).\n        Weights (np.ndarray): Weight vector w_h (H_pages,).\n\n    Returns:\n        np.ndarray: Averaged global signature F_bar (D_features,).\n    \"\"\"\n    Normalized_Weights = Weights / np.sum(Weights)\n    return np.average(Signatures, axis=0, weights=Normalized_Weights)\n</code></pre>"},{"location":"api/xrf/#comparison","title":"Comparison","text":""},{"location":"api/xrf/#xrf.comparison.category_registry","title":"<code>xrf.comparison.category_registry</code>","text":"<p>Module owning the structural page-category vocabulary and the read/write access to the <code>structural_*</code> fields on page metadata.</p> <p>Classes:</p> Name Description <code>Category_Registry</code> <p>Static-method utility for structural page-category tags.</p>"},{"location":"api/xrf/#xrf.comparison.category_registry-classes","title":"Classes","text":""},{"location":"api/xrf/#xrf.comparison.category_registry.Category_Registry","title":"<code>Category_Registry</code>","text":"<p>Static-method utility for structural page-category tags.</p> <p>Mirrors the interface style of other XRF classes (Xrf_Loader, Clr_Transformer): stateless, static methods, no persisted instance.</p> <p>Methods:</p> Name Description <code>Validate_Category_Tag</code> <p>Validate a category tag against the allowed vocabulary.</p> <code>Write_Page_Category</code> <p>Validate Category, then persist it into the page metadata.</p> <code>Load_Page_Category</code> <p>Return the structural_category field, or None if not yet tagged.</p> <code>List_Tagged_Pages</code> <p>Scan page_*_meta.json files and group page ids by category.</p>"},{"location":"api/xrf/#xrf.comparison.category_registry.Category_Registry-functions","title":"Methods:","text":""},{"location":"api/xrf/#xrf.comparison.category_registry.Category_Registry.Validate_Category_Tag","title":"<code>Validate_Category_Tag(Tag, Config)</code>  <code>staticmethod</code>","text":"<p>Validate a category tag against the allowed vocabulary.</p> <p>Parameters:</p> Name Type Description Default <code>Tag</code> <code>str</code> <p>Category label to validate.</p> required <code>Config</code> <code>Xrf_Comparison_Config</code> <p>Comparison configuration holding the allowed vocabulary.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If Tag is not in Config.Allowed_Categories.</p> Source code in <code>src\\xrf\\comparison\\category_registry.py</code> <pre><code>@staticmethod\ndef Validate_Category_Tag(Tag: str, Config: Xrf_Comparison_Config) -&gt; None:\n    \"\"\"Validate a category tag against the allowed vocabulary.\n\n    Args:\n        Tag: Category label to validate.\n        Config: Comparison configuration holding the allowed vocabulary.\n\n    Raises:\n        ValueError: If Tag is not in Config.Allowed_Categories.\n    \"\"\"\n    if Tag not in Config.Allowed_Categories:\n        raise ValueError(\n            f\"Category tag '{Tag}' is not in the allowed vocabulary \"\n            f\"{Config.Allowed_Categories}.\"\n        )\n</code></pre>"},{"location":"api/xrf/#xrf.comparison.category_registry.Category_Registry.Write_Page_Category","title":"<code>Write_Page_Category(Meta_Path, Category, Config, Tag_Source='manual', Secondary_Categories=None, Notes=None)</code>  <code>staticmethod</code>","text":"<p>Validate Category, then persist it into the page metadata.</p> <p>Parameters:</p> Name Type Description Default <code>Meta_Path</code> <code>Path_Like</code> <p>Path to the page's meta.json file.</p> required <code>Category</code> <code>str</code> <p>Primary structural category tag.</p> required <code>Config</code> <code>Xrf_Comparison_Config</code> <p>Comparison configuration holding the allowed vocabulary.</p> required <code>Tag_Source</code> <code>str</code> <p>Either \"manual\" or \"heuristic\". Defaults to \"manual\".</p> <code>'manual'</code> <code>Secondary_Categories</code> <code>Optional[List[str]]</code> <p>Optional list of additional category tags for pages that do not fit one bucket cleanly.</p> <code>None</code> <code>Notes</code> <code>Optional[str]</code> <p>Optional free-text note, read by humans only.</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If Category is not in Config.Allowed_Categories.</p> <code>FileNotFoundError</code> <p>If Meta_Path does not exist.</p> Source code in <code>src\\xrf\\comparison\\category_registry.py</code> <pre><code>@staticmethod\ndef Write_Page_Category(\n    Meta_Path: Path_Like,\n    Category: str,\n    Config: Xrf_Comparison_Config,\n    Tag_Source: str = \"manual\",\n    Secondary_Categories: Optional[List[str]] = None,\n    Notes: Optional[str] = None,\n) -&gt; None:\n    \"\"\"Validate Category, then persist it into the page metadata.\n\n    Args:\n        Meta_Path: Path to the page's meta.json file.\n        Category: Primary structural category tag.\n        Config: Comparison configuration holding the allowed vocabulary.\n        Tag_Source: Either \"manual\" or \"heuristic\". Defaults to \"manual\".\n        Secondary_Categories: Optional list of additional category tags\n            for pages that do not fit one bucket cleanly.\n        Notes: Optional free-text note, read by humans only.\n\n    Raises:\n        ValueError: If Category is not in Config.Allowed_Categories.\n        FileNotFoundError: If Meta_Path does not exist.\n    \"\"\"\n    Category_Registry.Validate_Category_Tag(Category, Config)\n\n    Fields = {\n        \"structural_category\": Category,\n        \"structural_category_source\": Tag_Source,\n        \"structural_category_secondary\": Secondary_Categories or [],\n        \"structural_category_notes\": Notes or \"\",\n    }\n    Update_Page_Metadata(Meta_Path, **Fields)\n\n    Page_Id = Path(Meta_Path).stem.replace(\"_meta\", \"\")\n    print(f\"[Category_Registry] tagged {Page_Id} as {Category} ({Tag_Source})\")\n</code></pre>"},{"location":"api/xrf/#xrf.comparison.category_registry.Category_Registry.Load_Page_Category","title":"<code>Load_Page_Category(Meta_Path)</code>  <code>staticmethod</code>","text":"<p>Return the structural_category field, or None if not yet tagged.</p> <p>Parameters:</p> Name Type Description Default <code>Meta_Path</code> <code>Path_Like</code> <p>Path to the page's meta.json file.</p> required <p>Returns:</p> Type Description <code>Optional[str]</code> <p>The primary structural category tag, or None if the page has</p> <code>Optional[str]</code> <p>not been tagged yet.</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>If Meta_Path does not exist.</p> Source code in <code>src\\xrf\\comparison\\category_registry.py</code> <pre><code>@staticmethod\ndef Load_Page_Category(Meta_Path: Path_Like) -&gt; Optional[str]:\n    \"\"\"Return the structural_category field, or None if not yet tagged.\n\n    Args:\n        Meta_Path: Path to the page's meta.json file.\n\n    Returns:\n        The primary structural category tag, or None if the page has\n        not been tagged yet.\n\n    Raises:\n        FileNotFoundError: If Meta_Path does not exist.\n    \"\"\"\n    Meta_Path = Path(Meta_Path)\n    if not Meta_Path.exists():\n        raise FileNotFoundError(f\"Meta_Path {Meta_Path} does not exist.\")\n\n    with open(Meta_Path, \"r\", encoding=\"utf-8\") as Meta_File:\n        Metadata = json.load(Meta_File)\n\n    return Metadata.get(\"structural_category\")\n</code></pre>"},{"location":"api/xrf/#xrf.comparison.category_registry.Category_Registry.List_Tagged_Pages","title":"<code>List_Tagged_Pages(Output_Dir, Config=None)</code>  <code>staticmethod</code>","text":"<p>Scan page_*_meta.json files and group page ids by category.</p> <p>Parameters:</p> Name Type Description Default <code>Output_Dir</code> <code>Path_Like</code> <p>Directory containing page_NNN_meta.json files (typically data/xrf/output/processed/).</p> required <code>Config</code> <code>Optional[Xrf_Comparison_Config]</code> <p>Comparison configuration holding the allowed vocabulary, used only to flag unexpected tags with a warning. Defaults to Xrf_Comparison_Config() if omitted.</p> <code>None</code> <p>Returns:</p> Type Description <code>Dict[str, List[str]]</code> <p>Dict[str, List[str]]: {category: [page_id, ...]}. Pages without</p> <code>Dict[str, List[str]]</code> <p>a structural_category are grouped under \"untagged\".</p> Source code in <code>src\\xrf\\comparison\\category_registry.py</code> <pre><code>@staticmethod\ndef List_Tagged_Pages(\n    Output_Dir: Path_Like, Config: Optional[Xrf_Comparison_Config] = None\n) -&gt; Dict[str, List[str]]:\n    \"\"\"Scan page_*_meta.json files and group page ids by category.\n\n    Args:\n        Output_Dir: Directory containing page_NNN_meta.json files\n            (typically data/xrf/output/processed/).\n        Config: Comparison configuration holding the allowed\n            vocabulary, used only to flag unexpected tags with a\n            warning. Defaults to Xrf_Comparison_Config() if omitted.\n\n    Returns:\n        Dict[str, List[str]]: {category: [page_id, ...]}. Pages without\n        a structural_category are grouped under \"untagged\".\n    \"\"\"\n    if Config is None:\n        Config = Xrf_Comparison_Config()\n\n    Output_Dir = Path(Output_Dir)\n    Grouped: Dict[str, List[str]] = {}\n\n    for Meta_Path in sorted(Output_Dir.glob(\"page_*_meta.json\")):\n        Page_Id = Meta_Path.stem.replace(\"_meta\", \"\")\n        Category = Category_Registry.Load_Page_Category(Meta_Path)\n\n        if Category is None:\n            Category = \"untagged\"\n        elif Category not in Config.Allowed_Categories:\n            _Logger.warning(\n                \"[Category_Registry] page %s has unexpected category \"\n                \"'%s' not in Allowed_Categories %s\",\n                Page_Id,\n                Category,\n                Config.Allowed_Categories,\n            )\n\n        Grouped.setdefault(Category, []).append(Page_Id)\n\n    return Grouped\n</code></pre>"},{"location":"api/xrf/#xrf.comparison.category_registry-functions","title":"Functions:","text":""},{"location":"api/xrf/#xrf.comparison.category_signatures","title":"<code>xrf.comparison.category_signatures</code>","text":"<p>Module aggregating per-page leaf signatures into per-category signatures.</p> <p>Classes:</p> Name Description <code>Category_Signature_Aggregator</code> <p>Aggregates per-page leaf signatures into per-category signatures.</p>"},{"location":"api/xrf/#xrf.comparison.category_signatures-classes","title":"Classes","text":""},{"location":"api/xrf/#xrf.comparison.category_signatures.Category_Signature_Aggregator","title":"<code>Category_Signature_Aggregator</code>","text":"<p>Aggregates per-page leaf signatures into per-category signatures.</p> <p>Reuses the same weighted-average logic as Leaf_Signature_Extractor.Compute_Weighted_Book_Signature, applied per category group instead of over the whole book.</p> <p>Methods:</p> Name Description <code>Aggregate_By_Category</code> <p>Compute the mean abundance vector for each structural category.</p> <code>Compute_Category_Spread</code> <p>Compute the per-class MAD vector for each structural category.</p>"},{"location":"api/xrf/#xrf.comparison.category_signatures.Category_Signature_Aggregator-functions","title":"Methods:","text":""},{"location":"api/xrf/#xrf.comparison.category_signatures.Category_Signature_Aggregator.Aggregate_By_Category","title":"<code>Aggregate_By_Category(Page_Signatures, Page_Categories)</code>  <code>staticmethod</code>","text":"<p>Compute the mean abundance vector for each structural category.</p> <p>Parameters:</p> Name Type Description Default <code>Page_Signatures</code> <code>Dict[str, ndarray]</code> <p>Mapping of page id to its leaf signature (abundance vector) as produced by Leaf_Signature_Extractor.Compute_Abundances.</p> required <code>Page_Categories</code> <code>Dict[str, str]</code> <p>Mapping of page id to its structural category tag.</p> required <p>Returns:</p> Type Description <code>Dict[str, ndarray]</code> <p>Dict[str, np.ndarray]: {category: mean_abundance_vector}.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If a page in Page_Categories has no matching entry in Page_Signatures.</p> Source code in <code>src\\xrf\\comparison\\category_signatures.py</code> <pre><code>@staticmethod\ndef Aggregate_By_Category(\n    Page_Signatures: Dict[str, np.ndarray],\n    Page_Categories: Dict[str, str],\n) -&gt; Dict[str, np.ndarray]:\n    \"\"\"Compute the mean abundance vector for each structural category.\n\n    Args:\n        Page_Signatures: Mapping of page id to its leaf signature\n            (abundance vector) as produced by\n            Leaf_Signature_Extractor.Compute_Abundances.\n        Page_Categories: Mapping of page id to its structural category\n            tag.\n\n    Returns:\n        Dict[str, np.ndarray]: {category: mean_abundance_vector}.\n\n    Raises:\n        ValueError: If a page in Page_Categories has no matching entry\n            in Page_Signatures.\n    \"\"\"\n    Grouped = Category_Signature_Aggregator._Group_Signatures_By_Category(\n        Page_Signatures, Page_Categories\n    )\n\n    Category_Means: Dict[str, np.ndarray] = {}\n    for Category, Signatures in Grouped.items():\n        Signature_Matrix = np.stack(Signatures, axis=0)\n        Uniform_Weights = np.ones(Signature_Matrix.shape[0])\n        Category_Means[Category] = Leaf_Signature_Extractor.Compute_Weighted_Book_Signature(\n            Signature_Matrix, Uniform_Weights\n        )\n\n    return Category_Means\n</code></pre>"},{"location":"api/xrf/#xrf.comparison.category_signatures.Category_Signature_Aggregator.Compute_Category_Spread","title":"<code>Compute_Category_Spread(Page_Signatures, Page_Categories)</code>  <code>staticmethod</code>","text":"<p>Compute the per-class MAD vector for each structural category.</p> <p>The robust spread measure used both for plot error bars and for Rarity_Scorer.</p> <p>Parameters:</p> Name Type Description Default <code>Page_Signatures</code> <code>Dict[str, ndarray]</code> <p>Mapping of page id to its leaf signature (abundance vector).</p> required <code>Page_Categories</code> <code>Dict[str, str]</code> <p>Mapping of page id to its structural category tag.</p> required <p>Returns:</p> Type Description <code>Dict[str, ndarray]</code> <p>Dict[str, np.ndarray]: {category: per_class_mad_vector}.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If a page in Page_Categories has no matching entry in Page_Signatures.</p> Source code in <code>src\\xrf\\comparison\\category_signatures.py</code> <pre><code>@staticmethod\ndef Compute_Category_Spread(\n    Page_Signatures: Dict[str, np.ndarray],\n    Page_Categories: Dict[str, str],\n) -&gt; Dict[str, np.ndarray]:\n    \"\"\"Compute the per-class MAD vector for each structural category.\n\n    The robust spread measure used both for plot error bars and for\n    Rarity_Scorer.\n\n    Args:\n        Page_Signatures: Mapping of page id to its leaf signature\n            (abundance vector).\n        Page_Categories: Mapping of page id to its structural category\n            tag.\n\n    Returns:\n        Dict[str, np.ndarray]: {category: per_class_mad_vector}.\n\n    Raises:\n        ValueError: If a page in Page_Categories has no matching entry\n            in Page_Signatures.\n    \"\"\"\n    Grouped = Category_Signature_Aggregator._Group_Signatures_By_Category(\n        Page_Signatures, Page_Categories\n    )\n\n    Category_Spread: Dict[str, np.ndarray] = {}\n    for Category, Signatures in Grouped.items():\n        Signature_Matrix = np.stack(Signatures, axis=0)\n        Median = np.median(Signature_Matrix, axis=0)\n        Category_Spread[Category] = np.median(\n            np.abs(Signature_Matrix - Median), axis=0\n        )\n\n    return Category_Spread\n</code></pre>"},{"location":"api/xrf/#xrf.comparison.rarity_scoring","title":"<code>xrf.comparison.rarity_scoring</code>","text":"<p>Module flagging pages whose composition deviates unusually from their structural category, as a ranking/triage heuristic for human review.</p> <p>Classes:</p> Name Description <code>Rarity_Scorer</code> <p>Flags pages whose composition deviates unusually from their category.</p>"},{"location":"api/xrf/#xrf.comparison.rarity_scoring-classes","title":"Classes","text":""},{"location":"api/xrf/#xrf.comparison.rarity_scoring.Rarity_Scorer","title":"<code>Rarity_Scorer</code>","text":"<p>Flags pages whose composition deviates unusually from their category.</p> <p>This is a ranking/triage heuristic, not a statistical test \u2014 small-N category groups don't support real significance claims. Docstrings on the public methods repeat this caveat explicitly so it isn't lost downstream.</p> <p>Methods:</p> Name Description <code>Compute_Robust_Deviation</code> <p>Compute the per-class robust z-score of a page against its category.</p> <code>Rank_Pages_By_Rarity</code> <p>Rank pages by their maximum absolute deviation from their category.</p>"},{"location":"api/xrf/#xrf.comparison.rarity_scoring.Rarity_Scorer-functions","title":"Methods:","text":""},{"location":"api/xrf/#xrf.comparison.rarity_scoring.Rarity_Scorer.Compute_Robust_Deviation","title":"<code>Compute_Robust_Deviation(Page_Signature, Category_Median, Category_Mad)</code>  <code>staticmethod</code>","text":"<p>Compute the per-class robust z-score of a page against its category.</p> <p>Formula: 0.6745 * (x - median) / MAD. This is a triage heuristic for human review, not a hypothesis test.</p> <p>Parameters:</p> Name Type Description Default <code>Page_Signature</code> <code>ndarray</code> <p>Leaf signature (abundance vector) of the page being scored.</p> required <code>Category_Median</code> <code>ndarray</code> <p>Per-class median abundance vector for the page's structural category.</p> required <code>Category_Mad</code> <code>ndarray</code> <p>Per-class MAD vector for the page's structural category.</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>np.ndarray: Per-class robust z-score vector.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If any Category_Mad entry is exactly 0 (which happens when every page in a tiny category has an identical value for that class) \u2014 guarded explicitly rather than silently producing inf/nan, matching this project's existing guard-clause error-handling style.</p> Source code in <code>src\\xrf\\comparison\\rarity_scoring.py</code> <pre><code>@staticmethod\ndef Compute_Robust_Deviation(\n    Page_Signature: np.ndarray,\n    Category_Median: np.ndarray,\n    Category_Mad: np.ndarray,\n) -&gt; np.ndarray:\n    \"\"\"Compute the per-class robust z-score of a page against its category.\n\n    Formula: 0.6745 * (x - median) / MAD. This is a triage heuristic\n    for human review, not a hypothesis test.\n\n    Args:\n        Page_Signature: Leaf signature (abundance vector) of the page\n            being scored.\n        Category_Median: Per-class median abundance vector for the\n            page's structural category.\n        Category_Mad: Per-class MAD vector for the page's structural\n            category.\n\n    Returns:\n        np.ndarray: Per-class robust z-score vector.\n\n    Raises:\n        RuntimeError: If any Category_Mad entry is exactly 0 (which\n            happens when every page in a tiny category has an identical\n            value for that class) \u2014 guarded explicitly rather than\n            silently producing inf/nan, matching this project's\n            existing guard-clause error-handling style.\n    \"\"\"\n    if np.any(Category_Mad == 0):\n        raise RuntimeError(\n            \"Category_Mad contains a zero entry; robust z-score is \"\n            \"undefined (would produce inf/nan). This happens when \"\n            \"every page in a tiny category shares an identical value \"\n            \"for that class.\"\n        )\n\n    return 0.6745 * (Page_Signature - Category_Median) / Category_Mad\n</code></pre>"},{"location":"api/xrf/#xrf.comparison.rarity_scoring.Rarity_Scorer.Rank_Pages_By_Rarity","title":"<code>Rank_Pages_By_Rarity(Page_Signatures, Page_Categories, Config)</code>  <code>staticmethod</code>","text":"<p>Rank pages by their maximum absolute deviation from their category.</p> <p>This is a ranking/triage heuristic, not a hypothesis test \u2014 see the class docstring.</p> <p>Parameters:</p> Name Type Description Default <code>Page_Signatures</code> <code>Dict[str, ndarray]</code> <p>Mapping of page id to its leaf signature (abundance vector).</p> required <code>Page_Categories</code> <code>Dict[str, str]</code> <p>Mapping of page id to its structural category tag.</p> required <code>Config</code> <code>Xrf_Comparison_Config</code> <p>Comparison configuration holding Rarity_Mad_Threshold.</p> required <p>Returns:</p> Type Description <code>List[Tuple[str, float, bool]]</code> <p>List[Tuple[str, float, bool]]: [(page_id, max_abs_deviation,</p> <code>List[Tuple[str, float, bool]]</code> <p>is_flagged), ...] sorted descending by deviation. is_flagged is</p> <code>List[Tuple[str, float, bool]]</code> <p>True when the deviation exceeds Config.Rarity_Mad_Threshold.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If a page in Page_Categories has no matching entry in Page_Signatures.</p> <code>RuntimeError</code> <p>If a category's MAD vector has a zero entry for any class present in a page's signature \u2014 see Compute_Robust_Deviation.</p> Source code in <code>src\\xrf\\comparison\\rarity_scoring.py</code> <pre><code>@staticmethod\ndef Rank_Pages_By_Rarity(\n    Page_Signatures: Dict[str, np.ndarray],\n    Page_Categories: Dict[str, str],\n    Config: Xrf_Comparison_Config,\n) -&gt; List[Tuple[str, float, bool]]:\n    \"\"\"Rank pages by their maximum absolute deviation from their category.\n\n    This is a ranking/triage heuristic, not a hypothesis test \u2014 see the\n    class docstring.\n\n    Args:\n        Page_Signatures: Mapping of page id to its leaf signature\n            (abundance vector).\n        Page_Categories: Mapping of page id to its structural category\n            tag.\n        Config: Comparison configuration holding Rarity_Mad_Threshold.\n\n    Returns:\n        List[Tuple[str, float, bool]]: [(page_id, max_abs_deviation,\n        is_flagged), ...] sorted descending by deviation. is_flagged is\n        True when the deviation exceeds Config.Rarity_Mad_Threshold.\n\n    Raises:\n        ValueError: If a page in Page_Categories has no matching entry\n            in Page_Signatures.\n        RuntimeError: If a category's MAD vector has a zero entry for\n            any class present in a page's signature \u2014 see\n            Compute_Robust_Deviation.\n    \"\"\"\n    Category_Medians = Rarity_Scorer._Compute_Category_Medians(\n        Page_Signatures, Page_Categories\n    )\n    Category_Mads = Category_Signature_Aggregator.Compute_Category_Spread(\n        Page_Signatures, Page_Categories\n    )\n\n    Rankings: List[Tuple[str, float, bool]] = []\n    for Page_Id, Category in Page_Categories.items():\n        if Page_Id not in Page_Signatures:\n            raise ValueError(\n                f\"Page '{Page_Id}' listed in Page_Categories has no \"\n                \"matching entry in Page_Signatures.\"\n            )\n\n        Deviation = Rarity_Scorer.Compute_Robust_Deviation(\n            Page_Signatures[Page_Id],\n            Category_Medians[Category],\n            Category_Mads[Category],\n        )\n        Max_Abs_Deviation = float(np.max(np.abs(Deviation)))\n        Is_Flagged = Max_Abs_Deviation &gt; Config.Rarity_Mad_Threshold\n        Rankings.append((Page_Id, Max_Abs_Deviation, Is_Flagged))\n\n    Rankings.sort(key=lambda Entry: Entry[1], reverse=True)\n    return Rankings\n</code></pre>"},{"location":"api/xrf/#xrf.comparison.spatial_comparison","title":"<code>xrf.comparison.spatial_comparison</code>","text":"<p>Module aggregating Spatial_Analyzer region descriptors by structural category.</p> <p>Classes:</p> Name Description <code>Category_Spatial_Comparator</code> <p>Aggregates Spatial_Analyzer region descriptors by category.</p>"},{"location":"api/xrf/#xrf.comparison.spatial_comparison-classes","title":"Classes","text":""},{"location":"api/xrf/#xrf.comparison.spatial_comparison.Category_Spatial_Comparator","title":"<code>Category_Spatial_Comparator</code>","text":"<p>Aggregates Spatial_Analyzer region descriptors by category.</p> <p>Methods:</p> Name Description <code>Aggregate_Region_Stats</code> <p>Aggregate per-page spatial descriptors into per-category stats.</p>"},{"location":"api/xrf/#xrf.comparison.spatial_comparison.Category_Spatial_Comparator-functions","title":"Methods:","text":""},{"location":"api/xrf/#xrf.comparison.spatial_comparison.Category_Spatial_Comparator.Aggregate_Region_Stats","title":"<code>Aggregate_Region_Stats(Page_Spatial_Descriptors, Page_Categories)</code>  <code>staticmethod</code>","text":"<p>Aggregate per-page spatial descriptors into per-category stats.</p> <p>Groups each page's Spatial_Analyzer.Extract_Spatial_Descriptors() output (keyed by compositional class id, with the Spanish keys \"Num_Regiones\" and \"Tamano_Promedio\") by its structural category, and averages them per class.</p> <p>Parameters:</p> Name Type Description Default <code>Page_Spatial_Descriptors</code> <code>Dict[str, dict]</code> <p>Mapping of page id to a dict of {class_k: {\"Num_Regiones\": float, \"Tamano_Promedio\": float}}, matching Spatial_Analyzer.Extract_Spatial_Descriptors's return schema.</p> required <code>Page_Categories</code> <code>Dict[str, str]</code> <p>Mapping of page id to its structural category tag.</p> required <p>Returns:</p> Type Description <code>Dict[str, dict]</code> <p>Dict[str, dict]: {category: {class_k: {mean_region_count,</p> <code>Dict[str, dict]</code> <p>mean_region_size}}}.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If a page in Page_Categories has no matching entry in Page_Spatial_Descriptors.</p> Source code in <code>src\\xrf\\comparison\\spatial_comparison.py</code> <pre><code>@staticmethod\ndef Aggregate_Region_Stats(\n    Page_Spatial_Descriptors: Dict[str, dict],\n    Page_Categories: Dict[str, str],\n) -&gt; Dict[str, dict]:\n    \"\"\"Aggregate per-page spatial descriptors into per-category stats.\n\n    Groups each page's Spatial_Analyzer.Extract_Spatial_Descriptors()\n    output (keyed by compositional class id, with the Spanish keys\n    \"Num_Regiones\" and \"Tamano_Promedio\") by its structural category,\n    and averages them per class.\n\n    Args:\n        Page_Spatial_Descriptors: Mapping of page id to a dict of\n            {class_k: {\"Num_Regiones\": float, \"Tamano_Promedio\": float}},\n            matching Spatial_Analyzer.Extract_Spatial_Descriptors's\n            return schema.\n        Page_Categories: Mapping of page id to its structural category\n            tag.\n\n    Returns:\n        Dict[str, dict]: {category: {class_k: {mean_region_count,\n        mean_region_size}}}.\n\n    Raises:\n        ValueError: If a page in Page_Categories has no matching entry\n            in Page_Spatial_Descriptors.\n    \"\"\"\n    Grouped: Dict[str, list] = {}\n    for Page_Id, Category in Page_Categories.items():\n        if Page_Id not in Page_Spatial_Descriptors:\n            raise ValueError(\n                f\"Page '{Page_Id}' listed in Page_Categories has no \"\n                \"matching entry in Page_Spatial_Descriptors.\"\n            )\n        Grouped.setdefault(Category, []).append(\n            Page_Spatial_Descriptors[Page_Id]\n        )\n\n    Category_Stats: Dict[str, dict] = {}\n    for Category, Descriptor_List in Grouped.items():\n        Class_Ids = sorted(\n            {Class_Id for Descriptors in Descriptor_List for Class_Id in Descriptors}\n        )\n\n        Class_Stats = {}\n        for Class_Id in Class_Ids:\n            Region_Counts = [\n                Descriptors[Class_Id][\"Num_Regiones\"]\n                for Descriptors in Descriptor_List\n                if Class_Id in Descriptors\n            ]\n            Region_Sizes = [\n                Descriptors[Class_Id][\"Tamano_Promedio\"]\n                for Descriptors in Descriptor_List\n                if Class_Id in Descriptors\n            ]\n            Class_Stats[Class_Id] = {\n                \"mean_region_count\": float(np.mean(Region_Counts)),\n                \"mean_region_size\": float(np.mean(Region_Sizes)),\n            }\n\n        Category_Stats[Category] = Class_Stats\n\n    return Category_Stats\n</code></pre>"},{"location":"api/xrf/#config","title":"Config","text":""},{"location":"api/xrf/#xrf.config","title":"<code>xrf.config</code>","text":"<p>Configuration module for the X-Ray Fluorescence (XRF) pipeline. Defines hyperparameters for preprocessing, segmentation, and spatial analysis.</p> <p>Classes:</p> Name Description <code>Xrf_Preprocessing_Config</code> <p>Configuration for XRF data reading and preprocessing.</p> <code>Xrf_Segmentation_Config</code> <p>Configuration for dimensionality reduction and GMM clustering.</p> <code>Leaf_Signature_Config</code> <p>Configuration for leaf signature (F_h) extraction and spatial descriptors.</p> <code>Xrf_Comparison_Config</code> <p>Configuration for category-level XRF page comparison.</p> <code>Bcf_Extraction_Config</code> <p>Hyperparameters for BCF-to-elemental-TIFF extraction.</p>"},{"location":"api/xrf/#xrf.config-classes","title":"Classes","text":""},{"location":"api/xrf/#xrf.config.Xrf_Preprocessing_Config","title":"<code>Xrf_Preprocessing_Config(Noise_Threshold=5.0, Zero_Replacement_Delta=0.0001, Compute_Dtype='float64')</code>  <code>dataclass</code>","text":"<p>Configuration for XRF data reading and preprocessing.</p> <p>Parameters:</p> Name Type Description Default <code>Noise_Threshold</code> <code>float</code> <p>Minimum accumulated intensity threshold tau_noise to consider a pixel valid. Defaults to 5.0.</p> <code>5.0</code> <code>Zero_Replacement_Delta</code> <code>float</code> <p>Tiny delta constant to replace zeros before the CLR transformation. Defaults to 1e-4.</p> <code>0.0001</code> <code>Compute_Dtype</code> <code>str</code> <p>Numeric precision for computations. Defaults to 'float64' to avoid instability in logarithms.</p> <code>'float64'</code>"},{"location":"api/xrf/#xrf.config.Xrf_Segmentation_Config","title":"<code>Xrf_Segmentation_Config(Pca_Variance_Ratio=0.95, Gmm_Min_K=2, Gmm_Max_K=8, Covariance_Type='full')</code>  <code>dataclass</code>","text":"<p>Configuration for dimensionality reduction and GMM clustering.</p> <p>Parameters:</p> Name Type Description Default <code>Pca_Variance_Ratio</code> <code>float</code> <p>Fraction of cumulative explained variance to retain in PCA reduction. Range (0, 1]. Defaults to 0.95.</p> <code>0.95</code> <code>Gmm_Min_K</code> <code>int</code> <p>Minimum number of compositional classes K to evaluate. Defaults to 2.</p> <code>2</code> <code>Gmm_Max_K</code> <code>int</code> <p>Maximum number of compositional classes K to evaluate. Defaults to 8.</p> <code>8</code> <code>Covariance_Type</code> <code>str</code> <p>Covariance matrix type for the GMM (full, tied, diag, spherical). Defaults to 'full'.</p> <code>'full'</code>"},{"location":"api/xrf/#xrf.config.Leaf_Signature_Config","title":"<code>Leaf_Signature_Config(Connectivity=8, Min_Region_Size=10)</code>  <code>dataclass</code>","text":"<p>Configuration for leaf signature (F_h) extraction and spatial descriptors.</p> <p>Parameters:</p> Name Type Description Default <code>Connectivity</code> <code>int</code> <p>Number of neighbors for the connected-components algorithm (4 or 8 for 2D). Defaults to 8.</p> <code>8</code> <code>Min_Region_Size</code> <code>int</code> <p>Minimum number of pixels to consider a region valid in morphological analysis. Defaults to 10.</p> <code>10</code>"},{"location":"api/xrf/#xrf.config.Xrf_Comparison_Config","title":"<code>Xrf_Comparison_Config(Allowed_Categories=(lambda: ['text_only', 'chapter_start', 'illustration', 'mixed', 'unknown'])(), Min_Pages_Per_Category=5, Rarity_Mad_Threshold=3.5, Min_Region_Size=10)</code>  <code>dataclass</code>","text":"<p>Configuration for category-level XRF page comparison.</p> <p>Attributes:</p> Name Type Description <code>Allowed_Categories</code> <code>List[str]</code> <p>Fixed vocabulary of structural page-category labels. Kept as an explicit, editable list rather than an enum so new categories can be added without a code change.</p> <code>Min_Pages_Per_Category</code> <code>int</code> <p>Categories with fewer tagged pages than this still get computed, but are flagged low-confidence everywhere they're reported (plots, summary json).</p> <code>Rarity_Mad_Threshold</code> <code>float</code> <p>Robust z-score magnitude above which a page is flagged as \"rare\" relative to its category. This is a triage threshold for human review, not a significance level.</p> <code>Min_Region_Size</code> <code>int</code> <p>Passed through to spatial comparison; mirrors Leaf_Signature_Config.Min_Region_Size for consistency.</p>"},{"location":"api/xrf/#xrf.config.Bcf_Extraction_Config","title":"<code>Bcf_Extraction_Config(Cutoff_At_Kv=40.0, Peak_Width_Kev=0.2, Bg_Width_Kev=0.1, Bg_Offset_Kev=0.25, Output_Dir=(lambda: Path('data', 'xrf', 'raw'))())</code>  <code>dataclass</code>","text":"<p>Hyperparameters for BCF-to-elemental-TIFF extraction.</p> <p>Controls dual-window Bremsstrahlung subtraction window geometry and the detector energy cutoff. All energy values in keV.</p> <p>Parameters:</p> Name Type Description Default <code>Cutoff_At_Kv</code> <code>float</code> <p>Detector energy ceiling; channels above this are discarded during BCF loading. Defaults to 40.0.</p> <code>40.0</code> <code>Peak_Width_Kev</code> <code>float</code> <p>Half-width of the integration window centered on the emission line. Defaults to 0.20.</p> <code>0.2</code> <code>Bg_Width_Kev</code> <code>float</code> <p>Half-width of each background-sideband window used for continuum estimation. Defaults to 0.10.</p> <code>0.1</code> <code>Bg_Offset_Kev</code> <code>float</code> <p>Distance from the line center to each sideband center. Defaults to 0.25.</p> <code>0.25</code> <code>Output_Dir</code> <code>Path</code> <p>Directory where extracted element TIFFs are written. Defaults to <code>data/xrf/raw/</code> relative to the project root.</p> <code>(lambda: Path('data', 'xrf', 'raw'))()</code>"},{"location":"guides/contributing/","title":"Contributing","text":""},{"location":"guides/contributing/#code-style","title":"Code style","text":"<ul> <li>Black formatter, line length 100</li> <li>Google-style docstrings on all public APIs</li> <li>Type hints on all function signatures</li> <li>Pascal_Case_With_Underscores for classes</li> </ul>"},{"location":"guides/contributing/#before-committing","title":"Before committing","text":"<pre><code>black src/ tests/\nmypy src/\npytest\n</code></pre>"},{"location":"guides/contributing/#adding-documentation","title":"Adding documentation","text":"<ol> <li>Write your markdown file in <code>docs/</code> or <code>docs/guides/</code></li> <li>Add it to the <code>nav:</code> tree in <code>mkdocs.yml</code></li> <li>Run <code>mkdocs serve</code> to preview locally</li> <li>Run <code>mkdocs build</code> to generate the static site</li> </ol>"},{"location":"guides/ct_pipeline/","title":"CT Pipeline Guide","text":"<p>End-to-end walkthrough of the micro-CT segmentation pipeline.</p>"},{"location":"guides/ct_pipeline/#overview","title":"Overview","text":"<ol> <li>Raw data loading \u2014 TIFF stacks to 3D numpy array</li> <li>Preprocessing \u2014 Background correction, Gaussian smoothing, Percentile normalization</li> <li>Segmentation \u2014 Flat GMM (BIC), Hierarchical refinement, HMRF spatial regularization</li> <li>Analysis \u2014 Material statistics, uncertainty maps, entropy, margin</li> <li>Export \u2014 Labels, probabilities, napari, Dragonfly TIFF</li> </ol>"},{"location":"guides/ct_pipeline/#notebooks","title":"Notebooks","text":"Notebook Purpose <code>01_explore_raw_data.ipynb</code> Inspect raw slices and histograms <code>02_run_preprocessing.ipynb</code> Run pipeline_revised, diagnostics <code>03_gmm_and_hierarchical_segmentation.ipynb</code> GMM fitting, BIC, hierarchy <code>04_spatial_hmrf.ipynb</code> HMRF parameter tuning <code>05_uncertainty_and_visualization.ipynb</code> Material stats, napari, exports <p>See the interactive notebooks in the <code>notebooks/</code> directory.</p>"},{"location":"guides/installation/","title":"Installation","text":""},{"location":"guides/installation/#requirements","title":"Requirements","text":"<ul> <li>Python 3.10+</li> <li>Windows, macOS, or Linux</li> <li>32 GB RAM minimum for CT pipeline (larger volumes use chunked processing)</li> </ul>"},{"location":"guides/installation/#setup-from-source","title":"Setup from source","text":"<pre><code>python -m venv venv\nvenv\\Scripts\\activate          # Windows\nsource venv/bin/activate       # Linux/macOS\n\npip install -e .\npip install -r requirements.txt\n</code></pre>"},{"location":"guides/installation/#documentation-dependencies-optional","title":"Documentation dependencies (optional)","text":"<pre><code>pip install mkdocs mkdocs-material mkdocstrings[python] mkdocs-jupyter\n</code></pre>"},{"location":"guides/installation/#verify-installation","title":"Verify installation","text":"<pre><code>python -c \"from research_ct.segmentation.gmm_fitter import Gmm_Fitter; print('CT OK')\"\npython -c \"from xrf.io.xrf_loader import Xrf_Loader; print('XRF OK')\"\npytest\n</code></pre>"},{"location":"guides/notebooks/","title":"Notebooks","text":"<p>All interactive demos live in <code>notebooks/</code>.</p>"},{"location":"guides/notebooks/#ct-notebooks","title":"CT Notebooks","text":"File Purpose <code>01_explore_raw_data.ipynb</code> Load raw TIFF, inspect histograms, metadata <code>02_run_preprocessing.ipynb</code> Run pipeline_revised, histogram diagnostics <code>03_gmm_and_hierarchical_segmentation.ipynb</code> BIC-GMM, Sparse Bayesian GMM, hierarchy, streaming export <code>04_spatial_hmrf.ipynb</code> HMRF on test region, GMM vs HMRF comparison <code>05_uncertainty_and_visualization.ipynb</code> Material stats, uncertainty maps, napari 3D, video export"},{"location":"guides/notebooks/#xrf-notebooks","title":"XRF Notebooks","text":"File Purpose <code>01_xrf_loading_and_masking.ipynb</code> Load elemental TIFFs, compute intensity mask <code>02_coda_transformations.ipynb</code> CLR transformation, zero replacement <code>03_gmm_spatial_clustering.ipynb</code> PCA reduction, GMM clustering, 2D class map <code>04_leaf_signatures.ipynb</code> Per-page leaf signature F_h computation <code>05_page_categorization.ipynb</code> Structural category assignment <code>06_category_signature_comparison.ipynb</code> Category-level compositional norms <code>07_rarity_review.ipynb</code> Robust z-score rarity flagging <code>xrf_bcf_extraction.ipynb</code> Bruker BCF hypercube to elemental TIFF extraction"},{"location":"guides/notebooks/#view-in-docs-site","title":"View in docs site","text":"<p>Notebooks are rendered automatically by <code>mkdocs-jupyter</code> when the docs site is built.</p>"},{"location":"guides/quickstart/","title":"Quick Start","text":""},{"location":"guides/quickstart/#ct-pipeline-research_ct","title":"CT Pipeline (research_ct)","text":"<pre><code>from research_ct.io.volume_loader import Load_Slice_Stack\nfrom research_ct.preprocessing.pipeline_revised import Preprocess_For_Gmm_Revised\nfrom research_ct.segmentation.gmm_fitter import Gmm_Fitter\n\nVolume = Load_Slice_Stack(\"data/raw/\")\nPreprocessed, Diagnostics = Preprocess_For_Gmm_Revised(Volume)\nGmm = Gmm_Fitter(Min_Components=2, Max_Components=8)\nGmm.Fit(Preprocessed.reshape(-1, 1))\nLabels = Gmm.Predict_Labels(Preprocessed.reshape(-1, 1)).reshape(Volume.shape)\nProbabilities = Gmm.Predict_Probabilities(Preprocessed.reshape(-1, 1))\n</code></pre>"},{"location":"guides/quickstart/#xrf-pipeline-xrf","title":"XRF Pipeline (xrf)","text":"<pre><code>from xrf.io.xrf_loader import Xrf_Loader\nfrom xrf.transforms.coda import Clr_Transformer\nfrom xrf.segmentation.xrf_gmm import Xrf_Gmm_Segmenter\n\nStack = Xrf_Loader.Load_Element_Stack([\n    \"data/xrf/raw/Letter_1_Pb_La.tiff\",\n    \"data/xrf/raw/Letter_1_Fe_Ka.tiff\",\n])\nMask = Xrf_Loader.Compute_Intensity_Mask(Stack, Tau_Noise=5.0)\nValid_Pixels = Stack[Mask]\nClr_Data = Clr_Transformer.Apply_Clr_Transform(Valid_Pixels)\nLabels, Probs, Pca, Gmm = Xrf_Gmm_Segmenter.Fit_Predict(\n    Clr_Data, Num_Components=7, Variance_Ratio=0.95\n)\n</code></pre>"},{"location":"guides/xrf_pipeline/","title":"XRF Pipeline Guide","text":"<p>End-to-end walkthrough of the X-Ray Fluorescence elemental analysis pipeline.</p>"},{"location":"guides/xrf_pipeline/#overview","title":"Overview","text":"<ol> <li>Element loading \u2014 Per-element TIFFs to (M, N, n_elements) cube</li> <li>Masking \u2014 Threshold on total intensity to discard background/noise pixels</li> <li>CLR transform \u2014 Zero-replace, normalize, Centered Log-Ratio projection</li> <li>PCA reduction \u2014 Retain 95% cumulative variance</li> <li>GMM clustering \u2014 Discover compositional classes K in latent space</li> <li>Spatial analysis \u2014 Connected-component descriptors per class</li> <li>Signatures \u2014 Build per-page leaf signature F_h and book-level weighted average</li> <li>Comparison \u2014 Category norms + rarity scoring against structural categories</li> </ol>"}]}